var Zc = (e, t) => () => (t || e((t = { exports: {} }).exports, t), t.exports);
var bb = Zc((ie, se) => {
  function qs(e, t) {
    return function() {
      return e.apply(t, arguments);
    };
  }
  const { toString: Yc } = Object.prototype, { getPrototypeOf: vn } = Object, { iterator: Tn, toStringTag: Gs } = Symbol, Cn = /* @__PURE__ */ ((e) => (t) => {
    const n = Yc.call(t);
    return e[n] || (e[n] = n.slice(8, -1).toLowerCase());
  })(/* @__PURE__ */ Object.create(null)), he = (e) => (e = e.toLowerCase(), (t) => Cn(t) === e), Rn = (e) => (t) => typeof t === e, { isArray: dt } = Array, ot = Rn("undefined");
  function Ft(e) {
    return e !== null && !ot(e) && e.constructor !== null && !ot(e.constructor) && te(e.constructor.isBuffer) && e.constructor.isBuffer(e);
  }
  const Js = he("ArrayBuffer");
  function Xc(e) {
    let t;
    return typeof ArrayBuffer < "u" && ArrayBuffer.isView ? t = ArrayBuffer.isView(e) : t = e && e.buffer && Js(e.buffer), t;
  }
  const Qc = Rn("string"), te = Rn("function"), Ks = Rn("number"), jt = (e) => e !== null && typeof e == "object", eu = (e) => e === !0 || e === !1, cn = (e) => {
    if (Cn(e) !== "object")
      return !1;
    const t = vn(e);
    return (t === null || t === Object.prototype || Object.getPrototypeOf(t) === null) && !(Gs in e) && !(Tn in e);
  }, tu = (e) => {
    if (!jt(e) || Ft(e))
      return !1;
    try {
      return Object.keys(e).length === 0 && Object.getPrototypeOf(e) === Object.prototype;
    } catch {
      return !1;
    }
  }, nu = he("Date"), ru = he("File"), ou = (e) => !!(e && typeof e.uri < "u"), iu = (e) => e && typeof e.getParts < "u", su = he("Blob"), au = he("FileList"), cu = (e) => jt(e) && te(e.pipe);
  function uu() {
    return typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : typeof global < "u" ? global : {};
  }
  const No = uu(), Lo = typeof No.FormData < "u" ? No.FormData : void 0, lu = (e) => {
    if (!e) return !1;
    if (Lo && e instanceof Lo) return !0;
    const t = vn(e);
    if (!t || t === Object.prototype || !te(e.append)) return !1;
    const n = Cn(e);
    return n === "formdata" || // detect form-data instance
    n === "object" && te(e.toString) && e.toString() === "[object FormData]";
  }, fu = he("URLSearchParams"), [du, pu, hu, mu] = [
    "ReadableStream",
    "Request",
    "Response",
    "Headers"
  ].map(he), gu = (e) => e.trim ? e.trim() : e.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, "");
  function Bt(e, t, { allOwnKeys: n = !1 } = {}) {
    if (e === null || typeof e > "u")
      return;
    let r, o;
    if (typeof e != "object" && (e = [e]), dt(e))
      for (r = 0, o = e.length; r < o; r++)
        t.call(null, e[r], r, e);
    else {
      if (Ft(e))
        return;
      const i = n ? Object.getOwnPropertyNames(e) : Object.keys(e), s = i.length;
      let a;
      for (r = 0; r < s; r++)
        a = i[r], t.call(null, e[a], a, e);
    }
  }
  function Zs(e, t) {
    if (Ft(e))
      return null;
    t = t.toLowerCase();
    const n = Object.keys(e);
    let r = n.length, o;
    for (; r-- > 0; )
      if (o = n[r], t === o.toLowerCase())
        return o;
    return null;
  }
  const Ue = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : global, Ys = (e) => !ot(e) && e !== Ue;
  function yr() {
    const { caseless: e, skipUndefined: t } = Ys(this) && this || {}, n = {}, r = (o, i) => {
      if (i === "__proto__" || i === "constructor" || i === "prototype")
        return;
      const s = e && Zs(n, i) || i;
      cn(n[s]) && cn(o) ? n[s] = yr(n[s], o) : cn(o) ? n[s] = yr({}, o) : dt(o) ? n[s] = o.slice() : (!t || !ot(o)) && (n[s] = o);
    };
    for (let o = 0, i = arguments.length; o < i; o++)
      arguments[o] && Bt(arguments[o], r);
    return n;
  }
  const yu = (e, t, n, { allOwnKeys: r } = {}) => (Bt(
    t,
    (o, i) => {
      n && te(o) ? Object.defineProperty(e, i, {
        value: qs(o, n),
        writable: !0,
        enumerable: !0,
        configurable: !0
      }) : Object.defineProperty(e, i, {
        value: o,
        writable: !0,
        enumerable: !0,
        configurable: !0
      });
    },
    { allOwnKeys: r }
  ), e), Au = (e) => (e.charCodeAt(0) === 65279 && (e = e.slice(1)), e), _u = (e, t, n, r) => {
    e.prototype = Object.create(t.prototype, r), Object.defineProperty(e.prototype, "constructor", {
      value: e,
      writable: !0,
      enumerable: !1,
      configurable: !0
    }), Object.defineProperty(e, "super", {
      value: t.prototype
    }), n && Object.assign(e.prototype, n);
  }, Eu = (e, t, n, r) => {
    let o, i, s;
    const a = {};
    if (t = t || {}, e == null) return t;
    do {
      for (o = Object.getOwnPropertyNames(e), i = o.length; i-- > 0; )
        s = o[i], (!r || r(s, e, t)) && !a[s] && (t[s] = e[s], a[s] = !0);
      e = n !== !1 && vn(e);
    } while (e && (!n || n(e, t)) && e !== Object.prototype);
    return t;
  }, bu = (e, t, n) => {
    e = String(e), (n === void 0 || n > e.length) && (n = e.length), n -= t.length;
    const r = e.indexOf(t, n);
    return r !== -1 && r === n;
  }, Su = (e) => {
    if (!e) return null;
    if (dt(e)) return e;
    let t = e.length;
    if (!Ks(t)) return null;
    const n = new Array(t);
    for (; t-- > 0; )
      n[t] = e[t];
    return n;
  }, wu = /* @__PURE__ */ ((e) => (t) => e && t instanceof e)(typeof Uint8Array < "u" && vn(Uint8Array)), vu = (e, t) => {
    const r = (e && e[Tn]).call(e);
    let o;
    for (; (o = r.next()) && !o.done; ) {
      const i = o.value;
      t.call(e, i[0], i[1]);
    }
  }, Tu = (e, t) => {
    let n;
    const r = [];
    for (; (n = e.exec(t)) !== null; )
      r.push(n);
    return r;
  }, Cu = he("HTMLFormElement"), Ru = (e) => e.toLowerCase().replace(/[-_\s]([a-z\d])(\w*)/g, function(n, r, o) {
    return r.toUpperCase() + o;
  }), xo = (({ hasOwnProperty: e }) => (t, n) => e.call(t, n))(Object.prototype), Iu = he("RegExp"), Xs = (e, t) => {
    const n = Object.getOwnPropertyDescriptors(e), r = {};
    Bt(n, (o, i) => {
      let s;
      (s = t(o, i, e)) !== !1 && (r[i] = s || o);
    }), Object.defineProperties(e, r);
  }, Ou = (e) => {
    Xs(e, (t, n) => {
      if (te(e) && ["arguments", "caller", "callee"].indexOf(n) !== -1)
        return !1;
      const r = e[n];
      if (te(r)) {
        if (t.enumerable = !1, "writable" in t) {
          t.writable = !1;
          return;
        }
        t.set || (t.set = () => {
          throw Error("Can not rewrite read-only method '" + n + "'");
        });
      }
    });
  }, Mu = (e, t) => {
    const n = {}, r = (o) => {
      o.forEach((i) => {
        n[i] = !0;
      });
    };
    return dt(e) ? r(e) : r(String(e).split(t)), n;
  }, Pu = () => {
  }, Nu = (e, t) => e != null && Number.isFinite(e = +e) ? e : t;
  function Lu(e) {
    return !!(e && te(e.append) && e[Gs] === "FormData" && e[Tn]);
  }
  const xu = (e) => {
    const t = new Array(10), n = (r, o) => {
      if (jt(r)) {
        if (t.indexOf(r) >= 0)
          return;
        if (Ft(r))
          return r;
        if (!("toJSON" in r)) {
          t[o] = r;
          const i = dt(r) ? [] : {};
          return Bt(r, (s, a) => {
            const c = n(s, o + 1);
            !ot(c) && (i[a] = c);
          }), t[o] = void 0, i;
        }
      }
      return r;
    };
    return n(e, 0);
  }, ku = he("AsyncFunction"), Du = (e) => e && (jt(e) || te(e)) && te(e.then) && te(e.catch), Qs = ((e, t) => e ? setImmediate : t ? ((n, r) => (Ue.addEventListener(
    "message",
    ({ source: o, data: i }) => {
      o === Ue && i === n && r.length && r.shift()();
    },
    !1
  ), (o) => {
    r.push(o), Ue.postMessage(n, "*");
  }))(`axios@${Math.random()}`, []) : (n) => setTimeout(n))(typeof setImmediate == "function", te(Ue.postMessage)), $u = typeof queueMicrotask < "u" ? queueMicrotask.bind(Ue) : typeof process < "u" && process.nextTick || Qs, Fu = (e) => e != null && te(e[Tn]), m = {
    isArray: dt,
    isArrayBuffer: Js,
    isBuffer: Ft,
    isFormData: lu,
    isArrayBufferView: Xc,
    isString: Qc,
    isNumber: Ks,
    isBoolean: eu,
    isObject: jt,
    isPlainObject: cn,
    isEmptyObject: tu,
    isReadableStream: du,
    isRequest: pu,
    isResponse: hu,
    isHeaders: mu,
    isUndefined: ot,
    isDate: nu,
    isFile: ru,
    isReactNativeBlob: ou,
    isReactNative: iu,
    isBlob: su,
    isRegExp: Iu,
    isFunction: te,
    isStream: cu,
    isURLSearchParams: fu,
    isTypedArray: wu,
    isFileList: au,
    forEach: Bt,
    merge: yr,
    extend: yu,
    trim: gu,
    stripBOM: Au,
    inherits: _u,
    toFlatObject: Eu,
    kindOf: Cn,
    kindOfTest: he,
    endsWith: bu,
    toArray: Su,
    forEachEntry: vu,
    matchAll: Tu,
    isHTMLForm: Cu,
    hasOwnProperty: xo,
    hasOwnProp: xo,
    // an alias to avoid ESLint no-prototype-builtins detection
    reduceDescriptors: Xs,
    freezeMethods: Ou,
    toObjectSet: Mu,
    toCamelCase: Ru,
    noop: Pu,
    toFiniteNumber: Nu,
    findKey: Zs,
    global: Ue,
    isContextDefined: Ys,
    isSpecCompliantForm: Lu,
    toJSONObject: xu,
    isAsyncFn: ku,
    isThenable: Du,
    setImmediate: Qs,
    asap: $u,
    isIterable: Fu
  };
  let R = class ea extends Error {
    static from(t, n, r, o, i, s) {
      const a = new ea(t.message, n || t.code, r, o, i);
      return a.cause = t, a.name = t.name, t.status != null && a.status == null && (a.status = t.status), s && Object.assign(a, s), a;
    }
    /**
     * Create an Error with the specified message, config, error code, request and response.
     *
     * @param {string} message The error message.
     * @param {string} [code] The error code (for example, 'ECONNABORTED').
     * @param {Object} [config] The config.
     * @param {Object} [request] The request.
     * @param {Object} [response] The response.
     *
     * @returns {Error} The created error.
     */
    constructor(t, n, r, o, i) {
      super(t), Object.defineProperty(this, "message", {
        value: t,
        enumerable: !0,
        writable: !0,
        configurable: !0
      }), this.name = "AxiosError", this.isAxiosError = !0, n && (this.code = n), r && (this.config = r), o && (this.request = o), i && (this.response = i, this.status = i.status);
    }
    toJSON() {
      return {
        // Standard
        message: this.message,
        name: this.name,
        // Microsoft
        description: this.description,
        number: this.number,
        // Mozilla
        fileName: this.fileName,
        lineNumber: this.lineNumber,
        columnNumber: this.columnNumber,
        stack: this.stack,
        // Axios
        config: m.toJSONObject(this.config),
        code: this.code,
        status: this.status
      };
    }
  };
  R.ERR_BAD_OPTION_VALUE = "ERR_BAD_OPTION_VALUE";
  R.ERR_BAD_OPTION = "ERR_BAD_OPTION";
  R.ECONNABORTED = "ECONNABORTED";
  R.ETIMEDOUT = "ETIMEDOUT";
  R.ERR_NETWORK = "ERR_NETWORK";
  R.ERR_FR_TOO_MANY_REDIRECTS = "ERR_FR_TOO_MANY_REDIRECTS";
  R.ERR_DEPRECATED = "ERR_DEPRECATED";
  R.ERR_BAD_RESPONSE = "ERR_BAD_RESPONSE";
  R.ERR_BAD_REQUEST = "ERR_BAD_REQUEST";
  R.ERR_CANCELED = "ERR_CANCELED";
  R.ERR_NOT_SUPPORT = "ERR_NOT_SUPPORT";
  R.ERR_INVALID_URL = "ERR_INVALID_URL";
  R.ERR_FORM_DATA_DEPTH_EXCEEDED = "ERR_FORM_DATA_DEPTH_EXCEEDED";
  const ju = null;
  function Ar(e) {
    return m.isPlainObject(e) || m.isArray(e);
  }
  function ta(e) {
    return m.endsWith(e, "[]") ? e.slice(0, -2) : e;
  }
  function Wn(e, t, n) {
    return e ? e.concat(t).map(function(o, i) {
      return o = ta(o), !n && i ? "[" + o + "]" : o;
    }).join(n ? "." : "") : t;
  }
  function Bu(e) {
    return m.isArray(e) && !e.some(Ar);
  }
  const Uu = m.toFlatObject(m, {}, null, function(t) {
    return /^is[A-Z]/.test(t);
  });
  function In(e, t, n) {
    if (!m.isObject(e))
      throw new TypeError("target must be an object");
    t = t || new FormData(), n = m.toFlatObject(
      n,
      {
        metaTokens: !0,
        dots: !1,
        indexes: !1
      },
      !1,
      function(g, _) {
        return !m.isUndefined(_[g]);
      }
    );
    const r = n.metaTokens, o = n.visitor || f, i = n.dots, s = n.indexes, a = n.Blob || typeof Blob < "u" && Blob, c = n.maxDepth === void 0 ? 100 : n.maxDepth, u = a && m.isSpecCompliantForm(t);
    if (!m.isFunction(o))
      throw new TypeError("visitor must be a function");
    function l(h) {
      if (h === null) return "";
      if (m.isDate(h))
        return h.toISOString();
      if (m.isBoolean(h))
        return h.toString();
      if (!u && m.isBlob(h))
        throw new R("Blob is not supported. Use a Buffer instead.");
      return m.isArrayBuffer(h) || m.isTypedArray(h) ? u && typeof Blob == "function" ? new Blob([h]) : Buffer.from(h) : h;
    }
    function f(h, g, _) {
      let v = h;
      if (m.isReactNative(t) && m.isReactNativeBlob(h))
        return t.append(Wn(_, g, i), l(h)), !1;
      if (h && !_ && typeof h == "object") {
        if (m.endsWith(g, "{}"))
          g = r ? g : g.slice(0, -2), h = JSON.stringify(h);
        else if (m.isArray(h) && Bu(h) || (m.isFileList(h) || m.endsWith(g, "[]")) && (v = m.toArray(h)))
          return g = ta(g), v.forEach(function(T, I) {
            !(m.isUndefined(T) || T === null) && t.append(
              // eslint-disable-next-line no-nested-ternary
              s === !0 ? Wn([g], I, i) : s === null ? g : g + "[]",
              l(T)
            );
          }), !1;
      }
      return Ar(h) ? !0 : (t.append(Wn(_, g, i), l(h)), !1);
    }
    const p = [], d = Object.assign(Uu, {
      defaultVisitor: f,
      convertValue: l,
      isVisitable: Ar
    });
    function y(h, g, _ = 0) {
      if (!m.isUndefined(h)) {
        if (_ > c)
          throw new R(
            "Object is too deeply nested (" + _ + " levels). Max depth: " + c,
            R.ERR_FORM_DATA_DEPTH_EXCEEDED
          );
        if (p.indexOf(h) !== -1)
          throw Error("Circular reference detected in " + g.join("."));
        p.push(h), m.forEach(h, function(w, T) {
          (!(m.isUndefined(w) || w === null) && o.call(t, w, m.isString(T) ? T.trim() : T, g, d)) === !0 && y(w, g ? g.concat(T) : [T], _ + 1);
        }), p.pop();
      }
    }
    if (!m.isObject(e))
      throw new TypeError("data must be an object");
    return y(e), t;
  }
  function ko(e) {
    const t = {
      "!": "%21",
      "'": "%27",
      "(": "%28",
      ")": "%29",
      "~": "%7E",
      "%20": "+"
    };
    return encodeURIComponent(e).replace(/[!'()~]|%20/g, function(r) {
      return t[r];
    });
  }
  function Jr(e, t) {
    this._pairs = [], e && In(e, this, t);
  }
  const na = Jr.prototype;
  na.append = function(t, n) {
    this._pairs.push([t, n]);
  };
  na.toString = function(t) {
    const n = t ? function(r) {
      return t.call(this, r, ko);
    } : ko;
    return this._pairs.map(function(o) {
      return n(o[0]) + "=" + n(o[1]);
    }, "").join("&");
  };
  function Hu(e) {
    return encodeURIComponent(e).replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+");
  }
  function ra(e, t, n) {
    if (!t)
      return e;
    const r = n && n.encode || Hu, o = m.isFunction(n) ? {
      serialize: n
    } : n, i = o && o.serialize;
    let s;
    if (i ? s = i(t, o) : s = m.isURLSearchParams(t) ? t.toString() : new Jr(t, o).toString(r), s) {
      const a = e.indexOf("#");
      a !== -1 && (e = e.slice(0, a)), e += (e.indexOf("?") === -1 ? "?" : "&") + s;
    }
    return e;
  }
  class Do {
    constructor() {
      this.handlers = [];
    }
    /**
     * Add a new interceptor to the stack
     *
     * @param {Function} fulfilled The function to handle `then` for a `Promise`
     * @param {Function} rejected The function to handle `reject` for a `Promise`
     * @param {Object} options The options for the interceptor, synchronous and runWhen
     *
     * @return {Number} An ID used to remove interceptor later
     */
    use(t, n, r) {
      return this.handlers.push({
        fulfilled: t,
        rejected: n,
        synchronous: r ? r.synchronous : !1,
        runWhen: r ? r.runWhen : null
      }), this.handlers.length - 1;
    }
    /**
     * Remove an interceptor from the stack
     *
     * @param {Number} id The ID that was returned by `use`
     *
     * @returns {void}
     */
    eject(t) {
      this.handlers[t] && (this.handlers[t] = null);
    }
    /**
     * Clear all interceptors from the stack
     *
     * @returns {void}
     */
    clear() {
      this.handlers && (this.handlers = []);
    }
    /**
     * Iterate over all the registered interceptors
     *
     * This method is particularly useful for skipping over any
     * interceptors that may have become `null` calling `eject`.
     *
     * @param {Function} fn The function to call for each interceptor
     *
     * @returns {void}
     */
    forEach(t) {
      m.forEach(this.handlers, function(r) {
        r !== null && t(r);
      });
    }
  }
  const Kr = {
    silentJSONParsing: !0,
    forcedJSONParsing: !0,
    clarifyTimeoutError: !1,
    legacyInterceptorReqResOrdering: !0
  }, zu = typeof URLSearchParams < "u" ? URLSearchParams : Jr, Wu = typeof FormData < "u" ? FormData : null, Vu = typeof Blob < "u" ? Blob : null, qu = {
    isBrowser: !0,
    classes: {
      URLSearchParams: zu,
      FormData: Wu,
      Blob: Vu
    },
    protocols: ["http", "https", "file", "blob", "url", "data"]
  }, Zr = typeof window < "u" && typeof document < "u", _r = typeof navigator == "object" && navigator || void 0, Gu = Zr && (!_r || ["ReactNative", "NativeScript", "NS"].indexOf(_r.product) < 0), Ju = typeof WorkerGlobalScope < "u" && // eslint-disable-next-line no-undef
  self instanceof WorkerGlobalScope && typeof self.importScripts == "function", Ku = Zr && window.location.href || "http://localhost", Zu = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
    __proto__: null,
    hasBrowserEnv: Zr,
    hasStandardBrowserEnv: Gu,
    hasStandardBrowserWebWorkerEnv: Ju,
    navigator: _r,
    origin: Ku
  }, Symbol.toStringTag, { value: "Module" })), Y = {
    ...Zu,
    ...qu
  };
  function Yu(e, t) {
    return In(e, new Y.classes.URLSearchParams(), {
      visitor: function(n, r, o, i) {
        return Y.isNode && m.isBuffer(n) ? (this.append(r, n.toString("base64")), !1) : i.defaultVisitor.apply(this, arguments);
      },
      ...t
    });
  }
  function Xu(e) {
    return m.matchAll(/\w+|\[(\w*)]/g, e).map((t) => t[0] === "[]" ? "" : t[1] || t[0]);
  }
  function Qu(e) {
    const t = {}, n = Object.keys(e);
    let r;
    const o = n.length;
    let i;
    for (r = 0; r < o; r++)
      i = n[r], t[i] = e[i];
    return t;
  }
  function oa(e) {
    function t(n, r, o, i) {
      let s = n[i++];
      if (s === "__proto__") return !0;
      const a = Number.isFinite(+s), c = i >= n.length;
      return s = !s && m.isArray(o) ? o.length : s, c ? (m.hasOwnProp(o, s) ? o[s] = m.isArray(o[s]) ? o[s].concat(r) : [o[s], r] : o[s] = r, !a) : ((!o[s] || !m.isObject(o[s])) && (o[s] = []), t(n, r, o[s], i) && m.isArray(o[s]) && (o[s] = Qu(o[s])), !a);
    }
    if (m.isFormData(e) && m.isFunction(e.entries)) {
      const n = {};
      return m.forEachEntry(e, (r, o) => {
        t(Xu(r), o, n, 0);
      }), n;
    }
    return null;
  }
  const tt = (e, t) => e != null && m.hasOwnProp(e, t) ? e[t] : void 0;
  function el(e, t, n) {
    if (m.isString(e))
      try {
        return (t || JSON.parse)(e), m.trim(e);
      } catch (r) {
        if (r.name !== "SyntaxError")
          throw r;
      }
    return (n || JSON.stringify)(e);
  }
  const Ut = {
    transitional: Kr,
    adapter: ["xhr", "http", "fetch"],
    transformRequest: [
      function(t, n) {
        const r = n.getContentType() || "", o = r.indexOf("application/json") > -1, i = m.isObject(t);
        if (i && m.isHTMLForm(t) && (t = new FormData(t)), m.isFormData(t))
          return o ? JSON.stringify(oa(t)) : t;
        if (m.isArrayBuffer(t) || m.isBuffer(t) || m.isStream(t) || m.isFile(t) || m.isBlob(t) || m.isReadableStream(t))
          return t;
        if (m.isArrayBufferView(t))
          return t.buffer;
        if (m.isURLSearchParams(t))
          return n.setContentType("application/x-www-form-urlencoded;charset=utf-8", !1), t.toString();
        let a;
        if (i) {
          const c = tt(this, "formSerializer");
          if (r.indexOf("application/x-www-form-urlencoded") > -1)
            return Yu(t, c).toString();
          if ((a = m.isFileList(t)) || r.indexOf("multipart/form-data") > -1) {
            const u = tt(this, "env"), l = u && u.FormData;
            return In(
              a ? { "files[]": t } : t,
              l && new l(),
              c
            );
          }
        }
        return i || o ? (n.setContentType("application/json", !1), el(t)) : t;
      }
    ],
    transformResponse: [
      function(t) {
        const n = tt(this, "transitional") || Ut.transitional, r = n && n.forcedJSONParsing, o = tt(this, "responseType"), i = o === "json";
        if (m.isResponse(t) || m.isReadableStream(t))
          return t;
        if (t && m.isString(t) && (r && !o || i)) {
          const a = !(n && n.silentJSONParsing) && i;
          try {
            return JSON.parse(t, tt(this, "parseReviver"));
          } catch (c) {
            if (a)
              throw c.name === "SyntaxError" ? R.from(c, R.ERR_BAD_RESPONSE, this, null, tt(this, "response")) : c;
          }
        }
        return t;
      }
    ],
    /**
     * A timeout in milliseconds to abort a request. If set to 0 (default) a
     * timeout is not created.
     */
    timeout: 0,
    xsrfCookieName: "XSRF-TOKEN",
    xsrfHeaderName: "X-XSRF-TOKEN",
    maxContentLength: -1,
    maxBodyLength: -1,
    env: {
      FormData: Y.classes.FormData,
      Blob: Y.classes.Blob
    },
    validateStatus: function(t) {
      return t >= 200 && t < 300;
    },
    headers: {
      common: {
        Accept: "application/json, text/plain, */*",
        "Content-Type": void 0
      }
    }
  };
  m.forEach(["delete", "get", "head", "post", "put", "patch"], (e) => {
    Ut.headers[e] = {};
  });
  const tl = m.toObjectSet([
    "age",
    "authorization",
    "content-length",
    "content-type",
    "etag",
    "expires",
    "from",
    "host",
    "if-modified-since",
    "if-unmodified-since",
    "last-modified",
    "location",
    "max-forwards",
    "proxy-authorization",
    "referer",
    "retry-after",
    "user-agent"
  ]), nl = (e) => {
    const t = {};
    let n, r, o;
    return e && e.split(`
`).forEach(function(s) {
      o = s.indexOf(":"), n = s.substring(0, o).trim().toLowerCase(), r = s.substring(o + 1).trim(), !(!n || t[n] && tl[n]) && (n === "set-cookie" ? t[n] ? t[n].push(r) : t[n] = [r] : t[n] = t[n] ? t[n] + ", " + r : r);
    }), t;
  }, $o = /* @__PURE__ */ Symbol("internals"), rl = /[^\x09\x20-\x7E\x80-\xFF]/g;
  function ol(e) {
    let t = 0, n = e.length;
    for (; t < n; ) {
      const r = e.charCodeAt(t);
      if (r !== 9 && r !== 32)
        break;
      t += 1;
    }
    for (; n > t; ) {
      const r = e.charCodeAt(n - 1);
      if (r !== 9 && r !== 32)
        break;
      n -= 1;
    }
    return t === 0 && n === e.length ? e : e.slice(t, n);
  }
  function wt(e) {
    return e && String(e).trim().toLowerCase();
  }
  function il(e) {
    return ol(e.replace(rl, ""));
  }
  function un(e) {
    return e === !1 || e == null ? e : m.isArray(e) ? e.map(un) : il(String(e));
  }
  function sl(e) {
    const t = /* @__PURE__ */ Object.create(null), n = /([^\s,;=]+)\s*(?:=\s*([^,;]+))?/g;
    let r;
    for (; r = n.exec(e); )
      t[r[1]] = r[2];
    return t;
  }
  const al = (e) => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(e.trim());
  function Vn(e, t, n, r, o) {
    if (m.isFunction(r))
      return r.call(this, t, n);
    if (o && (t = n), !!m.isString(t)) {
      if (m.isString(r))
        return t.indexOf(r) !== -1;
      if (m.isRegExp(r))
        return r.test(t);
    }
  }
  function cl(e) {
    return e.trim().toLowerCase().replace(/([a-z\d])(\w*)/g, (t, n, r) => n.toUpperCase() + r);
  }
  function ul(e, t) {
    const n = m.toCamelCase(" " + t);
    ["get", "set", "has"].forEach((r) => {
      Object.defineProperty(e, r + n, {
        value: function(o, i, s) {
          return this[r].call(this, t, o, i, s);
        },
        configurable: !0
      });
    });
  }
  let ne = class {
    constructor(t) {
      t && this.set(t);
    }
    set(t, n, r) {
      const o = this;
      function i(a, c, u) {
        const l = wt(c);
        if (!l)
          throw new Error("header name must be a non-empty string");
        const f = m.findKey(o, l);
        (!f || o[f] === void 0 || u === !0 || u === void 0 && o[f] !== !1) && (o[f || c] = un(a));
      }
      const s = (a, c) => m.forEach(a, (u, l) => i(u, l, c));
      if (m.isPlainObject(t) || t instanceof this.constructor)
        s(t, n);
      else if (m.isString(t) && (t = t.trim()) && !al(t))
        s(nl(t), n);
      else if (m.isObject(t) && m.isIterable(t)) {
        let a = {}, c, u;
        for (const l of t) {
          if (!m.isArray(l))
            throw TypeError("Object iterator must return a key-value pair");
          a[u = l[0]] = (c = a[u]) ? m.isArray(c) ? [...c, l[1]] : [c, l[1]] : l[1];
        }
        s(a, n);
      } else
        t != null && i(n, t, r);
      return this;
    }
    get(t, n) {
      if (t = wt(t), t) {
        const r = m.findKey(this, t);
        if (r) {
          const o = this[r];
          if (!n)
            return o;
          if (n === !0)
            return sl(o);
          if (m.isFunction(n))
            return n.call(this, o, r);
          if (m.isRegExp(n))
            return n.exec(o);
          throw new TypeError("parser must be boolean|regexp|function");
        }
      }
    }
    has(t, n) {
      if (t = wt(t), t) {
        const r = m.findKey(this, t);
        return !!(r && this[r] !== void 0 && (!n || Vn(this, this[r], r, n)));
      }
      return !1;
    }
    delete(t, n) {
      const r = this;
      let o = !1;
      function i(s) {
        if (s = wt(s), s) {
          const a = m.findKey(r, s);
          a && (!n || Vn(r, r[a], a, n)) && (delete r[a], o = !0);
        }
      }
      return m.isArray(t) ? t.forEach(i) : i(t), o;
    }
    clear(t) {
      const n = Object.keys(this);
      let r = n.length, o = !1;
      for (; r--; ) {
        const i = n[r];
        (!t || Vn(this, this[i], i, t, !0)) && (delete this[i], o = !0);
      }
      return o;
    }
    normalize(t) {
      const n = this, r = {};
      return m.forEach(this, (o, i) => {
        const s = m.findKey(r, i);
        if (s) {
          n[s] = un(o), delete n[i];
          return;
        }
        const a = t ? cl(i) : String(i).trim();
        a !== i && delete n[i], n[a] = un(o), r[a] = !0;
      }), this;
    }
    concat(...t) {
      return this.constructor.concat(this, ...t);
    }
    toJSON(t) {
      const n = /* @__PURE__ */ Object.create(null);
      return m.forEach(this, (r, o) => {
        r != null && r !== !1 && (n[o] = t && m.isArray(r) ? r.join(", ") : r);
      }), n;
    }
    [Symbol.iterator]() {
      return Object.entries(this.toJSON())[Symbol.iterator]();
    }
    toString() {
      return Object.entries(this.toJSON()).map(([t, n]) => t + ": " + n).join(`
`);
    }
    getSetCookie() {
      return this.get("set-cookie") || [];
    }
    get [Symbol.toStringTag]() {
      return "AxiosHeaders";
    }
    static from(t) {
      return t instanceof this ? t : new this(t);
    }
    static concat(t, ...n) {
      const r = new this(t);
      return n.forEach((o) => r.set(o)), r;
    }
    static accessor(t) {
      const r = (this[$o] = this[$o] = {
        accessors: {}
      }).accessors, o = this.prototype;
      function i(s) {
        const a = wt(s);
        r[a] || (ul(o, s), r[a] = !0);
      }
      return m.isArray(t) ? t.forEach(i) : i(t), this;
    }
  };
  ne.accessor([
    "Content-Type",
    "Content-Length",
    "Accept",
    "Accept-Encoding",
    "User-Agent",
    "Authorization"
  ]);
  m.reduceDescriptors(ne.prototype, ({ value: e }, t) => {
    let n = t[0].toUpperCase() + t.slice(1);
    return {
      get: () => e,
      set(r) {
        this[n] = r;
      }
    };
  });
  m.freezeMethods(ne);
  function qn(e, t) {
    const n = this || Ut, r = t || n, o = ne.from(r.headers);
    let i = r.data;
    return m.forEach(e, function(a) {
      i = a.call(n, i, o.normalize(), t ? t.status : void 0);
    }), o.normalize(), i;
  }
  function ia(e) {
    return !!(e && e.__CANCEL__);
  }
  let Ht = class extends R {
    /**
     * A `CanceledError` is an object that is thrown when an operation is canceled.
     *
     * @param {string=} message The message.
     * @param {Object=} config The config.
     * @param {Object=} request The request.
     *
     * @returns {CanceledError} The created error.
     */
    constructor(t, n, r) {
      super(t ?? "canceled", R.ERR_CANCELED, n, r), this.name = "CanceledError", this.__CANCEL__ = !0;
    }
  };
  function sa(e, t, n) {
    const r = n.config.validateStatus;
    !n.status || !r || r(n.status) ? e(n) : t(
      new R(
        "Request failed with status code " + n.status,
        [R.ERR_BAD_REQUEST, R.ERR_BAD_RESPONSE][Math.floor(n.status / 100) - 4],
        n.config,
        n.request,
        n
      )
    );
  }
  function ll(e) {
    const t = /^([-+\w]{1,25})(:?\/\/|:)/.exec(e);
    return t && t[1] || "";
  }
  function fl(e, t) {
    e = e || 10;
    const n = new Array(e), r = new Array(e);
    let o = 0, i = 0, s;
    return t = t !== void 0 ? t : 1e3, function(c) {
      const u = Date.now(), l = r[i];
      s || (s = u), n[o] = c, r[o] = u;
      let f = i, p = 0;
      for (; f !== o; )
        p += n[f++], f = f % e;
      if (o = (o + 1) % e, o === i && (i = (i + 1) % e), u - s < t)
        return;
      const d = l && u - l;
      return d ? Math.round(p * 1e3 / d) : void 0;
    };
  }
  function dl(e, t) {
    let n = 0, r = 1e3 / t, o, i;
    const s = (u, l = Date.now()) => {
      n = l, o = null, i && (clearTimeout(i), i = null), e(...u);
    };
    return [(...u) => {
      const l = Date.now(), f = l - n;
      f >= r ? s(u, l) : (o = u, i || (i = setTimeout(() => {
        i = null, s(o);
      }, r - f)));
    }, () => o && s(o)];
  }
  const An = (e, t, n = 3) => {
    let r = 0;
    const o = fl(50, 250);
    return dl((i) => {
      const s = i.loaded, a = i.lengthComputable ? i.total : void 0, c = a != null ? Math.min(s, a) : s, u = Math.max(0, c - r), l = o(u);
      r = Math.max(r, c);
      const f = {
        loaded: c,
        total: a,
        progress: a ? c / a : void 0,
        bytes: u,
        rate: l || void 0,
        estimated: l && a ? (a - c) / l : void 0,
        event: i,
        lengthComputable: a != null,
        [t ? "download" : "upload"]: !0
      };
      e(f);
    }, n);
  }, Fo = (e, t) => {
    const n = e != null;
    return [
      (r) => t[0]({
        lengthComputable: n,
        total: e,
        loaded: r
      }),
      t[1]
    ];
  }, jo = (e) => (...t) => m.asap(() => e(...t)), pl = Y.hasStandardBrowserEnv ? /* @__PURE__ */ ((e, t) => (n) => (n = new URL(n, Y.origin), e.protocol === n.protocol && e.host === n.host && (t || e.port === n.port)))(
    new URL(Y.origin),
    Y.navigator && /(msie|trident)/i.test(Y.navigator.userAgent)
  ) : () => !0, hl = Y.hasStandardBrowserEnv ? (
    // Standard browser envs support document.cookie
    {
      write(e, t, n, r, o, i, s) {
        if (typeof document > "u") return;
        const a = [`${e}=${encodeURIComponent(t)}`];
        m.isNumber(n) && a.push(`expires=${new Date(n).toUTCString()}`), m.isString(r) && a.push(`path=${r}`), m.isString(o) && a.push(`domain=${o}`), i === !0 && a.push("secure"), m.isString(s) && a.push(`SameSite=${s}`), document.cookie = a.join("; ");
      },
      read(e) {
        if (typeof document > "u") return null;
        const t = document.cookie.match(new RegExp("(?:^|; )" + e + "=([^;]*)"));
        return t ? decodeURIComponent(t[1]) : null;
      },
      remove(e) {
        this.write(e, "", Date.now() - 864e5, "/");
      }
    }
  ) : (
    // Non-standard browser env (web workers, react-native) lack needed support.
    {
      write() {
      },
      read() {
        return null;
      },
      remove() {
      }
    }
  );
  function ml(e) {
    return typeof e != "string" ? !1 : /^([a-z][a-z\d+\-.]*:)?\/\//i.test(e);
  }
  function gl(e, t) {
    return t ? e.replace(/\/?\/$/, "") + "/" + t.replace(/^\/+/, "") : e;
  }
  function aa(e, t, n) {
    let r = !ml(t);
    return e && (r || n === !1) ? gl(e, t) : t;
  }
  const Bo = (e) => e instanceof ne ? { ...e } : e;
  function We(e, t) {
    t = t || {};
    const n = /* @__PURE__ */ Object.create(null);
    Object.defineProperty(n, "hasOwnProperty", {
      value: Object.prototype.hasOwnProperty,
      enumerable: !1,
      writable: !0,
      configurable: !0
    });
    function r(u, l, f, p) {
      return m.isPlainObject(u) && m.isPlainObject(l) ? m.merge.call({ caseless: p }, u, l) : m.isPlainObject(l) ? m.merge({}, l) : m.isArray(l) ? l.slice() : l;
    }
    function o(u, l, f, p) {
      if (m.isUndefined(l)) {
        if (!m.isUndefined(u))
          return r(void 0, u, f, p);
      } else return r(u, l, f, p);
    }
    function i(u, l) {
      if (!m.isUndefined(l))
        return r(void 0, l);
    }
    function s(u, l) {
      if (m.isUndefined(l)) {
        if (!m.isUndefined(u))
          return r(void 0, u);
      } else return r(void 0, l);
    }
    function a(u, l, f) {
      if (m.hasOwnProp(t, f))
        return r(u, l);
      if (m.hasOwnProp(e, f))
        return r(void 0, u);
    }
    const c = {
      url: i,
      method: i,
      data: i,
      baseURL: s,
      transformRequest: s,
      transformResponse: s,
      paramsSerializer: s,
      timeout: s,
      timeoutMessage: s,
      withCredentials: s,
      withXSRFToken: s,
      adapter: s,
      responseType: s,
      xsrfCookieName: s,
      xsrfHeaderName: s,
      onUploadProgress: s,
      onDownloadProgress: s,
      decompress: s,
      maxContentLength: s,
      maxBodyLength: s,
      beforeRedirect: s,
      transport: s,
      httpAgent: s,
      httpsAgent: s,
      cancelToken: s,
      socketPath: s,
      allowedSocketPaths: s,
      responseEncoding: s,
      validateStatus: a,
      headers: (u, l, f) => o(Bo(u), Bo(l), f, !0)
    };
    return m.forEach(Object.keys({ ...e, ...t }), function(l) {
      if (l === "__proto__" || l === "constructor" || l === "prototype") return;
      const f = m.hasOwnProp(c, l) ? c[l] : o, p = m.hasOwnProp(e, l) ? e[l] : void 0, d = m.hasOwnProp(t, l) ? t[l] : void 0, y = f(p, d, l);
      m.isUndefined(y) && f !== a || (n[l] = y);
    }), n;
  }
  const ca = (e) => {
    const t = We({}, e), n = (p) => m.hasOwnProp(t, p) ? t[p] : void 0, r = n("data");
    let o = n("withXSRFToken");
    const i = n("xsrfHeaderName"), s = n("xsrfCookieName");
    let a = n("headers");
    const c = n("auth"), u = n("baseURL"), l = n("allowAbsoluteUrls"), f = n("url");
    if (t.headers = a = ne.from(a), t.url = ra(
      aa(u, f, l),
      e.params,
      e.paramsSerializer
    ), c && a.set(
      "Authorization",
      "Basic " + btoa(
        (c.username || "") + ":" + (c.password ? unescape(encodeURIComponent(c.password)) : "")
      )
    ), m.isFormData(r)) {
      if (Y.hasStandardBrowserEnv || Y.hasStandardBrowserWebWorkerEnv)
        a.setContentType(void 0);
      else if (m.isFunction(r.getHeaders)) {
        const p = r.getHeaders(), d = ["content-type", "content-length"];
        Object.entries(p).forEach(([y, h]) => {
          d.includes(y.toLowerCase()) && a.set(y, h);
        });
      }
    }
    if (Y.hasStandardBrowserEnv && (m.isFunction(o) && (o = o(t)), o === !0 || o == null && pl(t.url))) {
      const d = i && s && hl.read(s);
      d && a.set(i, d);
    }
    return t;
  }, yl = typeof XMLHttpRequest < "u", Al = yl && function(e) {
    return new Promise(function(n, r) {
      const o = ca(e);
      let i = o.data;
      const s = ne.from(o.headers).normalize();
      let { responseType: a, onUploadProgress: c, onDownloadProgress: u } = o, l, f, p, d, y;
      function h() {
        d && d(), y && y(), o.cancelToken && o.cancelToken.unsubscribe(l), o.signal && o.signal.removeEventListener("abort", l);
      }
      let g = new XMLHttpRequest();
      g.open(o.method.toUpperCase(), o.url, !0), g.timeout = o.timeout;
      function _() {
        if (!g)
          return;
        const w = ne.from(
          "getAllResponseHeaders" in g && g.getAllResponseHeaders()
        ), I = {
          data: !a || a === "text" || a === "json" ? g.responseText : g.response,
          status: g.status,
          statusText: g.statusText,
          headers: w,
          config: e,
          request: g
        };
        sa(
          function(x) {
            n(x), h();
          },
          function(x) {
            r(x), h();
          },
          I
        ), g = null;
      }
      "onloadend" in g ? g.onloadend = _ : g.onreadystatechange = function() {
        !g || g.readyState !== 4 || g.status === 0 && !(g.responseURL && g.responseURL.indexOf("file:") === 0) || setTimeout(_);
      }, g.onabort = function() {
        g && (r(new R("Request aborted", R.ECONNABORTED, e, g)), g = null);
      }, g.onerror = function(T) {
        const I = T && T.message ? T.message : "Network Error", L = new R(I, R.ERR_NETWORK, e, g);
        L.event = T || null, r(L), g = null;
      }, g.ontimeout = function() {
        let T = o.timeout ? "timeout of " + o.timeout + "ms exceeded" : "timeout exceeded";
        const I = o.transitional || Kr;
        o.timeoutErrorMessage && (T = o.timeoutErrorMessage), r(
          new R(
            T,
            I.clarifyTimeoutError ? R.ETIMEDOUT : R.ECONNABORTED,
            e,
            g
          )
        ), g = null;
      }, i === void 0 && s.setContentType(null), "setRequestHeader" in g && m.forEach(s.toJSON(), function(T, I) {
        g.setRequestHeader(I, T);
      }), m.isUndefined(o.withCredentials) || (g.withCredentials = !!o.withCredentials), a && a !== "json" && (g.responseType = o.responseType), u && ([p, y] = An(u, !0), g.addEventListener("progress", p)), c && g.upload && ([f, d] = An(c), g.upload.addEventListener("progress", f), g.upload.addEventListener("loadend", d)), (o.cancelToken || o.signal) && (l = (w) => {
        g && (r(!w || w.type ? new Ht(null, e, g) : w), g.abort(), g = null);
      }, o.cancelToken && o.cancelToken.subscribe(l), o.signal && (o.signal.aborted ? l() : o.signal.addEventListener("abort", l)));
      const v = ll(o.url);
      if (v && Y.protocols.indexOf(v) === -1) {
        r(
          new R(
            "Unsupported protocol " + v + ":",
            R.ERR_BAD_REQUEST,
            e
          )
        );
        return;
      }
      g.send(i || null);
    });
  }, _l = (e, t) => {
    const { length: n } = e = e ? e.filter(Boolean) : [];
    if (t || n) {
      let r = new AbortController(), o;
      const i = function(u) {
        if (!o) {
          o = !0, a();
          const l = u instanceof Error ? u : this.reason;
          r.abort(
            l instanceof R ? l : new Ht(l instanceof Error ? l.message : l)
          );
        }
      };
      let s = t && setTimeout(() => {
        s = null, i(new R(`timeout of ${t}ms exceeded`, R.ETIMEDOUT));
      }, t);
      const a = () => {
        e && (s && clearTimeout(s), s = null, e.forEach((u) => {
          u.unsubscribe ? u.unsubscribe(i) : u.removeEventListener("abort", i);
        }), e = null);
      };
      e.forEach((u) => u.addEventListener("abort", i));
      const { signal: c } = r;
      return c.unsubscribe = () => m.asap(a), c;
    }
  }, El = function* (e, t) {
    let n = e.byteLength;
    if (n < t) {
      yield e;
      return;
    }
    let r = 0, o;
    for (; r < n; )
      o = r + t, yield e.slice(r, o), r = o;
  }, bl = async function* (e, t) {
    for await (const n of Sl(e))
      yield* El(n, t);
  }, Sl = async function* (e) {
    if (e[Symbol.asyncIterator]) {
      yield* e;
      return;
    }
    const t = e.getReader();
    try {
      for (; ; ) {
        const { done: n, value: r } = await t.read();
        if (n)
          break;
        yield r;
      }
    } finally {
      await t.cancel();
    }
  }, Uo = (e, t, n, r) => {
    const o = bl(e, t);
    let i = 0, s, a = (c) => {
      s || (s = !0, r && r(c));
    };
    return new ReadableStream(
      {
        async pull(c) {
          try {
            const { done: u, value: l } = await o.next();
            if (u) {
              a(), c.close();
              return;
            }
            let f = l.byteLength;
            if (n) {
              let p = i += f;
              n(p);
            }
            c.enqueue(new Uint8Array(l));
          } catch (u) {
            throw a(u), u;
          }
        },
        cancel(c) {
          return a(c), o.return();
        }
      },
      {
        highWaterMark: 2
      }
    );
  }, Ho = 64 * 1024, { isFunction: Qt } = m, wl = (({ Request: e, Response: t }) => ({
    Request: e,
    Response: t
  }))(m.global), { ReadableStream: zo, TextEncoder: Wo } = m.global, Vo = (e, ...t) => {
    try {
      return !!e(...t);
    } catch {
      return !1;
    }
  }, vl = (e) => {
    e = m.merge.call(
      {
        skipUndefined: !0
      },
      wl,
      e
    );
    const { fetch: t, Request: n, Response: r } = e, o = t ? Qt(t) : typeof fetch == "function", i = Qt(n), s = Qt(r);
    if (!o)
      return !1;
    const a = o && Qt(zo), c = o && (typeof Wo == "function" ? /* @__PURE__ */ ((y) => (h) => y.encode(h))(new Wo()) : async (y) => new Uint8Array(await new n(y).arrayBuffer())), u = i && a && Vo(() => {
      let y = !1;
      const h = new n(Y.origin, {
        body: new zo(),
        method: "POST",
        get duplex() {
          return y = !0, "half";
        }
      }), g = h.headers.has("Content-Type");
      return h.body != null && h.body.cancel(), y && !g;
    }), l = s && a && Vo(() => m.isReadableStream(new r("").body)), f = {
      stream: l && ((y) => y.body)
    };
    o && ["text", "arrayBuffer", "blob", "formData", "stream"].forEach((y) => {
      !f[y] && (f[y] = (h, g) => {
        let _ = h && h[y];
        if (_)
          return _.call(h);
        throw new R(
          `Response type '${y}' is not supported`,
          R.ERR_NOT_SUPPORT,
          g
        );
      });
    });
    const p = async (y) => {
      if (y == null)
        return 0;
      if (m.isBlob(y))
        return y.size;
      if (m.isSpecCompliantForm(y))
        return (await new n(Y.origin, {
          method: "POST",
          body: y
        }).arrayBuffer()).byteLength;
      if (m.isArrayBufferView(y) || m.isArrayBuffer(y))
        return y.byteLength;
      if (m.isURLSearchParams(y) && (y = y + ""), m.isString(y))
        return (await c(y)).byteLength;
    }, d = async (y, h) => {
      const g = m.toFiniteNumber(y.getContentLength());
      return g ?? p(h);
    };
    return async (y) => {
      let {
        url: h,
        method: g,
        data: _,
        signal: v,
        cancelToken: w,
        timeout: T,
        onDownloadProgress: I,
        onUploadProgress: L,
        responseType: x,
        headers: $,
        withCredentials: J = "same-origin",
        fetchOptions: ae
      } = ca(y), Ee = t || fetch;
      x = x ? (x + "").toLowerCase() : "text";
      let Me = _l(
        [v, w && w.toAbortSignal()],
        T
      ), be = null;
      const me = Me && Me.unsubscribe && (() => {
        Me.unsubscribe();
      });
      let Et;
      try {
        if (L && u && g !== "get" && g !== "head" && (Et = await d($, _)) !== 0) {
          let D = new n(h, {
            method: "POST",
            body: _,
            duplex: "half"
          }), U;
          if (m.isFormData(_) && (U = D.headers.get("content-type")) && $.setContentType(U), D.body) {
            const [k, z] = Fo(
              Et,
              An(jo(L))
            );
            _ = Uo(D.body, Ho, k, z);
          }
        }
        m.isString(J) || (J = J ? "include" : "omit");
        const A = i && "credentials" in n.prototype;
        if (m.isFormData(_)) {
          const D = $.getContentType();
          D && /^multipart\/form-data/i.test(D) && !/boundary=/i.test(D) && $.delete("content-type");
        }
        const E = {
          ...ae,
          signal: Me,
          method: g.toUpperCase(),
          headers: $.normalize().toJSON(),
          body: _,
          duplex: "half",
          credentials: A ? J : void 0
        };
        be = i && new n(h, E);
        let b = await (i ? Ee(be, ae) : Ee(h, E));
        const P = l && (x === "stream" || x === "response");
        if (l && (I || P && me)) {
          const D = {};
          ["status", "statusText", "headers"].forEach((S) => {
            D[S] = b[S];
          });
          const U = m.toFiniteNumber(b.headers.get("content-length")), [k, z] = I && Fo(
            U,
            An(jo(I), !0)
          ) || [];
          b = new r(
            Uo(b.body, Ho, k, () => {
              z && z(), me && me();
            }),
            D
          );
        }
        x = x || "text";
        let N = await f[m.findKey(f, x) || "text"](
          b,
          y
        );
        return !P && me && me(), await new Promise((D, U) => {
          sa(D, U, {
            data: N,
            headers: ne.from(b.headers),
            status: b.status,
            statusText: b.statusText,
            config: y,
            request: be
          });
        });
      } catch (A) {
        throw me && me(), A && A.name === "TypeError" && /Load failed|fetch/i.test(A.message) ? Object.assign(
          new R(
            "Network Error",
            R.ERR_NETWORK,
            y,
            be,
            A && A.response
          ),
          {
            cause: A.cause || A
          }
        ) : R.from(A, A && A.code, y, be, A && A.response);
      }
    };
  }, Tl = /* @__PURE__ */ new Map(), ua = (e) => {
    let t = e && e.env || {};
    const { fetch: n, Request: r, Response: o } = t, i = [r, o, n];
    let s = i.length, a = s, c, u, l = Tl;
    for (; a--; )
      c = i[a], u = l.get(c), u === void 0 && l.set(c, u = a ? /* @__PURE__ */ new Map() : vl(t)), l = u;
    return u;
  };
  ua();
  const Yr = {
    http: ju,
    xhr: Al,
    fetch: {
      get: ua
    }
  };
  m.forEach(Yr, (e, t) => {
    if (e) {
      try {
        Object.defineProperty(e, "name", { value: t });
      } catch {
      }
      Object.defineProperty(e, "adapterName", { value: t });
    }
  });
  const qo = (e) => `- ${e}`, Cl = (e) => m.isFunction(e) || e === null || e === !1;
  function Rl(e, t) {
    e = m.isArray(e) ? e : [e];
    const { length: n } = e;
    let r, o;
    const i = {};
    for (let s = 0; s < n; s++) {
      r = e[s];
      let a;
      if (o = r, !Cl(r) && (o = Yr[(a = String(r)).toLowerCase()], o === void 0))
        throw new R(`Unknown adapter '${a}'`);
      if (o && (m.isFunction(o) || (o = o.get(t))))
        break;
      i[a || "#" + s] = o;
    }
    if (!o) {
      const s = Object.entries(i).map(
        ([c, u]) => `adapter ${c} ` + (u === !1 ? "is not supported by the environment" : "is not available in the build")
      );
      let a = n ? s.length > 1 ? `since :
` + s.map(qo).join(`
`) : " " + qo(s[0]) : "as no adapter specified";
      throw new R(
        "There is no suitable adapter to dispatch the request " + a,
        "ERR_NOT_SUPPORT"
      );
    }
    return o;
  }
  const la = {
    /**
     * Resolve an adapter from a list of adapter names or functions.
     * @type {Function}
     */
    getAdapter: Rl,
    /**
     * Exposes all known adapters
     * @type {Object<string, Function|Object>}
     */
    adapters: Yr
  };
  function Gn(e) {
    if (e.cancelToken && e.cancelToken.throwIfRequested(), e.signal && e.signal.aborted)
      throw new Ht(null, e);
  }
  function Go(e) {
    return Gn(e), e.headers = ne.from(e.headers), e.data = qn.call(e, e.transformRequest), ["post", "put", "patch"].indexOf(e.method) !== -1 && e.headers.setContentType("application/x-www-form-urlencoded", !1), la.getAdapter(e.adapter || Ut.adapter, e)(e).then(
      function(r) {
        return Gn(e), r.data = qn.call(e, e.transformResponse, r), r.headers = ne.from(r.headers), r;
      },
      function(r) {
        return ia(r) || (Gn(e), r && r.response && (r.response.data = qn.call(
          e,
          e.transformResponse,
          r.response
        ), r.response.headers = ne.from(r.response.headers))), Promise.reject(r);
      }
    );
  }
  const fa = "1.15.2", On = {};
  ["object", "boolean", "number", "function", "string", "symbol"].forEach((e, t) => {
    On[e] = function(r) {
      return typeof r === e || "a" + (t < 1 ? "n " : " ") + e;
    };
  });
  const Jo = {};
  On.transitional = function(t, n, r) {
    function o(i, s) {
      return "[Axios v" + fa + "] Transitional option '" + i + "'" + s + (r ? ". " + r : "");
    }
    return (i, s, a) => {
      if (t === !1)
        throw new R(
          o(s, " has been removed" + (n ? " in " + n : "")),
          R.ERR_DEPRECATED
        );
      return n && !Jo[s] && (Jo[s] = !0, console.warn(
        o(
          s,
          " has been deprecated since v" + n + " and will be removed in the near future"
        )
      )), t ? t(i, s, a) : !0;
    };
  };
  On.spelling = function(t) {
    return (n, r) => (console.warn(`${r} is likely a misspelling of ${t}`), !0);
  };
  function Il(e, t, n) {
    if (typeof e != "object")
      throw new R("options must be an object", R.ERR_BAD_OPTION_VALUE);
    const r = Object.keys(e);
    let o = r.length;
    for (; o-- > 0; ) {
      const i = r[o], s = Object.prototype.hasOwnProperty.call(t, i) ? t[i] : void 0;
      if (s) {
        const a = e[i], c = a === void 0 || s(a, i, e);
        if (c !== !0)
          throw new R(
            "option " + i + " must be " + c,
            R.ERR_BAD_OPTION_VALUE
          );
        continue;
      }
      if (n !== !0)
        throw new R("Unknown option " + i, R.ERR_BAD_OPTION);
    }
  }
  const ln = {
    assertOptions: Il,
    validators: On
  }, ce = ln.validators;
  let He = class {
    constructor(t) {
      this.defaults = t || {}, this.interceptors = {
        request: new Do(),
        response: new Do()
      };
    }
    /**
     * Dispatch a request
     *
     * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)
     * @param {?Object} config
     *
     * @returns {Promise} The Promise to be fulfilled
     */
    async request(t, n) {
      try {
        return await this._request(t, n);
      } catch (r) {
        if (r instanceof Error) {
          let o = {};
          Error.captureStackTrace ? Error.captureStackTrace(o) : o = new Error();
          const i = (() => {
            if (!o.stack)
              return "";
            const s = o.stack.indexOf(`
`);
            return s === -1 ? "" : o.stack.slice(s + 1);
          })();
          try {
            if (!r.stack)
              r.stack = i;
            else if (i) {
              const s = i.indexOf(`
`), a = s === -1 ? -1 : i.indexOf(`
`, s + 1), c = a === -1 ? "" : i.slice(a + 1);
              String(r.stack).endsWith(c) || (r.stack += `
` + i);
            }
          } catch {
          }
        }
        throw r;
      }
    }
    _request(t, n) {
      typeof t == "string" ? (n = n || {}, n.url = t) : n = t || {}, n = We(this.defaults, n);
      const { transitional: r, paramsSerializer: o, headers: i } = n;
      r !== void 0 && ln.assertOptions(
        r,
        {
          silentJSONParsing: ce.transitional(ce.boolean),
          forcedJSONParsing: ce.transitional(ce.boolean),
          clarifyTimeoutError: ce.transitional(ce.boolean),
          legacyInterceptorReqResOrdering: ce.transitional(ce.boolean)
        },
        !1
      ), o != null && (m.isFunction(o) ? n.paramsSerializer = {
        serialize: o
      } : ln.assertOptions(
        o,
        {
          encode: ce.function,
          serialize: ce.function
        },
        !0
      )), n.allowAbsoluteUrls !== void 0 || (this.defaults.allowAbsoluteUrls !== void 0 ? n.allowAbsoluteUrls = this.defaults.allowAbsoluteUrls : n.allowAbsoluteUrls = !0), ln.assertOptions(
        n,
        {
          baseUrl: ce.spelling("baseURL"),
          withXsrfToken: ce.spelling("withXSRFToken")
        },
        !0
      ), n.method = (n.method || this.defaults.method || "get").toLowerCase();
      let s = i && m.merge(i.common, i[n.method]);
      i && m.forEach(["delete", "get", "head", "post", "put", "patch", "common"], (y) => {
        delete i[y];
      }), n.headers = ne.concat(s, i);
      const a = [];
      let c = !0;
      this.interceptors.request.forEach(function(h) {
        if (typeof h.runWhen == "function" && h.runWhen(n) === !1)
          return;
        c = c && h.synchronous;
        const g = n.transitional || Kr;
        g && g.legacyInterceptorReqResOrdering ? a.unshift(h.fulfilled, h.rejected) : a.push(h.fulfilled, h.rejected);
      });
      const u = [];
      this.interceptors.response.forEach(function(h) {
        u.push(h.fulfilled, h.rejected);
      });
      let l, f = 0, p;
      if (!c) {
        const y = [Go.bind(this), void 0];
        for (y.unshift(...a), y.push(...u), p = y.length, l = Promise.resolve(n); f < p; )
          l = l.then(y[f++], y[f++]);
        return l;
      }
      p = a.length;
      let d = n;
      for (; f < p; ) {
        const y = a[f++], h = a[f++];
        try {
          d = y(d);
        } catch (g) {
          h.call(this, g);
          break;
        }
      }
      try {
        l = Go.call(this, d);
      } catch (y) {
        return Promise.reject(y);
      }
      for (f = 0, p = u.length; f < p; )
        l = l.then(u[f++], u[f++]);
      return l;
    }
    getUri(t) {
      t = We(this.defaults, t);
      const n = aa(t.baseURL, t.url, t.allowAbsoluteUrls);
      return ra(n, t.params, t.paramsSerializer);
    }
  };
  m.forEach(["delete", "get", "head", "options"], function(t) {
    He.prototype[t] = function(n, r) {
      return this.request(
        We(r || {}, {
          method: t,
          url: n,
          data: (r || {}).data
        })
      );
    };
  });
  m.forEach(["post", "put", "patch"], function(t) {
    function n(r) {
      return function(i, s, a) {
        return this.request(
          We(a || {}, {
            method: t,
            headers: r ? {
              "Content-Type": "multipart/form-data"
            } : {},
            url: i,
            data: s
          })
        );
      };
    }
    He.prototype[t] = n(), He.prototype[t + "Form"] = n(!0);
  });
  let Ol = class da {
    constructor(t) {
      if (typeof t != "function")
        throw new TypeError("executor must be a function.");
      let n;
      this.promise = new Promise(function(i) {
        n = i;
      });
      const r = this;
      this.promise.then((o) => {
        if (!r._listeners) return;
        let i = r._listeners.length;
        for (; i-- > 0; )
          r._listeners[i](o);
        r._listeners = null;
      }), this.promise.then = (o) => {
        let i;
        const s = new Promise((a) => {
          r.subscribe(a), i = a;
        }).then(o);
        return s.cancel = function() {
          r.unsubscribe(i);
        }, s;
      }, t(function(i, s, a) {
        r.reason || (r.reason = new Ht(i, s, a), n(r.reason));
      });
    }
    /**
     * Throws a `CanceledError` if cancellation has been requested.
     */
    throwIfRequested() {
      if (this.reason)
        throw this.reason;
    }
    /**
     * Subscribe to the cancel signal
     */
    subscribe(t) {
      if (this.reason) {
        t(this.reason);
        return;
      }
      this._listeners ? this._listeners.push(t) : this._listeners = [t];
    }
    /**
     * Unsubscribe from the cancel signal
     */
    unsubscribe(t) {
      if (!this._listeners)
        return;
      const n = this._listeners.indexOf(t);
      n !== -1 && this._listeners.splice(n, 1);
    }
    toAbortSignal() {
      const t = new AbortController(), n = (r) => {
        t.abort(r);
      };
      return this.subscribe(n), t.signal.unsubscribe = () => this.unsubscribe(n), t.signal;
    }
    /**
     * Returns an object that contains a new `CancelToken` and a function that, when called,
     * cancels the `CancelToken`.
     */
    static source() {
      let t;
      return {
        token: new da(function(o) {
          t = o;
        }),
        cancel: t
      };
    }
  };
  function Ml(e) {
    return function(n) {
      return e.apply(null, n);
    };
  }
  function Pl(e) {
    return m.isObject(e) && e.isAxiosError === !0;
  }
  const Er = {
    Continue: 100,
    SwitchingProtocols: 101,
    Processing: 102,
    EarlyHints: 103,
    Ok: 200,
    Created: 201,
    Accepted: 202,
    NonAuthoritativeInformation: 203,
    NoContent: 204,
    ResetContent: 205,
    PartialContent: 206,
    MultiStatus: 207,
    AlreadyReported: 208,
    ImUsed: 226,
    MultipleChoices: 300,
    MovedPermanently: 301,
    Found: 302,
    SeeOther: 303,
    NotModified: 304,
    UseProxy: 305,
    Unused: 306,
    TemporaryRedirect: 307,
    PermanentRedirect: 308,
    BadRequest: 400,
    Unauthorized: 401,
    PaymentRequired: 402,
    Forbidden: 403,
    NotFound: 404,
    MethodNotAllowed: 405,
    NotAcceptable: 406,
    ProxyAuthenticationRequired: 407,
    RequestTimeout: 408,
    Conflict: 409,
    Gone: 410,
    LengthRequired: 411,
    PreconditionFailed: 412,
    PayloadTooLarge: 413,
    UriTooLong: 414,
    UnsupportedMediaType: 415,
    RangeNotSatisfiable: 416,
    ExpectationFailed: 417,
    ImATeapot: 418,
    MisdirectedRequest: 421,
    UnprocessableEntity: 422,
    Locked: 423,
    FailedDependency: 424,
    TooEarly: 425,
    UpgradeRequired: 426,
    PreconditionRequired: 428,
    TooManyRequests: 429,
    RequestHeaderFieldsTooLarge: 431,
    UnavailableForLegalReasons: 451,
    InternalServerError: 500,
    NotImplemented: 501,
    BadGateway: 502,
    ServiceUnavailable: 503,
    GatewayTimeout: 504,
    HttpVersionNotSupported: 505,
    VariantAlsoNegotiates: 506,
    InsufficientStorage: 507,
    LoopDetected: 508,
    NotExtended: 510,
    NetworkAuthenticationRequired: 511,
    WebServerIsDown: 521,
    ConnectionTimedOut: 522,
    OriginIsUnreachable: 523,
    TimeoutOccurred: 524,
    SslHandshakeFailed: 525,
    InvalidSslCertificate: 526
  };
  Object.entries(Er).forEach(([e, t]) => {
    Er[t] = e;
  });
  function pa(e) {
    const t = new He(e), n = qs(He.prototype.request, t);
    return m.extend(n, He.prototype, t, { allOwnKeys: !0 }), m.extend(n, t, null, { allOwnKeys: !0 }), n.create = function(o) {
      return pa(We(e, o));
    }, n;
  }
  const V = pa(Ut);
  V.Axios = He;
  V.CanceledError = Ht;
  V.CancelToken = Ol;
  V.isCancel = ia;
  V.VERSION = fa;
  V.toFormData = In;
  V.AxiosError = R;
  V.Cancel = V.CanceledError;
  V.all = function(t) {
    return Promise.all(t);
  };
  V.spread = Ml;
  V.isAxiosError = Pl;
  V.mergeConfig = We;
  V.AxiosHeaders = ne;
  V.formToJSON = (e) => oa(m.isHTMLForm(e) ? new FormData(e) : e);
  V.getAdapter = la.getAdapter;
  V.HttpStatusCode = Er;
  V.default = V;
  const {
    Axios: Cb,
    AxiosError: Nl,
    CanceledError: Rb,
    isCancel: Ib,
    CancelToken: Ob,
    VERSION: Mb,
    all: Pb,
    Cancel: Nb,
    isAxiosError: Lb,
    spread: xb,
    toFormData: kb,
    AxiosHeaders: Db,
    HttpStatusCode: $b,
    formToJSON: Fb,
    getAdapter: jb,
    mergeConfig: Bb
  } = V;
  var ha = typeof global == "object" && global && global.Object === Object && global, Ll = typeof self == "object" && self && self.Object === Object && self, _e = ha || Ll || Function("return this")(), ge = _e.Symbol, ma = Object.prototype, xl = ma.hasOwnProperty, kl = ma.toString, vt = ge ? ge.toStringTag : void 0;
  function Dl(e) {
    var t = xl.call(e, vt), n = e[vt];
    try {
      e[vt] = void 0;
      var r = !0;
    } catch {
    }
    var o = kl.call(e);
    return r && (t ? e[vt] = n : delete e[vt]), o;
  }
  var $l = Object.prototype, Fl = $l.toString;
  function jl(e) {
    return Fl.call(e);
  }
  var Bl = "[object Null]", Ul = "[object Undefined]", Ko = ge ? ge.toStringTag : void 0;
  function Je(e) {
    return e == null ? e === void 0 ? Ul : Bl : Ko && Ko in Object(e) ? Dl(e) : jl(e);
  }
  function Ke(e) {
    return e != null && typeof e == "object";
  }
  var Hl = "[object Symbol]";
  function Xr(e) {
    return typeof e == "symbol" || Ke(e) && Je(e) == Hl;
  }
  function ga(e, t) {
    for (var n = -1, r = e == null ? 0 : e.length, o = Array(r); ++n < r; )
      o[n] = t(e[n], n, e);
    return o;
  }
  var De = Array.isArray, Zo = ge ? ge.prototype : void 0, Yo = Zo ? Zo.toString : void 0;
  function ya(e) {
    if (typeof e == "string")
      return e;
    if (De(e))
      return ga(e, ya) + "";
    if (Xr(e))
      return Yo ? Yo.call(e) : "";
    var t = e + "";
    return t == "0" && 1 / e == -1 / 0 ? "-0" : t;
  }
  function it(e) {
    var t = typeof e;
    return e != null && (t == "object" || t == "function");
  }
  function zl(e) {
    return e;
  }
  var Wl = "[object AsyncFunction]", Vl = "[object Function]", ql = "[object GeneratorFunction]", Gl = "[object Proxy]";
  function Aa(e) {
    if (!it(e))
      return !1;
    var t = Je(e);
    return t == Vl || t == ql || t == Wl || t == Gl;
  }
  var Jn = _e["__core-js_shared__"], Xo = (function() {
    var e = /[^.]+$/.exec(Jn && Jn.keys && Jn.keys.IE_PROTO || "");
    return e ? "Symbol(src)_1." + e : "";
  })();
  function Jl(e) {
    return !!Xo && Xo in e;
  }
  var Kl = Function.prototype, Zl = Kl.toString;
  function Ze(e) {
    if (e != null) {
      try {
        return Zl.call(e);
      } catch {
      }
      try {
        return e + "";
      } catch {
      }
    }
    return "";
  }
  var Yl = /[\\^$.*+?()[\]{}|]/g, Xl = /^\[object .+?Constructor\]$/, Ql = Function.prototype, ef = Object.prototype, tf = Ql.toString, nf = ef.hasOwnProperty, rf = RegExp(
    "^" + tf.call(nf).replace(Yl, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
  );
  function of(e) {
    if (!it(e) || Jl(e))
      return !1;
    var t = Aa(e) ? rf : Xl;
    return t.test(Ze(e));
  }
  function sf(e, t) {
    return e?.[t];
  }
  function Ye(e, t) {
    var n = sf(e, t);
    return of(n) ? n : void 0;
  }
  var br = Ye(_e, "WeakMap");
  function af(e, t, n) {
    switch (n.length) {
      case 0:
        return e.call(t);
      case 1:
        return e.call(t, n[0]);
      case 2:
        return e.call(t, n[0], n[1]);
      case 3:
        return e.call(t, n[0], n[1], n[2]);
    }
    return e.apply(t, n);
  }
  var cf = 800, uf = 16, lf = Date.now;
  function ff(e) {
    var t = 0, n = 0;
    return function() {
      var r = lf(), o = uf - (r - n);
      if (n = r, o > 0) {
        if (++t >= cf)
          return arguments[0];
      } else
        t = 0;
      return e.apply(void 0, arguments);
    };
  }
  function df(e) {
    return function() {
      return e;
    };
  }
  var _n = (function() {
    try {
      var e = Ye(Object, "defineProperty");
      return e({}, "", {}), e;
    } catch {
    }
  })(), pf = _n ? function(e, t) {
    return _n(e, "toString", {
      configurable: !0,
      enumerable: !1,
      value: df(t),
      writable: !0
    });
  } : zl, hf = ff(pf);
  function mf(e, t) {
    for (var n = -1, r = e == null ? 0 : e.length; ++n < r && t(e[n], n, e) !== !1; )
      ;
    return e;
  }
  var gf = 9007199254740991, yf = /^(?:0|[1-9]\d*)$/;
  function Qr(e, t) {
    var n = typeof e;
    return t = t ?? gf, !!t && (n == "number" || n != "symbol" && yf.test(e)) && e > -1 && e % 1 == 0 && e < t;
  }
  function _a(e, t, n) {
    t == "__proto__" && _n ? _n(e, t, {
      configurable: !0,
      enumerable: !0,
      value: n,
      writable: !0
    }) : e[t] = n;
  }
  function Ea(e, t) {
    return e === t || e !== e && t !== t;
  }
  var Af = Object.prototype, _f = Af.hasOwnProperty;
  function eo(e, t, n) {
    var r = e[t];
    (!(_f.call(e, t) && Ea(r, n)) || n === void 0 && !(t in e)) && _a(e, t, n);
  }
  function Ef(e, t, n, r) {
    var o = !n;
    n || (n = {});
    for (var i = -1, s = t.length; ++i < s; ) {
      var a = t[i], c = void 0;
      c === void 0 && (c = e[a]), o ? _a(n, a, c) : eo(n, a, c);
    }
    return n;
  }
  var Qo = Math.max;
  function bf(e, t, n) {
    return t = Qo(t === void 0 ? e.length - 1 : t, 0), function() {
      for (var r = arguments, o = -1, i = Qo(r.length - t, 0), s = Array(i); ++o < i; )
        s[o] = r[t + o];
      o = -1;
      for (var a = Array(t + 1); ++o < t; )
        a[o] = r[o];
      return a[t] = n(s), af(e, this, a);
    };
  }
  var Sf = 9007199254740991;
  function to(e) {
    return typeof e == "number" && e > -1 && e % 1 == 0 && e <= Sf;
  }
  function wf(e) {
    return e != null && to(e.length) && !Aa(e);
  }
  var vf = Object.prototype;
  function Tf(e) {
    var t = e && e.constructor, n = typeof t == "function" && t.prototype || vf;
    return e === n;
  }
  function Cf(e, t) {
    for (var n = -1, r = Array(e); ++n < e; )
      r[n] = t(n);
    return r;
  }
  var Rf = "[object Arguments]";
  function ei(e) {
    return Ke(e) && Je(e) == Rf;
  }
  var ba = Object.prototype, If = ba.hasOwnProperty, Of = ba.propertyIsEnumerable, no = ei(/* @__PURE__ */ (function() {
    return arguments;
  })()) ? ei : function(e) {
    return Ke(e) && If.call(e, "callee") && !Of.call(e, "callee");
  };
  function Mf() {
    return !1;
  }
  var Sa = typeof ie == "object" && ie && !ie.nodeType && ie, ti = Sa && typeof se == "object" && se && !se.nodeType && se, Pf = ti && ti.exports === Sa, ni = Pf ? _e.Buffer : void 0, Nf = ni ? ni.isBuffer : void 0, wa = Nf || Mf, Lf = "[object Arguments]", xf = "[object Array]", kf = "[object Boolean]", Df = "[object Date]", $f = "[object Error]", Ff = "[object Function]", jf = "[object Map]", Bf = "[object Number]", Uf = "[object Object]", Hf = "[object RegExp]", zf = "[object Set]", Wf = "[object String]", Vf = "[object WeakMap]", qf = "[object ArrayBuffer]", Gf = "[object DataView]", Jf = "[object Float32Array]", Kf = "[object Float64Array]", Zf = "[object Int8Array]", Yf = "[object Int16Array]", Xf = "[object Int32Array]", Qf = "[object Uint8Array]", ed = "[object Uint8ClampedArray]", td = "[object Uint16Array]", nd = "[object Uint32Array]", B = {};
  B[Jf] = B[Kf] = B[Zf] = B[Yf] = B[Xf] = B[Qf] = B[ed] = B[td] = B[nd] = !0;
  B[Lf] = B[xf] = B[qf] = B[kf] = B[Gf] = B[Df] = B[$f] = B[Ff] = B[jf] = B[Bf] = B[Uf] = B[Hf] = B[zf] = B[Wf] = B[Vf] = !1;
  function rd(e) {
    return Ke(e) && to(e.length) && !!B[Je(e)];
  }
  function ro(e) {
    return function(t) {
      return e(t);
    };
  }
  var va = typeof ie == "object" && ie && !ie.nodeType && ie, Mt = va && typeof se == "object" && se && !se.nodeType && se, od = Mt && Mt.exports === va, Kn = od && ha.process, st = (function() {
    try {
      var e = Mt && Mt.require && Mt.require("util").types;
      return e || Kn && Kn.binding && Kn.binding("util");
    } catch {
    }
  })(), ri = st && st.isTypedArray, id = ri ? ro(ri) : rd;
  function sd(e, t) {
    var n = De(e), r = !n && no(e), o = !n && !r && wa(e), i = !n && !r && !o && id(e), s = n || r || o || i, a = s ? Cf(e.length, String) : [], c = a.length;
    for (var u in e)
      s && // Safari 9 has enumerable `arguments.length` in strict mode.
      (u == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
      o && (u == "offset" || u == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
      i && (u == "buffer" || u == "byteLength" || u == "byteOffset") || // Skip index properties.
      Qr(u, c)) || a.push(u);
    return a;
  }
  function ad(e, t) {
    return function(n) {
      return e(t(n));
    };
  }
  function cd(e) {
    var t = [];
    if (e != null)
      for (var n in Object(e))
        t.push(n);
    return t;
  }
  var ud = Object.prototype, ld = ud.hasOwnProperty;
  function fd(e) {
    if (!it(e))
      return cd(e);
    var t = Tf(e), n = [];
    for (var r in e)
      r == "constructor" && (t || !ld.call(e, r)) || n.push(r);
    return n;
  }
  function dd(e) {
    return wf(e) ? sd(e) : fd(e);
  }
  var pd = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/, hd = /^\w*$/;
  function md(e, t) {
    if (De(e))
      return !1;
    var n = typeof e;
    return n == "number" || n == "symbol" || n == "boolean" || e == null || Xr(e) ? !0 : hd.test(e) || !pd.test(e) || t != null && e in Object(t);
  }
  var Nt = Ye(Object, "create");
  function gd() {
    this.__data__ = Nt ? Nt(null) : {}, this.size = 0;
  }
  function yd(e) {
    var t = this.has(e) && delete this.__data__[e];
    return this.size -= t ? 1 : 0, t;
  }
  var Ad = "__lodash_hash_undefined__", _d = Object.prototype, Ed = _d.hasOwnProperty;
  function bd(e) {
    var t = this.__data__;
    if (Nt) {
      var n = t[e];
      return n === Ad ? void 0 : n;
    }
    return Ed.call(t, e) ? t[e] : void 0;
  }
  var Sd = Object.prototype, wd = Sd.hasOwnProperty;
  function vd(e) {
    var t = this.__data__;
    return Nt ? t[e] !== void 0 : wd.call(t, e);
  }
  var Td = "__lodash_hash_undefined__";
  function Cd(e, t) {
    var n = this.__data__;
    return this.size += this.has(e) ? 0 : 1, n[e] = Nt && t === void 0 ? Td : t, this;
  }
  function Ve(e) {
    var t = -1, n = e == null ? 0 : e.length;
    for (this.clear(); ++t < n; ) {
      var r = e[t];
      this.set(r[0], r[1]);
    }
  }
  Ve.prototype.clear = gd;
  Ve.prototype.delete = yd;
  Ve.prototype.get = bd;
  Ve.prototype.has = vd;
  Ve.prototype.set = Cd;
  function Rd() {
    this.__data__ = [], this.size = 0;
  }
  function Mn(e, t) {
    for (var n = e.length; n--; )
      if (Ea(e[n][0], t))
        return n;
    return -1;
  }
  var Id = Array.prototype, Od = Id.splice;
  function Md(e) {
    var t = this.__data__, n = Mn(t, e);
    if (n < 0)
      return !1;
    var r = t.length - 1;
    return n == r ? t.pop() : Od.call(t, n, 1), --this.size, !0;
  }
  function Pd(e) {
    var t = this.__data__, n = Mn(t, e);
    return n < 0 ? void 0 : t[n][1];
  }
  function Nd(e) {
    return Mn(this.__data__, e) > -1;
  }
  function Ld(e, t) {
    var n = this.__data__, r = Mn(n, e);
    return r < 0 ? (++this.size, n.push([e, t])) : n[r][1] = t, this;
  }
  function Ie(e) {
    var t = -1, n = e == null ? 0 : e.length;
    for (this.clear(); ++t < n; ) {
      var r = e[t];
      this.set(r[0], r[1]);
    }
  }
  Ie.prototype.clear = Rd;
  Ie.prototype.delete = Md;
  Ie.prototype.get = Pd;
  Ie.prototype.has = Nd;
  Ie.prototype.set = Ld;
  var Lt = Ye(_e, "Map");
  function xd() {
    this.size = 0, this.__data__ = {
      hash: new Ve(),
      map: new (Lt || Ie)(),
      string: new Ve()
    };
  }
  function kd(e) {
    var t = typeof e;
    return t == "string" || t == "number" || t == "symbol" || t == "boolean" ? e !== "__proto__" : e === null;
  }
  function Pn(e, t) {
    var n = e.__data__;
    return kd(t) ? n[typeof t == "string" ? "string" : "hash"] : n.map;
  }
  function Dd(e) {
    var t = Pn(this, e).delete(e);
    return this.size -= t ? 1 : 0, t;
  }
  function $d(e) {
    return Pn(this, e).get(e);
  }
  function Fd(e) {
    return Pn(this, e).has(e);
  }
  function jd(e, t) {
    var n = Pn(this, e), r = n.size;
    return n.set(e, t), this.size += n.size == r ? 0 : 1, this;
  }
  function $e(e) {
    var t = -1, n = e == null ? 0 : e.length;
    for (this.clear(); ++t < n; ) {
      var r = e[t];
      this.set(r[0], r[1]);
    }
  }
  $e.prototype.clear = xd;
  $e.prototype.delete = Dd;
  $e.prototype.get = $d;
  $e.prototype.has = Fd;
  $e.prototype.set = jd;
  var Bd = "Expected a function";
  function oo(e, t) {
    if (typeof e != "function" || t != null && typeof t != "function")
      throw new TypeError(Bd);
    var n = function() {
      var r = arguments, o = t ? t.apply(this, r) : r[0], i = n.cache;
      if (i.has(o))
        return i.get(o);
      var s = e.apply(this, r);
      return n.cache = i.set(o, s) || i, s;
    };
    return n.cache = new (oo.Cache || $e)(), n;
  }
  oo.Cache = $e;
  var Ud = 500;
  function Hd(e) {
    var t = oo(e, function(r) {
      return n.size === Ud && n.clear(), r;
    }), n = t.cache;
    return t;
  }
  var zd = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g, Wd = /\\(\\)?/g, Vd = Hd(function(e) {
    var t = [];
    return e.charCodeAt(0) === 46 && t.push(""), e.replace(zd, function(n, r, o, i) {
      t.push(o ? i.replace(Wd, "$1") : r || n);
    }), t;
  });
  function qd(e) {
    return e == null ? "" : ya(e);
  }
  function pt(e, t) {
    return De(e) ? e : md(e, t) ? [e] : Vd(qd(e));
  }
  function xt(e) {
    if (typeof e == "string" || Xr(e))
      return e;
    var t = e + "";
    return t == "0" && 1 / e == -1 / 0 ? "-0" : t;
  }
  function Ta(e, t) {
    t = pt(t, e);
    for (var n = 0, r = t.length; e != null && n < r; )
      e = e[xt(t[n++])];
    return n && n == r ? e : void 0;
  }
  function io(e, t) {
    for (var n = -1, r = t.length, o = e.length; ++n < r; )
      e[o + n] = t[n];
    return e;
  }
  var oi = ge ? ge.isConcatSpreadable : void 0;
  function Gd(e) {
    return De(e) || no(e) || !!(oi && e && e[oi]);
  }
  function Jd(e, t, n, r, o) {
    var i = -1, s = e.length;
    for (n || (n = Gd), o || (o = []); ++i < s; ) {
      var a = e[i];
      n(a) ? io(o, a) : o[o.length] = a;
    }
    return o;
  }
  function Kd(e) {
    var t = e == null ? 0 : e.length;
    return t ? Jd(e) : [];
  }
  function Ca(e) {
    return hf(bf(e, void 0, Kd), e + "");
  }
  var Ra = ad(Object.getPrototypeOf, Object), Zd = "[object Object]", Yd = Function.prototype, Xd = Object.prototype, Ia = Yd.toString, Qd = Xd.hasOwnProperty, ep = Ia.call(Object);
  function tp(e) {
    if (!Ke(e) || Je(e) != Zd)
      return !1;
    var t = Ra(e);
    if (t === null)
      return !0;
    var n = Qd.call(t, "constructor") && t.constructor;
    return typeof n == "function" && n instanceof n && Ia.call(n) == ep;
  }
  function np(e, t, n) {
    var r = -1, o = e.length;
    t < 0 && (t = -t > o ? 0 : o + t), n = n > o ? o : n, n < 0 && (n += o), o = t > n ? 0 : n - t >>> 0, t >>>= 0;
    for (var i = Array(o); ++r < o; )
      i[r] = e[r + t];
    return i;
  }
  function rp() {
    this.__data__ = new Ie(), this.size = 0;
  }
  function op(e) {
    var t = this.__data__, n = t.delete(e);
    return this.size = t.size, n;
  }
  function ip(e) {
    return this.__data__.get(e);
  }
  function sp(e) {
    return this.__data__.has(e);
  }
  var ap = 200;
  function cp(e, t) {
    var n = this.__data__;
    if (n instanceof Ie) {
      var r = n.__data__;
      if (!Lt || r.length < ap - 1)
        return r.push([e, t]), this.size = ++n.size, this;
      n = this.__data__ = new $e(r);
    }
    return n.set(e, t), this.size = n.size, this;
  }
  function ht(e) {
    var t = this.__data__ = new Ie(e);
    this.size = t.size;
  }
  ht.prototype.clear = rp;
  ht.prototype.delete = op;
  ht.prototype.get = ip;
  ht.prototype.has = sp;
  ht.prototype.set = cp;
  var Oa = typeof ie == "object" && ie && !ie.nodeType && ie, ii = Oa && typeof se == "object" && se && !se.nodeType && se, up = ii && ii.exports === Oa, si = up ? _e.Buffer : void 0;
  si && si.allocUnsafe;
  function lp(e, t) {
    return e.slice();
  }
  function fp(e, t) {
    for (var n = -1, r = e == null ? 0 : e.length, o = 0, i = []; ++n < r; ) {
      var s = e[n];
      t(s, n, e) && (i[o++] = s);
    }
    return i;
  }
  function Ma() {
    return [];
  }
  var dp = Object.prototype, pp = dp.propertyIsEnumerable, ai = Object.getOwnPropertySymbols, hp = ai ? function(e) {
    return e == null ? [] : (e = Object(e), fp(ai(e), function(t) {
      return pp.call(e, t);
    }));
  } : Ma, mp = Object.getOwnPropertySymbols, gp = mp ? function(e) {
    for (var t = []; e; )
      io(t, hp(e)), e = Ra(e);
    return t;
  } : Ma;
  function yp(e, t, n) {
    var r = t(e);
    return De(e) ? r : io(r, n(e));
  }
  function Pa(e) {
    return yp(e, dd, gp);
  }
  var Sr = Ye(_e, "DataView"), wr = Ye(_e, "Promise"), vr = Ye(_e, "Set"), ci = "[object Map]", Ap = "[object Object]", ui = "[object Promise]", li = "[object Set]", fi = "[object WeakMap]", di = "[object DataView]", _p = Ze(Sr), Ep = Ze(Lt), bp = Ze(wr), Sp = Ze(vr), wp = Ze(br), ve = Je;
  (Sr && ve(new Sr(new ArrayBuffer(1))) != di || Lt && ve(new Lt()) != ci || wr && ve(wr.resolve()) != ui || vr && ve(new vr()) != li || br && ve(new br()) != fi) && (ve = function(e) {
    var t = Je(e), n = t == Ap ? e.constructor : void 0, r = n ? Ze(n) : "";
    if (r)
      switch (r) {
        case _p:
          return di;
        case Ep:
          return ci;
        case bp:
          return ui;
        case Sp:
          return li;
        case wp:
          return fi;
      }
    return t;
  });
  var vp = Object.prototype, Tp = vp.hasOwnProperty;
  function Cp(e) {
    var t = e.length, n = new e.constructor(t);
    return t && typeof e[0] == "string" && Tp.call(e, "index") && (n.index = e.index, n.input = e.input), n;
  }
  var pi = _e.Uint8Array;
  function so(e) {
    var t = new e.constructor(e.byteLength);
    return new pi(t).set(new pi(e)), t;
  }
  function Rp(e, t) {
    var n = so(e.buffer);
    return new e.constructor(n, e.byteOffset, e.byteLength);
  }
  var Ip = /\w*$/;
  function Op(e) {
    var t = new e.constructor(e.source, Ip.exec(e));
    return t.lastIndex = e.lastIndex, t;
  }
  var hi = ge ? ge.prototype : void 0, mi = hi ? hi.valueOf : void 0;
  function Mp(e) {
    return mi ? Object(mi.call(e)) : {};
  }
  function Pp(e, t) {
    var n = so(e.buffer);
    return new e.constructor(n, e.byteOffset, e.length);
  }
  var Np = "[object Boolean]", Lp = "[object Date]", xp = "[object Map]", kp = "[object Number]", Dp = "[object RegExp]", $p = "[object Set]", Fp = "[object String]", jp = "[object Symbol]", Bp = "[object ArrayBuffer]", Up = "[object DataView]", Hp = "[object Float32Array]", zp = "[object Float64Array]", Wp = "[object Int8Array]", Vp = "[object Int16Array]", qp = "[object Int32Array]", Gp = "[object Uint8Array]", Jp = "[object Uint8ClampedArray]", Kp = "[object Uint16Array]", Zp = "[object Uint32Array]";
  function Yp(e, t, n) {
    var r = e.constructor;
    switch (t) {
      case Bp:
        return so(e);
      case Np:
      case Lp:
        return new r(+e);
      case Up:
        return Rp(e);
      case Hp:
      case zp:
      case Wp:
      case Vp:
      case qp:
      case Gp:
      case Jp:
      case Kp:
      case Zp:
        return Pp(e);
      case xp:
        return new r();
      case kp:
      case Fp:
        return new r(e);
      case Dp:
        return Op(e);
      case $p:
        return new r();
      case jp:
        return Mp(e);
    }
  }
  var Xp = "[object Map]";
  function Qp(e) {
    return Ke(e) && ve(e) == Xp;
  }
  var gi = st && st.isMap, eh = gi ? ro(gi) : Qp, th = "[object Set]";
  function nh(e) {
    return Ke(e) && ve(e) == th;
  }
  var yi = st && st.isSet, rh = yi ? ro(yi) : nh, Na = "[object Arguments]", oh = "[object Array]", ih = "[object Boolean]", sh = "[object Date]", ah = "[object Error]", La = "[object Function]", ch = "[object GeneratorFunction]", uh = "[object Map]", lh = "[object Number]", xa = "[object Object]", fh = "[object RegExp]", dh = "[object Set]", ph = "[object String]", hh = "[object Symbol]", mh = "[object WeakMap]", gh = "[object ArrayBuffer]", yh = "[object DataView]", Ah = "[object Float32Array]", _h = "[object Float64Array]", Eh = "[object Int8Array]", bh = "[object Int16Array]", Sh = "[object Int32Array]", wh = "[object Uint8Array]", vh = "[object Uint8ClampedArray]", Th = "[object Uint16Array]", Ch = "[object Uint32Array]", j = {};
  j[Na] = j[oh] = j[gh] = j[yh] = j[ih] = j[sh] = j[Ah] = j[_h] = j[Eh] = j[bh] = j[Sh] = j[uh] = j[lh] = j[xa] = j[fh] = j[dh] = j[ph] = j[hh] = j[wh] = j[vh] = j[Th] = j[Ch] = !0;
  j[ah] = j[La] = j[mh] = !1;
  function fn(e, t, n, r, o, i) {
    var s;
    if (n && (s = o ? n(e, r, o, i) : n(e)), s !== void 0)
      return s;
    if (!it(e))
      return e;
    var a = De(e);
    if (a)
      s = Cp(e);
    else {
      var c = ve(e), u = c == La || c == ch;
      if (wa(e))
        return lp(e);
      if (c == xa || c == Na || u && !o)
        s = {};
      else {
        if (!j[c])
          return o ? e : {};
        s = Yp(e, c);
      }
    }
    i || (i = new ht());
    var l = i.get(e);
    if (l)
      return l;
    i.set(e, s), rh(e) ? e.forEach(function(d) {
      s.add(fn(d, t, n, d, e, i));
    }) : eh(e) && e.forEach(function(d, y) {
      s.set(y, fn(d, t, n, y, e, i));
    });
    var f = Pa, p = a ? void 0 : f(e);
    return mf(p || e, function(d, y) {
      p && (y = d, d = e[y]), eo(s, y, fn(d, t, n, y, e, i));
    }), s;
  }
  function Rh(e, t) {
    return e != null && t in Object(e);
  }
  function Ih(e, t, n) {
    t = pt(t, e);
    for (var r = -1, o = t.length, i = !1; ++r < o; ) {
      var s = xt(t[r]);
      if (!(i = e != null && n(e, s)))
        break;
      e = e[s];
    }
    return i || ++r != o ? i : (o = e == null ? 0 : e.length, !!o && to(o) && Qr(s, o) && (De(e) || no(e)));
  }
  function Oh(e, t) {
    return e != null && Ih(e, t, Rh);
  }
  function Mh(e) {
    var t = e == null ? 0 : e.length;
    return t ? e[t - 1] : void 0;
  }
  function Ph(e, t) {
    return t.length < 2 ? e : Ta(e, np(t, 0, -1));
  }
  var Nh = Object.prototype, Lh = Nh.hasOwnProperty;
  function xh(e, t) {
    t = pt(t, e);
    var n = -1, r = t.length;
    if (!r)
      return !0;
    for (; ++n < r; ) {
      var o = xt(t[n]);
      if (o === "__proto__" && !Lh.call(e, "__proto__") || (o === "constructor" || o === "prototype") && n < r - 1)
        return !1;
    }
    var i = Ph(e, t);
    return i == null || delete i[xt(Mh(t))];
  }
  function kh(e) {
    return tp(e) ? void 0 : e;
  }
  var Dh = 1, $h = 2, Fh = 4, jh = Ca(function(e, t) {
    var n = {};
    if (e == null)
      return n;
    var r = !1;
    t = ga(t, function(i) {
      return i = pt(i, e), r || (r = i.length > 1), i;
    }), Ef(e, Pa(e), n), r && (n = fn(n, Dh | $h | Fh, kh));
    for (var o = t.length; o--; )
      xh(n, t[o]);
    return n;
  });
  function Bh(e, t, n, r) {
    if (!it(e))
      return e;
    t = pt(t, e);
    for (var o = -1, i = t.length, s = i - 1, a = e; a != null && ++o < i; ) {
      var c = xt(t[o]), u = n;
      if (c === "__proto__" || c === "constructor" || c === "prototype")
        return e;
      if (o != s) {
        var l = a[c];
        u = void 0, u === void 0 && (u = it(l) ? l : Qr(t[o + 1]) ? [] : {});
      }
      eo(a, c, u), a = a[c];
    }
    return e;
  }
  function Uh(e, t, n) {
    for (var r = -1, o = t.length, i = {}; ++r < o; ) {
      var s = t[r], a = Ta(e, s);
      n(a, s) && Bh(i, pt(s, e), a);
    }
    return i;
  }
  function Hh(e, t) {
    return Uh(e, t, function(n, r) {
      return Oh(e, r);
    });
  }
  var zh = Ca(function(e, t) {
    return e == null ? {} : Hh(e, t);
  });
  function Wh(e) {
    return e && e.__esModule && Object.prototype.hasOwnProperty.call(e, "default") ? e.default : e;
  }
  var Tt = { exports: {} }, Zn, Ai;
  function Vh() {
    if (Ai) return Zn;
    Ai = 1;
    function e(n) {
      try {
        return JSON.stringify(n);
      } catch {
        return '"[Circular]"';
      }
    }
    Zn = t;
    function t(n, r, o) {
      var i = o && o.stringify || e, s = 1;
      if (typeof n == "object" && n !== null) {
        var a = r.length + s;
        if (a === 1) return n;
        var c = new Array(a);
        c[0] = i(n);
        for (var u = 1; u < a; u++)
          c[u] = i(r[u]);
        return c.join(" ");
      }
      if (typeof n != "string")
        return n;
      var l = r.length;
      if (l === 0) return n;
      for (var f = "", p = 1 - s, d = -1, y = n && n.length || 0, h = 0; h < y; ) {
        if (n.charCodeAt(h) === 37 && h + 1 < y) {
          switch (d = d > -1 ? d : 0, n.charCodeAt(h + 1)) {
            case 100:
            // 'd'
            case 102:
              if (p >= l || r[p] == null) break;
              d < h && (f += n.slice(d, h)), f += Number(r[p]), d = h + 2, h++;
              break;
            case 105:
              if (p >= l || r[p] == null) break;
              d < h && (f += n.slice(d, h)), f += Math.floor(Number(r[p])), d = h + 2, h++;
              break;
            case 79:
            // 'O'
            case 111:
            // 'o'
            case 106:
              if (p >= l || r[p] === void 0) break;
              d < h && (f += n.slice(d, h));
              var g = typeof r[p];
              if (g === "string") {
                f += "'" + r[p] + "'", d = h + 2, h++;
                break;
              }
              if (g === "function") {
                f += r[p].name || "<anonymous>", d = h + 2, h++;
                break;
              }
              f += i(r[p]), d = h + 2, h++;
              break;
            case 115:
              if (p >= l)
                break;
              d < h && (f += n.slice(d, h)), f += String(r[p]), d = h + 2, h++;
              break;
            case 37:
              d < h && (f += n.slice(d, h)), f += "%", d = h + 2, h++, p--;
              break;
          }
          ++p;
        }
        ++h;
      }
      return d === -1 ? n : (d < y && (f += n.slice(d)), f);
    }
    return Zn;
  }
  var _i;
  function qh() {
    if (_i) return Tt.exports;
    _i = 1;
    const e = Vh();
    Tt.exports = l;
    const t = me().console || {}, n = {
      mapHttpRequest: x,
      mapHttpResponse: x,
      wrapRequestSerializer: $,
      wrapResponseSerializer: $,
      wrapErrorSerializer: $,
      req: x,
      res: x,
      err: I,
      errWithCause: I
    };
    function r(A, E) {
      return A === "silent" ? 1 / 0 : E.levels.values[A];
    }
    const o = /* @__PURE__ */ Symbol("pino.logFuncs"), i = /* @__PURE__ */ Symbol("pino.hierarchy"), s = {
      error: "log",
      fatal: "error",
      warn: "error",
      info: "log",
      debug: "log",
      trace: "log"
    };
    function a(A, E) {
      const b = {
        logger: E,
        parent: A[i]
      };
      E[i] = b;
    }
    function c(A, E, b) {
      const P = {};
      E.forEach((N) => {
        P[N] = b[N] ? b[N] : t[N] || t[s[N] || "log"] || J;
      }), A[o] = P;
    }
    function u(A, E) {
      return Array.isArray(A) ? A.filter(function(P) {
        return P !== "!stdSerializers.err";
      }) : A === !0 ? Object.keys(E) : !1;
    }
    function l(A) {
      A = A || {}, A.browser = A.browser || {};
      const E = A.browser.transmit;
      if (E && typeof E.send != "function")
        throw Error("pino: transmit option must have a send function");
      const b = A.browser.write || t;
      A.browser.write && (A.browser.asObject = !0);
      const P = A.serializers || {}, N = u(A.browser.serialize, P);
      let D = A.browser.serialize;
      Array.isArray(A.browser.serialize) && A.browser.serialize.indexOf("!stdSerializers.err") > -1 && (D = !1);
      const U = Object.keys(A.customLevels || {}), k = ["error", "fatal", "warn", "info", "debug", "trace"].concat(U);
      typeof b == "function" && k.forEach(function(ee) {
        b[ee] = b;
      }), (A.enabled === !1 || A.browser.disabled) && (A.level = "silent");
      const z = A.level || "info", S = Object.create(b);
      S.log || (S.log = J), c(S, k, b), a({}, S), Object.defineProperty(S, "levelVal", {
        get: W
      }), Object.defineProperty(S, "level", {
        get: Fe,
        set: Zt
      });
      const q = {
        transmit: E,
        serialize: N,
        asObject: A.browser.asObject,
        asObjectBindingsOnly: A.browser.asObjectBindingsOnly,
        formatters: A.browser.formatters,
        reportCaller: A.browser.reportCaller,
        levels: k,
        timestamp: L(A),
        messageKey: A.messageKey || "msg",
        onChild: A.onChild || J
      };
      S.levels = f(A), S.level = z, S.isLevelEnabled = function(ee) {
        return this.levels.values[ee] ? this.levels.values[ee] >= this.levels.values[this.level] : !1;
      }, S.setMaxListeners = S.getMaxListeners = S.emit = S.addListener = S.on = S.prependListener = S.once = S.prependOnceListener = S.removeListener = S.removeAllListeners = S.listeners = S.listenerCount = S.eventNames = S.write = S.flush = J, S.serializers = P, S._serialize = N, S._stdErrSerialize = D, S.child = function(...ee) {
        return Io.call(this, q, ...ee);
      }, E && (S._logEvent = T());
      function W() {
        return r(this.level, this);
      }
      function Fe() {
        return this._level;
      }
      function Zt(ee) {
        if (ee !== "silent" && !this.levels.values[ee])
          throw Error("unknown level " + ee);
        this._level = ee, y(this, q, S, "error"), y(this, q, S, "fatal"), y(this, q, S, "warn"), y(this, q, S, "info"), y(this, q, S, "debug"), y(this, q, S, "trace"), U.forEach((Se) => {
          y(this, q, S, Se);
        });
      }
      function Io(ee, Se, bt) {
        if (!Se)
          throw new Error("missing bindings for child Pino");
        bt = bt || {}, N && Se.serializers && (bt.serializers = Se.serializers);
        const Oo = bt.serializers;
        if (N && Oo) {
          var Yt = Object.assign({}, P, Oo), Mo = A.browser.serialize === !0 ? Object.keys(Yt) : N;
          delete Se.serializers, v([Se], Mo, Yt, this._stdErrSerialize);
        }
        function Po(Xt) {
          this._childLevel = (Xt._childLevel | 0) + 1, this.bindings = Se, Yt && (this.serializers = Yt, this._serialize = Mo), E && (this._logEvent = T(
            [].concat(Xt._logEvent.bindings, Se)
          ));
        }
        Po.prototype = this;
        const St = new Po(this);
        return a(this, St), St.child = function(...Xt) {
          return Io.call(this, ee, ...Xt);
        }, St.level = bt.level || this.level, ee.onChild(St), St;
      }
      return S;
    }
    function f(A) {
      const E = A.customLevels || {}, b = Object.assign({}, l.levels.values, E), P = Object.assign({}, l.levels.labels, p(E));
      return {
        values: b,
        labels: P
      };
    }
    function p(A) {
      const E = {};
      return Object.keys(A).forEach(function(b) {
        E[A[b]] = b;
      }), E;
    }
    l.levels = {
      values: {
        fatal: 60,
        error: 50,
        warn: 40,
        info: 30,
        debug: 20,
        trace: 10
      },
      labels: {
        10: "trace",
        20: "debug",
        30: "info",
        40: "warn",
        50: "error",
        60: "fatal"
      }
    }, l.stdSerializers = n, l.stdTimeFunctions = Object.assign({}, { nullTime: ae, epochTime: Ee, unixTime: Me, isoTime: be });
    function d(A) {
      const E = [];
      A.bindings && E.push(A.bindings);
      let b = A[i];
      for (; b.parent; )
        b = b.parent, b.logger.bindings && E.push(b.logger.bindings);
      return E.reverse();
    }
    function y(A, E, b, P) {
      if (Object.defineProperty(A, P, {
        value: r(A.level, b) > r(P, b) ? J : b[o][P],
        writable: !0,
        enumerable: !0,
        configurable: !0
      }), A[P] === J) {
        if (!E.transmit) return;
        const D = E.transmit.level || A.level, U = r(D, b);
        if (r(P, b) < U) return;
      }
      A[P] = g(A, E, b, P);
      const N = d(A);
      N.length !== 0 && (A[P] = h(N, A[P]));
    }
    function h(A, E) {
      return function() {
        return E.apply(this, [...A, ...arguments]);
      };
    }
    function g(A, E, b, P) {
      return /* @__PURE__ */ (function(N) {
        return function() {
          const U = E.timestamp(), k = new Array(arguments.length), z = Object.getPrototypeOf && Object.getPrototypeOf(this) === t ? t : this;
          for (var S = 0; S < k.length; S++) k[S] = arguments[S];
          var q = !1;
          if (E.serialize && (v(k, this._serialize, this.serializers, this._stdErrSerialize), q = !0), E.asObject || E.formatters) {
            const W = _(this, P, k, U, E);
            if (E.reportCaller && W && W.length > 0 && W[0] && typeof W[0] == "object")
              try {
                const Fe = Et();
                Fe && (W[0].caller = Fe);
              } catch {
              }
            N.call(z, ...W);
          } else {
            if (E.reportCaller)
              try {
                const W = Et();
                W && k.push(W);
              } catch {
              }
            N.apply(z, k);
          }
          if (E.transmit) {
            const W = E.transmit.level || A._level, Fe = r(W, b), Zt = r(P, b);
            if (Zt < Fe) return;
            w(this, {
              ts: U,
              methodLevel: P,
              methodValue: Zt,
              transmitValue: b.levels.values[E.transmit.level || A._level],
              send: E.transmit.send,
              val: r(A._level, b)
            }, k, q);
          }
        };
      })(A[o][P]);
    }
    function _(A, E, b, P, N) {
      const {
        level: D,
        log: U = (W) => W
      } = N.formatters || {}, k = b.slice();
      let z = k[0];
      const S = {};
      let q = (A._childLevel | 0) + 1;
      if (q < 1 && (q = 1), P && (S.time = P), D) {
        const W = D(E, A.levels.values[E]);
        Object.assign(S, W);
      } else
        S.level = A.levels.values[E];
      if (N.asObjectBindingsOnly) {
        if (z !== null && typeof z == "object")
          for (; q-- && typeof k[0] == "object"; )
            Object.assign(S, k.shift());
        return [U(S), ...k];
      } else {
        if (z !== null && typeof z == "object") {
          for (; q-- && typeof k[0] == "object"; )
            Object.assign(S, k.shift());
          z = k.length ? e(k.shift(), k) : void 0;
        } else typeof z == "string" && (z = e(k.shift(), k));
        return z !== void 0 && (S[N.messageKey] = z), [U(S)];
      }
    }
    function v(A, E, b, P) {
      for (const N in A)
        if (P && A[N] instanceof Error)
          A[N] = l.stdSerializers.err(A[N]);
        else if (typeof A[N] == "object" && !Array.isArray(A[N]) && E)
          for (const D in A[N])
            E.indexOf(D) > -1 && D in b && (A[N][D] = b[D](A[N][D]));
    }
    function w(A, E, b, P = !1) {
      const N = E.send, D = E.ts, U = E.methodLevel, k = E.methodValue, z = E.val, S = A._logEvent.bindings;
      P || v(
        b,
        A._serialize || Object.keys(A.serializers),
        A.serializers,
        A._stdErrSerialize === void 0 ? !0 : A._stdErrSerialize
      ), A._logEvent.ts = D, A._logEvent.messages = b.filter(function(q) {
        return S.indexOf(q) === -1;
      }), A._logEvent.level.label = U, A._logEvent.level.value = k, N(U, A._logEvent, z), A._logEvent = T(S);
    }
    function T(A) {
      return {
        ts: 0,
        messages: [],
        bindings: A || [],
        level: { label: "", value: 0 }
      };
    }
    function I(A) {
      const E = {
        type: A.constructor.name,
        msg: A.message,
        stack: A.stack
      };
      for (const b in A)
        E[b] === void 0 && (E[b] = A[b]);
      return E;
    }
    function L(A) {
      return typeof A.timestamp == "function" ? A.timestamp : A.timestamp === !1 ? ae : Ee;
    }
    function x() {
      return {};
    }
    function $(A) {
      return A;
    }
    function J() {
    }
    function ae() {
      return !1;
    }
    function Ee() {
      return Date.now();
    }
    function Me() {
      return Math.round(Date.now() / 1e3);
    }
    function be() {
      return new Date(Date.now()).toISOString();
    }
    function me() {
      function A(E) {
        return typeof E < "u" && E;
      }
      try {
        return typeof globalThis < "u" || Object.defineProperty(Object.prototype, "globalThis", {
          get: function() {
            return delete Object.prototype.globalThis, this.globalThis = this;
          },
          configurable: !0
        }), globalThis;
      } catch {
        return A(self) || A(window) || A(this) || {};
      }
    }
    Tt.exports.default = l, Tt.exports.pino = l;
    function Et() {
      const A = new Error().stack;
      if (!A) return null;
      const E = A.split(`
`);
      for (let b = 1; b < E.length; b++) {
        const P = E[b].trim();
        if (/(^at\s+)?(createWrap|LOG|set\s*\(|asObject|Object\.apply|Function\.apply)/.test(P) || P.indexOf("browser.js") !== -1 || P.indexOf("node:internal") !== -1 || P.indexOf("node_modules") !== -1) continue;
        let N = P.match(/\((.*?):(\d+):(\d+)\)/);
        if (N || (N = P.match(/at\s+(.*?):(\d+):(\d+)/)), N) {
          const D = N[1], U = N[2], k = N[3];
          return D + ":" + U + ":" + k;
        }
      }
      return null;
    }
    return Tt.exports;
  }
  var Gh = qh();
  const Jh = /* @__PURE__ */ Wh(Gh);
  var Yn, Ei;
  function ka() {
    if (Ei) return Yn;
    Ei = 1;
    const e = (s) => s && typeof s.message == "string", t = (s) => {
      if (!s) return;
      const a = s.cause;
      if (typeof a == "function") {
        const c = s.cause();
        return e(c) ? c : void 0;
      } else
        return e(a) ? a : void 0;
    }, n = (s, a) => {
      if (!e(s)) return "";
      const c = s.stack || "";
      if (a.has(s))
        return c + `
causes have become circular...`;
      const u = t(s);
      return u ? (a.add(s), c + `
caused by: ` + n(u, a)) : c;
    }, r = (s) => n(s, /* @__PURE__ */ new Set()), o = (s, a, c) => {
      if (!e(s)) return "";
      const u = c ? "" : s.message || "";
      if (a.has(s))
        return u + ": ...";
      const l = t(s);
      if (l) {
        a.add(s);
        const f = typeof s.cause == "function";
        return u + (f ? "" : ": ") + o(l, a, f);
      } else
        return u;
    };
    return Yn = {
      isErrorLike: e,
      getErrorCause: t,
      stackWithCauses: r,
      messageWithCauses: (s) => o(s, /* @__PURE__ */ new Set())
    }, Yn;
  }
  var Xn, bi;
  function Da() {
    if (bi) return Xn;
    bi = 1;
    const e = /* @__PURE__ */ Symbol("circular-ref-tag"), t = /* @__PURE__ */ Symbol("pino-raw-err-ref"), n = Object.create({}, {
      type: {
        enumerable: !0,
        writable: !0,
        value: void 0
      },
      message: {
        enumerable: !0,
        writable: !0,
        value: void 0
      },
      stack: {
        enumerable: !0,
        writable: !0,
        value: void 0
      },
      aggregateErrors: {
        enumerable: !0,
        writable: !0,
        value: void 0
      },
      raw: {
        enumerable: !1,
        get: function() {
          return this[t];
        },
        set: function(r) {
          this[t] = r;
        }
      }
    });
    return Object.defineProperty(n, t, {
      writable: !0,
      value: {}
    }), Xn = {
      pinoErrProto: n,
      pinoErrorSymbols: {
        seen: e,
        rawSymbol: t
      }
    }, Xn;
  }
  var Qn, Si;
  function Kh() {
    if (Si) return Qn;
    Si = 1, Qn = a;
    const { messageWithCauses: e, stackWithCauses: t, isErrorLike: n } = ka(), { pinoErrProto: r, pinoErrorSymbols: o } = Da(), { seen: i } = o, { toString: s } = Object.prototype;
    function a(c) {
      if (!n(c))
        return c;
      c[i] = void 0;
      const u = Object.create(r);
      u.type = s.call(c.constructor) === "[object Function]" ? c.constructor.name : c.name, u.message = e(c), u.stack = t(c), Array.isArray(c.errors) && (u.aggregateErrors = c.errors.map((l) => a(l)));
      for (const l in c)
        if (u[l] === void 0) {
          const f = c[l];
          n(f) ? l !== "cause" && !Object.prototype.hasOwnProperty.call(f, i) && (u[l] = a(f)) : u[l] = f;
        }
      return delete c[i], u.raw = c, u;
    }
    return Qn;
  }
  var er, wi;
  function Zh() {
    if (wi) return er;
    wi = 1, er = i;
    const { isErrorLike: e } = ka(), { pinoErrProto: t, pinoErrorSymbols: n } = Da(), { seen: r } = n, { toString: o } = Object.prototype;
    function i(s) {
      if (!e(s))
        return s;
      s[r] = void 0;
      const a = Object.create(t);
      a.type = o.call(s.constructor) === "[object Function]" ? s.constructor.name : s.name, a.message = s.message, a.stack = s.stack, Array.isArray(s.errors) && (a.aggregateErrors = s.errors.map((c) => i(c))), e(s.cause) && !Object.prototype.hasOwnProperty.call(s.cause, r) && (a.cause = i(s.cause));
      for (const c in s)
        if (a[c] === void 0) {
          const u = s[c];
          e(u) ? Object.prototype.hasOwnProperty.call(u, r) || (a[c] = i(u)) : a[c] = u;
        }
      return delete s[r], a.raw = s, a;
    }
    return er;
  }
  var tr, vi;
  function Yh() {
    if (vi) return tr;
    vi = 1, tr = {
      mapHttpRequest: r,
      reqSerializer: n
    };
    const e = /* @__PURE__ */ Symbol("pino-raw-req-ref"), t = Object.create({}, {
      id: {
        enumerable: !0,
        writable: !0,
        value: ""
      },
      method: {
        enumerable: !0,
        writable: !0,
        value: ""
      },
      url: {
        enumerable: !0,
        writable: !0,
        value: ""
      },
      query: {
        enumerable: !0,
        writable: !0,
        value: ""
      },
      params: {
        enumerable: !0,
        writable: !0,
        value: ""
      },
      headers: {
        enumerable: !0,
        writable: !0,
        value: {}
      },
      remoteAddress: {
        enumerable: !0,
        writable: !0,
        value: ""
      },
      remotePort: {
        enumerable: !0,
        writable: !0,
        value: ""
      },
      raw: {
        enumerable: !1,
        get: function() {
          return this[e];
        },
        set: function(o) {
          this[e] = o;
        }
      }
    });
    Object.defineProperty(t, e, {
      writable: !0,
      value: {}
    });
    function n(o) {
      const i = o.info || o.socket, s = Object.create(t);
      if (s.id = typeof o.id == "function" ? o.id() : o.id || (o.info ? o.info.id : void 0), s.method = o.method, o.originalUrl)
        s.url = o.originalUrl;
      else {
        const a = o.path;
        s.url = typeof a == "string" ? a : o.url ? o.url.path || o.url : void 0;
      }
      return o.query && (s.query = o.query), o.params && (s.params = o.params), s.headers = o.headers, s.remoteAddress = i && i.remoteAddress, s.remotePort = i && i.remotePort, s.raw = o.raw || o, s;
    }
    function r(o) {
      return {
        req: n(o)
      };
    }
    return tr;
  }
  var nr, Ti;
  function Xh() {
    if (Ti) return nr;
    Ti = 1, nr = {
      mapHttpResponse: r,
      resSerializer: n
    };
    const e = /* @__PURE__ */ Symbol("pino-raw-res-ref"), t = Object.create({}, {
      statusCode: {
        enumerable: !0,
        writable: !0,
        value: 0
      },
      headers: {
        enumerable: !0,
        writable: !0,
        value: ""
      },
      raw: {
        enumerable: !1,
        get: function() {
          return this[e];
        },
        set: function(o) {
          this[e] = o;
        }
      }
    });
    Object.defineProperty(t, e, {
      writable: !0,
      value: {}
    });
    function n(o) {
      const i = Object.create(t);
      return i.statusCode = o.headersSent ? o.statusCode : null, i.headers = o.getHeaders ? o.getHeaders() : o._headers, i.raw = o, i;
    }
    function r(o) {
      return {
        res: n(o)
      };
    }
    return nr;
  }
  var rr, Ci;
  function Qh() {
    if (Ci) return rr;
    Ci = 1;
    const e = Kh(), t = Zh(), n = Yh(), r = Xh();
    return rr = {
      err: e,
      errWithCause: t,
      mapHttpRequest: n.mapHttpRequest,
      mapHttpResponse: r.mapHttpResponse,
      req: n.reqSerializer,
      res: r.resSerializer,
      wrapErrorSerializer: function(i) {
        return i === e ? i : function(a) {
          return i(e(a));
        };
      },
      wrapRequestSerializer: function(i) {
        return i === n.reqSerializer ? i : function(a) {
          return i(n.reqSerializer(a));
        };
      },
      wrapResponseSerializer: function(i) {
        return i === r.resSerializer ? i : function(a) {
          return i(r.resSerializer(a));
        };
      }
    }, rr;
  }
  var Tr = Qh();
  const or = (e) => {
    if (e.constructor === Nl) {
      const t = e;
      return {
        ...jh(t, "config", "request"),
        config: zh(t.config, "baseURL", "url")
      };
    }
    return Tr.err(e);
  }, em = () => typeof process > "u" ? "info" : process.env.HEAD_SITE_MAIN_SERVER_LOG_LEVEL || "info", tm = {
    level: em(),
    serializers: {
      req: Tr.req,
      res: Tr.res,
      e: or,
      err: or,
      error: or
    },
    browser: {
      asObject: !1
    }
  }, pe = Jh(tm), Ct = {};
  function G(e, t = !0) {
    if (e in Ct) return Ct[e];
    try {
      const n = document.getElementById(`pocket-${e}`);
      if (!n) {
        if (t)
          throw new Error(`Failed to get data from Pocket: ${e}`);
        return null;
      }
      Ct[e] = JSON.parse(n.innerText);
    } catch (n) {
      pe.error("Failed to get data from Pocket", e, n), Ct[e] = null;
    }
    return Ct[e];
  }
  function nm(e, t) {
    try {
      const n = `pocket-${e}`;
      let r = document.getElementById(n);
      r || (r = document.createElement("script"), r.id = n, r.type = "text/json", document.head.appendChild(r)), r.innerText = JSON.stringify(t);
    } catch (n) {
      pe.error("Failed to set data to Pocket", e, n);
    }
  }
  async function rm() {
    try {
      const t = await (await fetch("https://websiteapi.acronis.com/geo/location/v1/ip/geolocation")).json();
      nm("geo", t);
    } catch (e) {
      pe.error("Geolocation failed:", e);
    }
  }
  function en(e) {
    for (var t = 1; t < arguments.length; t++) {
      var n = arguments[t];
      for (var r in n)
        e[r] = n[r];
    }
    return e;
  }
  var om = {
    read: function(e) {
      return e[0] === '"' && (e = e.slice(1, -1)), e.replace(/(%[\dA-F]{2})+/gi, decodeURIComponent);
    },
    write: function(e) {
      return encodeURIComponent(e).replace(
        /%(2[346BF]|3[AC-F]|40|5[BDE]|60|7[BCD])/g,
        decodeURIComponent
      );
    }
  };
  function Cr(e, t) {
    function n(o, i, s) {
      if (!(typeof document > "u")) {
        s = en({}, t, s), typeof s.expires == "number" && (s.expires = new Date(Date.now() + s.expires * 864e5)), s.expires && (s.expires = s.expires.toUTCString()), o = encodeURIComponent(o).replace(/%(2[346B]|5E|60|7C)/g, decodeURIComponent).replace(/[()]/g, escape);
        var a = "";
        for (var c in s)
          s[c] && (a += "; " + c, s[c] !== !0 && (a += "=" + s[c].split(";")[0]));
        return document.cookie = o + "=" + e.write(i, o) + a;
      }
    }
    function r(o) {
      if (!(typeof document > "u" || arguments.length && !o)) {
        for (var i = document.cookie ? document.cookie.split("; ") : [], s = {}, a = 0; a < i.length; a++) {
          var c = i[a].split("="), u = c.slice(1).join("=");
          try {
            var l = decodeURIComponent(c[0]);
            if (s[l] = e.read(u, l), o === l)
              break;
          } catch {
          }
        }
        return o ? s[o] : s;
      }
    }
    return Object.create(
      {
        set: n,
        get: r,
        remove: function(o, i) {
          n(
            o,
            "",
            en({}, i, {
              expires: -1
            })
          );
        },
        withAttributes: function(o) {
          return Cr(this.converter, en({}, this.attributes, o));
        },
        withConverter: function(o) {
          return Cr(en({}, this.converter, o), this.attributes);
        }
      },
      {
        attributes: { value: Object.freeze(t) },
        converter: { value: Object.freeze(e) }
      }
    );
  }
  var dn = Cr(om, { path: "/" });
  function im() {
    (function(e, t, n, r, o, i) {
      e[n] = e[n] == 1 || t[n] == "yes" || t[n] == 1 || t[r] == 1 || e[o] && e[o].p && o[o][i]() ? 1 : 0;
    })(
      window,
      "navigator",
      "doNotTrack",
      "msDoNotTrack",
      "external",
      "msTrackingProtectionEnabled"
    ), (function(e, t, n, r, o) {
      e[r] = e[r] || [], e[r].push({ "gtm.start": (/* @__PURE__ */ new Date()).getTime(), event: "gtm.js" });
      const i = t.getElementsByTagName(n)[0], s = t.createElement(n), a = "";
      s.async = !0, s.src = `https://a.acronis.com/gtm.js?id=${o}${a}`, i.parentNode.insertBefore(s, i);
    })(window, document, "script", "dataLayer", "GTM-PFG6ZF");
  }
  function $a() {
    const e = G("clientinfo");
    switch (e?.ipSource) {
      case "internal":
      case "external":
        return e.ipSource;
      default:
        return "external";
    }
  }
  function sm() {
    const e = G("clientinfo");
    return !e || e.ja3 === "%%CLIENT_JA3%%" ? "" : e.ja3;
  }
  function am() {
    const e = G("clientinfo");
    return !e || e.ja4 === "%%CLIENT_JA4%%" ? "" : e.ja4;
  }
  function cm() {
    window.dataLayer = window.dataLayer || [];
    const e = $a(), t = e === "internal";
    dn.set("AcronisInternal", t.toString(), {
      path: "/",
      domain: ".acronis.com",
      expires: 30
      // days
    });
    const n = dn.get("_vwo_referrer");
    n && dn.remove("_vwo_referrer", { path: "/", domain: ".acronis.com" }), window.dataLayer.push({
      NewWebsite: 1,
      userType: e,
      ...n && { vwo_referrer: n }
    }), window._gtm_init || (window._gtm_init = 1, im());
  }
  function um() {
    window._vwo_code || (function() {
      var e = 816662, t = document, n = window, r = !1, o, i, s = {}, a = 2.2, c = 2e3, u = "body", l = "opacity:1 !important;filter:alpha(opacity=1) !important;";
      if (r = !1, o = t.querySelector("#vwoCode"), s = {}, -1 < t.URL.indexOf("__vwo_disable__") || n._vwo_code) return;
      try {
        var f = JSON.parse(localStorage.getItem("_vwo_" + e + "_config"));
        s = f && typeof f == "object" ? f : {};
      } catch {
      }
      function p(h) {
        try {
          return decodeURIComponent(h);
        } catch {
          return h;
        }
      }
      var d = (function() {
        var h = { combination: [], combinationChoose: [], split: [], exclude: [], uuid: null, consent: null, optOut: null }, g = t.cookie || "";
        if (!g) return h;
        for (var _, v, w = /(?:^|;\s*)(?:(_vis_opt_exp_(\d+)_combi=([^;]*))|(_vis_opt_exp_(\d+)_combi_choose=([^;]*))|(_vis_opt_exp_(\d+)_split=([^:;]*))|(_vis_opt_exp_(\d+)_exclude=[^;]*)|(_vis_opt_out=([^;]*))|(_vwo_global_opt_out=[^;]*)|(_vwo_uuid=([^;]*))|(_vwo_consent=([^;]*)))/g; (_ = w.exec(g)) !== null; ) try {
          _[1] ? h.combination.push({ id: _[2], value: p(_[3]) }) : _[4] ? h.combinationChoose.push({ id: _[5], value: p(_[6]) }) : _[7] ? h.split.push({ id: _[8], value: p(_[9]) }) : _[10] ? h.exclude.push({ id: _[11] }) : _[12] ? h.optOut = p(_[13]) : _[14] ? h.optOut = !0 : _[15] ? h.uuid = p(_[16]) : _[17] && (v = p(_[18]), h.consent = v && 3 <= v.length ? v.substring(0, 3) : null);
        } catch {
        }
        return h;
      })();
      function y() {
        var h = (function() {
          if (n.VWO && Array.isArray(n.VWO)) for (var v = 0; v < n.VWO.length; v++) {
            var w = n.VWO[v];
            if (Array.isArray(w) && (w[0] === "setVisitorId" || w[0] === "setSessionId")) return !0;
          }
          return !1;
        })(), g = "a=" + e + "&u=" + encodeURIComponent(n._vis_opt_url || t.URL) + "&vn=" + a + "&ph=1" + (typeof platform < "u" ? "&p=" + platform : "") + "&st=" + n.performance.now();
        h || ((_ = (function() {
          var v, w = [], T = {}, I = n.VWO && n.VWO.appliedCampaigns || {};
          for (v in I) {
            var L = I[v] && I[v].v;
            L && (w.push(v + "-" + L + "-1"), T[v] = !0);
          }
          if (d && d.combination) for (var x = 0; x < d.combination.length; x++) {
            var $ = d.combination[x];
            T[$.id] || w.push($.id + "-" + $.value);
          }
          return w.join("|");
        })()) && (g += "&c=" + _), (_ = (function() {
          var v = [], w = {};
          if (d && d.combinationChoose) for (var T = 0; T < d.combinationChoose.length; T++) {
            var I = d.combinationChoose[T];
            v.push(I.id + "-" + I.value), w[I.id] = !0;
          }
          if (d && d.split) for (var L = 0; L < d.split.length; L++) w[(I = d.split[L]).id] || v.push(I.id + "-" + I.value);
          return v.join("|");
        })()) && (g += "&cc=" + _), (_ = (function() {
          var v = {}, w = [];
          if (n.VWO && Array.isArray(n.VWO)) for (var T = 0; T < n.VWO.length; T++) {
            var I = n.VWO[T];
            if (Array.isArray(I) && I[0] === "setVariation" && I[1] && Array.isArray(I[1])) for (var L = 0; L < I[1].length; L++) {
              var x, $ = I[1][L];
              $ && typeof $ == "object" && (x = $.e, $ = $.v, x && $ && (v[x] = $));
            }
          }
          for (x in v) w.push(x + "-" + v[x]);
          return w.join("|");
        })()) && (g += "&sv=" + _)), d && d.optOut && (g += "&o=" + d.optOut);
        var _ = (function() {
          var v = [], w = {};
          if (d && d.exclude) for (var T = 0; T < d.exclude.length; T++) {
            var I = d.exclude[T];
            w[I.id] || (v.push(I.id), w[I.id] = !0);
          }
          return v.join("|");
        })();
        return _ && (g += "&e=" + _), d && d.uuid && (g += "&id=" + d.uuid), d && d.consent && (g += "&consent=" + d.consent), n.name && -1 < n.name.indexOf("_vis_preview") && (g += "&pM=true"), n.VWO && n.VWO.ed && (g += "&ed=" + n.VWO.ed), g;
      }
      i = { nonce: o && o.nonce, library_tolerance: function() {
        return typeof library_tolerance < "u" ? library_tolerance : void 0;
      }, settings_tolerance: function() {
        return s.sT || c;
      }, hide_element_style: function() {
        return "{" + (s.hES || l) + "}";
      }, hide_element: function() {
        return performance.getEntriesByName("first-contentful-paint")[0] ? "" : typeof s.hE == "string" ? s.hE : u;
      }, getVersion: function() {
        return a;
      }, finish: function(h) {
        var g;
        r || (r = !0, (g = t.getElementById("_vis_opt_path_hides")) && g.parentNode.removeChild(g), h && (new Image().src = "https://dev.visualwebsiteoptimizer.com/ee.gif?a=" + e + h));
      }, finished: function() {
        return r;
      }, addScript: function(h) {
        var g = t.createElement("script");
        g.type = "text/javascript", h.src ? g.src = h.src : g.text = h.text, o && g.setAttribute("nonce", o.nonce), t.getElementsByTagName("head")[0].appendChild(g);
      }, load: function(h, g) {
        g = g || {};
        var _ = new XMLHttpRequest();
        _.open("GET", h, !0), _.withCredentials = !g.dSC, _.responseType = g.responseType || "text", _.onload = function() {
          if (g.onloadCb) return g.onloadCb(_, h);
          _.status === 200 ? _vwo_code.addScript({ text: _.responseText }) : _vwo_code.finish("&e=loading_failure:" + h);
        }, _.onerror = function() {
          if (g.onerrorCb) return g.onerrorCb(h);
          _vwo_code.finish("&e=loading_failure:" + h);
        }, _.send();
      }, init: function() {
        var h, g = this.settings_tolerance();
        n._vwo_settings_timer = setTimeout(function() {
          _vwo_code.finish();
        }, g), this.hide_element() !== "body" ? (_ = t.createElement("style"), h = (g = this.hide_element()) ? g + this.hide_element_style() : "", g = t.getElementsByTagName("head")[0], _.setAttribute("id", "_vis_opt_path_hides"), o && _.setAttribute("nonce", o.nonce), _.setAttribute("type", "text/css"), _.styleSheet ? _.styleSheet.cssText = h : _.appendChild(t.createTextNode(h)), g.appendChild(_)) : (_ = t.getElementsByTagName("head")[0], (h = t.createElement("div")).style.cssText = "z-index: 2147483647 !important;position: fixed !important;left: 0 !important;top: 0 !important;width: 100% !important;height: 100% !important;background: white !important;", h.setAttribute("id", "_vis_opt_path_hides"), h.classList.add("_vis_hide_layer"), _.parentNode.insertBefore(h, _.nextSibling));
        var _ = "https://dev.visualwebsiteoptimizer.com/j.php?" + y();
        n.location.search.indexOf("_vwo_xhr") !== -1 ? this.addScript({ src: _ }) : this.load(_ + "&x=true", { l: 1 });
      } }, n._vwo_code = i;
    })();
  }
  const Ri = 2592e5, Fa = "thumbmark", ja = "https://api.thumbmarkjs.com", ao = { exclude: [], include: [], stabilize: ["private", "iframe"], logging: !0, timeout: 5e3, cache_api_call: !0, cache_lifetime_in_ms: 0, performance: !1, experimental: !1, property_name_factory: (e) => `${Fa}_${e}` }, lm = { private: [{ exclude: ["canvas"], browsers: ["firefox", "safari>=17", "brave"] }, { exclude: ["audio"], browsers: ["samsungbrowser", "safari"] }, { exclude: ["fonts"], browsers: ["firefox"] }, { exclude: ["audio.sampleHash", "hardware.deviceMemory", "header.acceptLanguage.q", "system.hardwareConcurrency", "plugins"], browsers: ["brave"] }, { exclude: ["tls.extensions"], browsers: ["firefox", "chrome", "safari"] }, { exclude: ["header.acceptLanguage"], browsers: ["edge", "chrome"] }], iframe: [{ exclude: ["system.applePayVersion", "system.cookieEnabled"], browsers: ["safari"] }, { exclude: ["permissions"] }], vpn: [{ exclude: ["ip"] }], always: [{ exclude: ["speech"], browsers: ["brave", "firefox"] }] }, Ii = typeof window < "u" ? window.OfflineAudioContext || window.webkitOfflineAudioContext : null;
  function fm(e) {
    let t = 0;
    for (let n = 0; n < e.length; ++n) t += Math.abs(e[n]);
    return t;
  }
  function Oi(e, t, n) {
    let r = [];
    for (let i = 0; i < e[0].data.length; i++) {
      let s = [];
      for (let a = 0; a < e.length; a++) s.push(e[a].data[i]);
      r.push(dm(s));
    }
    const o = new Uint8ClampedArray(r);
    return new ImageData(o, t, n);
  }
  function dm(e) {
    if (e.length === 0) return 0;
    const t = {};
    for (const r of e) t[r] = (t[r] || 0) + 1;
    let n = e[0];
    for (const r in t) t[r] > t[n] && (n = parseInt(r, 10));
    return n;
  }
  function tn(e) {
    return e ^= e >>> 16, e = Math.imul(e, 2246822507), e ^= e >>> 13, e = Math.imul(e, 3266489909), (e ^= e >>> 16) >>> 0;
  }
  const K = new Uint32Array([597399067, 2869860233, 951274213, 2716044179]);
  function ue(e, t) {
    return e << t | e >>> 32 - t;
  }
  function re(e, t = 0) {
    if (t = t ? 0 | t : 0, typeof e == "string" && (e = (function(o) {
      if (typeof TextEncoder < "u") return new TextEncoder().encode(o).buffer;
      const i = [];
      for (let s = 0; s < o.length; s++) {
        let a = o.charCodeAt(s);
        a < 128 ? i.push(a) : a < 2048 ? i.push(192 | a >> 6, 128 | 63 & a) : a < 55296 || a >= 57344 ? i.push(224 | a >> 12, 128 | a >> 6 & 63, 128 | 63 & a) : (s++, a = 65536 + ((1023 & a) << 10 | 1023 & o.charCodeAt(s)), i.push(240 | a >> 18, 128 | a >> 12 & 63, 128 | a >> 6 & 63, 128 | 63 & a));
      }
      return new Uint8Array(i).buffer;
    })(e)), !(e instanceof ArrayBuffer)) throw new TypeError("Expected key to be ArrayBuffer or string");
    const n = new Uint32Array([t, t, t, t]);
    (function(o, i) {
      const s = o.byteLength / 16 | 0, a = new Uint32Array(o, 0, 4 * s);
      for (let c = 0; c < s; c++) {
        const u = a.subarray(4 * c, 4 * (c + 1));
        u[0] = Math.imul(u[0], K[0]), u[0] = ue(u[0], 15), u[0] = Math.imul(u[0], K[1]), i[0] = i[0] ^ u[0], i[0] = ue(i[0], 19), i[0] = i[0] + i[1], i[0] = Math.imul(i[0], 5) + 1444728091, u[1] = Math.imul(u[1], K[1]), u[1] = ue(u[1], 16), u[1] = Math.imul(u[1], K[2]), i[1] = i[1] ^ u[1], i[1] = ue(i[1], 17), i[1] = i[1] + i[2], i[1] = Math.imul(i[1], 5) + 197830471, u[2] = Math.imul(u[2], K[2]), u[2] = ue(u[2], 17), u[2] = Math.imul(u[2], K[3]), i[2] = i[2] ^ u[2], i[2] = ue(i[2], 15), i[2] = i[2] + i[3], i[2] = Math.imul(i[2], 5) + 2530024501, u[3] = Math.imul(u[3], K[3]), u[3] = ue(u[3], 18), u[3] = Math.imul(u[3], K[0]), i[3] = i[3] ^ u[3], i[3] = ue(i[3], 13), i[3] = i[3] + i[0], i[3] = Math.imul(i[3], 5) + 850148119;
      }
    })(e, n), (function(o, i) {
      const s = o.byteLength / 16 | 0, a = o.byteLength % 16, c = new Uint32Array(4), u = new Uint8Array(o, 16 * s, a);
      switch (a) {
        case 15:
          c[3] = c[3] ^ u[14] << 16;
        case 14:
          c[3] = c[3] ^ u[13] << 8;
        case 13:
          c[3] = c[3] ^ u[12], c[3] = Math.imul(c[3], K[3]), c[3] = ue(c[3], 18), c[3] = Math.imul(c[3], K[0]), i[3] = i[3] ^ c[3];
        case 12:
          c[2] = c[2] ^ u[11] << 24;
        case 11:
          c[2] = c[2] ^ u[10] << 16;
        case 10:
          c[2] = c[2] ^ u[9] << 8;
        case 9:
          c[2] = c[2] ^ u[8], c[2] = Math.imul(c[2], K[2]), c[2] = ue(c[2], 17), c[2] = Math.imul(c[2], K[3]), i[2] = i[2] ^ c[2];
        case 8:
          c[1] = c[1] ^ u[7] << 24;
        case 7:
          c[1] = c[1] ^ u[6] << 16;
        case 6:
          c[1] = c[1] ^ u[5] << 8;
        case 5:
          c[1] = c[1] ^ u[4], c[1] = Math.imul(c[1], K[1]), c[1] = ue(c[1], 16), c[1] = Math.imul(c[1], K[2]), i[1] = i[1] ^ c[1];
        case 4:
          c[0] = c[0] ^ u[3] << 24;
        case 3:
          c[0] = c[0] ^ u[2] << 16;
        case 2:
          c[0] = c[0] ^ u[1] << 8;
        case 1:
          c[0] = c[0] ^ u[0], c[0] = Math.imul(c[0], K[0]), c[0] = ue(c[0], 15), c[0] = Math.imul(c[0], K[1]), i[0] = i[0] ^ c[0];
      }
    })(e, n), (function(o, i) {
      i[0] = i[0] ^ o.byteLength, i[1] = i[1] ^ o.byteLength, i[2] = i[2] ^ o.byteLength, i[3] = i[3] ^ o.byteLength, i[0] = i[0] + i[1] | 0, i[0] = i[0] + i[2] | 0, i[0] = i[0] + i[3] | 0, i[1] = i[1] + i[0] | 0, i[2] = i[2] + i[0] | 0, i[3] = i[3] + i[0] | 0, i[0] = tn(i[0]), i[1] = tn(i[1]), i[2] = tn(i[2]), i[3] = tn(i[3]), i[0] = i[0] + i[1] | 0, i[0] = i[0] + i[2] | 0, i[0] = i[0] + i[3] | 0, i[1] = i[1] + i[0] | 0, i[2] = i[2] + i[0] | 0, i[3] = i[3] + i[0] | 0;
    })(e, n);
    const r = new Uint8Array(n.buffer);
    return Array.from(r).map(((o) => o.toString(16).padStart(2, "0"))).join("");
  }
  const Mi = 280;
  async function Ba(e) {
    for (var t; !document.body; ) await pm(50);
    const n = document.createElement("iframe");
    n.setAttribute("frameBorder", "0");
    const r = n.style;
    r.setProperty("position", "fixed"), r.setProperty("display", "block", "important"), r.setProperty("visibility", "visible"), r.setProperty("border", "0"), r.setProperty("opacity", "0"), n.src = "about:blank", document.body.appendChild(n);
    const o = n.contentDocument || ((t = n.contentWindow) === null || t === void 0 ? void 0 : t.document);
    if (!o) throw new Error("Iframe document is not accessible");
    e({ iframe: o }), setTimeout((() => {
      document.body.removeChild(n);
    }), 0);
  }
  function pm(e, t) {
    return new Promise(((n) => setTimeout(n, e, t)));
  }
  const hm = ["Arial", "Arial Black", "Arial Narrow", "Arial Rounded MT", "Arimo", "Archivo", "Barlow", "Bebas Neue", "Bitter", "Bookman", "Calibri", "Cabin", "Candara", "Century", "Century Gothic", "Comic Sans MS", "Constantia", "Courier", "Courier New", "Crimson Text", "DM Mono", "DM Sans", "DM Serif Display", "DM Serif Text", "Dosis", "Droid Sans", "Exo", "Fira Code", "Fira Sans", "Franklin Gothic Medium", "Garamond", "Geneva", "Georgia", "Gill Sans", "Helvetica", "Impact", "Inconsolata", "Indie Flower", "Inter", "Josefin Sans", "Karla", "Lato", "Lexend", "Lucida Bright", "Lucida Console", "Lucida Sans Unicode", "Manrope", "Merriweather", "Merriweather Sans", "Montserrat", "Myriad", "Noto Sans", "Nunito", "Nunito Sans", "Open Sans", "Optima", "Orbitron", "Oswald", "Pacifico", "Palatino", "Perpetua", "PT Sans", "PT Serif", "Poppins", "Prompt", "Public Sans", "Quicksand", "Rajdhani", "Recursive", "Roboto", "Roboto Condensed", "Rockwell", "Rubik", "Segoe Print", "Segoe Script", "Segoe UI", "Sora", "Source Sans Pro", "Space Mono", "Tahoma", "Taviraj", "Times", "Times New Roman", "Titillium Web", "Trebuchet MS", "Ubuntu", "Varela Round", "Verdana", "Work Sans"], mm = ["monospace", "sans-serif", "serif"];
  function Pi(e, t) {
    return e.font = `72px ${t}`, e.measureText("WwMmLli0Oo").width;
  }
  function gm() {
    var e;
    const t = document.createElement("canvas"), n = (e = t.getContext("webgl")) !== null && e !== void 0 ? e : t.getContext("experimental-webgl");
    if (n && "getParameter" in n) try {
      const r = (n.getParameter(n.VENDOR) || "").toString(), o = (n.getParameter(n.RENDERER) || "").toString();
      let i = { vendor: r, renderer: o, version: (n.getParameter(n.VERSION) || "").toString(), shadingLanguageVersion: (n.getParameter(n.SHADING_LANGUAGE_VERSION) || "").toString() };
      if (!o.length || !r.length) {
        const s = n.getExtension("WEBGL_debug_renderer_info");
        if (s) {
          const a = (n.getParameter(s.UNMASKED_VENDOR_WEBGL) || "").toString(), c = (n.getParameter(s.UNMASKED_RENDERER_WEBGL) || "").toString();
          a && (i.vendorUnmasked = a), c && (i.rendererUnmasked = c);
        }
      }
      return i;
    } catch {
    }
    return "undefined";
  }
  function ym() {
    const e = new Float32Array(1), t = new Uint8Array(e.buffer);
    return e[0] = 1 / 0, e[0] = e[0] - e[0], t[3];
  }
  const nn = (e, t, n, r) => {
    const o = (n - t) / r;
    let i = 0;
    for (let s = 0; s < r; s++)
      i += e(t + (s + 0.5) * o);
    return i * o;
  };
  function Am(e, t) {
    const n = {};
    return t.forEach(((r) => {
      const o = (function(i) {
        if (i.length === 0) return null;
        const s = {};
        i.forEach(((u) => {
          const l = String(u);
          s[l] = (s[l] || 0) + 1;
        }));
        let a = i[0], c = 1;
        return Object.keys(s).forEach(((u) => {
          s[u] > c && (a = u, c = s[u]);
        })), a;
      })(e.map(((i) => r in i ? i[r] : void 0)).filter(((i) => i !== void 0)));
      o && (n[r] = o);
    })), n;
  }
  const _m = ["accelerometer", "accessibility", "accessibility-events", "ambient-light-sensor", "background-fetch", "background-sync", "bluetooth", "camera", "clipboard-read", "clipboard-write", "device-info", "display-capture", "gyroscope", "geolocation", "local-fonts", "magnetometer", "microphone", "midi", "nfc", "notifications", "payment-handler", "persistent-storage", "push", "speaker", "storage-access", "top-level-storage-access", "window-management", "query"];
  function co() {
    if (typeof navigator > "u") return { name: "unknown", version: "unknown" };
    const e = [{ name: "Brave", detect: () => !!navigator.brave }];
    for (const t of e) if (t.detect()) {
      const n = Ni(navigator.userAgent);
      return { name: t.name, version: n.version };
    }
    return Ni(navigator.userAgent);
  }
  function Ni(e) {
    var t, n, r, o, i, s;
    const a = [/(?<name>SamsungBrowser)\/(?<version>\d+(?:\.\d+)+)/, /(?<name>EdgA|EdgiOS|Edg)\/(?<version>\d+(?:\.\d+)+)/, /(?<name>OPR|OPX)\/(?<version>\d+(?:\.\d+)+)/, /Opera[\s\/](?<version>\d+(?:\.\d+)+)/, /Opera Mini\/(?<version>\d+(?:\.\d+)+)/, /Opera Mobi\/(?<version>\d+(?:\.\d+)+)/, /(?<name>Vivaldi)\/(?<version>\d+(?:\.\d+)+)/, /(?<name>CriOS)\/(?<version>\d+(?:\.\d+)+)/, /(?<name>FxiOS)\/(?<version>\d+(?:\.\d+)+)/, /(?<name>Chrome|Chromium)\/(?<version>\d+(?:\.\d+)+)/, /(?<name>Firefox|Waterfox|Iceweasel|IceCat)\/(?<version>\d+(?:\.\d+)+)/, /Version\/(?<version1>[\d.]+).*Safari\/[\d.]+|(?<name>Safari)\/(?<version2>[\d.]+)/, /(?<name>MSIE|Trident|IEMobile).+?(?<version>\d+(?:\.\d+)+)/, /(?<name>[A-Za-z]+)\/(?<version>\d+(?:\.\d+)+)/], c = { edg: "Edge", edga: "Edge", edgios: "Edge", opr: "Opera", opx: "Opera", crios: "Chrome", fxios: "Firefox", samsung: "SamsungBrowser", vivaldi: "Vivaldi" };
    for (const u of a) {
      const l = e.match(u);
      if (l) {
        let f = (t = l.groups) === null || t === void 0 ? void 0 : t.name, p = ((n = l.groups) === null || n === void 0 ? void 0 : n.version) || ((r = l.groups) === null || r === void 0 ? void 0 : r.version1) || ((o = l.groups) === null || o === void 0 ? void 0 : o.version2);
        if (f || !(!((i = l.groups) === null || i === void 0) && i.version1) && !(!((s = l.groups) === null || s === void 0) && s.version2) || (f = "Safari"), !f && u.source.includes("Opera Mini") && (f = "Opera Mini"), !f && u.source.includes("Opera Mobi") && (f = "Opera Mobi"), !f && u.source.includes("Opera") && (f = "Opera"), !f && l[1] && (f = l[1]), !p && l[2] && (p = l[2]), f)
          return { name: c[f.toLowerCase()] || f, version: p || "unknown" };
      }
    }
    return { name: "unknown", version: "unknown" };
  }
  function Li() {
    if (typeof navigator > "u" || !navigator.userAgent) return !1;
    const e = navigator.userAgent;
    return /Mobi|Android|iPhone|iPod|IEMobile|Opera Mini|Opera Mobi|webOS|BlackBerry|Windows Phone/i.test(e) && !/iPad/i.test(e);
  }
  function Em() {
    let e = [];
    const t = { "prefers-contrast": ["high", "more", "low", "less", "forced", "no-preference"], "any-hover": ["hover", "none"], "any-pointer": ["none", "coarse", "fine"], pointer: ["none", "coarse", "fine"], hover: ["hover", "none"], update: ["fast", "slow"], "inverted-colors": ["inverted", "none"], "prefers-reduced-motion": ["reduce", "no-preference"], "prefers-reduced-transparency": ["reduce", "no-preference"], scripting: ["none", "initial-only", "enabled"], "forced-colors": ["active", "none"] };
    return Object.keys(t).forEach(((n) => {
      t[n].forEach(((r) => {
        matchMedia(`(${n}: ${r})`).matches && e.push(`${n}: ${r}`);
      }));
    })), e;
  }
  function bm() {
    if (window.location.protocol === "https:" && typeof window.ApplePaySession == "function") try {
      const e = window.ApplePaySession.supportsVersion;
      for (let t = 15; t > 0; t--) if (e(t)) return t;
    } catch {
      return 0;
    }
    return 0;
  }
  const Sm = co().name !== "SamsungBrowser" ? 1 : 3;
  let Z, C = null;
  function fe(e) {
    const t = [];
    return (function n(r) {
      if (r && r.toJSON && typeof r.toJSON == "function" && (r = r.toJSON()), r === void 0) return;
      if (typeof r == "number") return isFinite(r) ? "" + r : "null";
      if (typeof r != "object") return JSON.stringify(r);
      let o, i;
      if (Array.isArray(r)) {
        for (i = "[", o = 0; o < r.length; o++) o && (i += ","), i += n(r[o]) || "null";
        return i + "]";
      }
      if (r === null) return "null";
      if (t.indexOf(r) !== -1) throw new TypeError("Converting circular structure to JSON");
      const s = t.push(r) - 1, a = Object.keys(r).sort();
      for (i = "", o = 0; o < a.length; o++) {
        const c = a[o], u = n(r[c]);
        u && (i && (i += ","), i += JSON.stringify(c) + ":" + u);
      }
      return t.splice(s, 1), "{" + i + "}";
    })(e) || "";
  }
  const Ua = ["𝔄", "𝔅", "ℭ", "𝔇", "𝔈", "𝔉", "𝔸", "𝔹", "ℂ", "𝔻", "𝔼", "𝔽"], Ha = ["β", "ψ", "λ", "ε", "ζ", "α", "ξ", "μ", "ρ", "φ", "κ", "τ", "η", "σ", "ι", "ω", "γ", "ν", "χ", "δ", "θ", "π", "υ", "ο"];
  function Pt(e, t) {
    return `<math><mrow>${t}</mrow></math>`;
  }
  function wm() {
    let e = "<mo>∏</mo>";
    return Ua.forEach(((t, n) => {
      const r = 2 * n, o = Ha.slice(r, r + 2);
      o.length === 2 && (e += `<mmultiscripts><mi>${t}</mi><none/><mi>${o[1]}</mi><mprescripts></mprescripts><mi>${o[0]}</mi><none/></mmultiscripts>`);
    })), Pt(0, `<munderover><mmultiscripts>${e}</mmultiscripts></munderover>`);
  }
  function vm() {
    const e = [];
    return Ua.forEach(((t, n) => {
      const r = 2 * n, o = Ha.slice(r, r + 2);
      o.length === 2 && e.push(Pt(0, `<mmultiscripts><mi>${t}</mi><none/><mi>${o[1]}</mi><mprescripts></mprescripts><mi>${o[0]}</mi><none/></mmultiscripts>`));
    })), e;
  }
  const Tm = { audio: async function() {
    return Ii ? (async function() {
      return new Promise(((e, t) => {
        try {
          const n = new Ii(1, 5e3, 44100), r = n.createBufferSource(), o = n.createOscillator();
          o.frequency.value = 1e3;
          const i = n.createDynamicsCompressor();
          let s;
          i.threshold.value = -50, i.knee.value = 40, i.ratio.value = 12, i.attack.value = 0, i.release.value = 0.2, o.connect(i), i.connect(n.destination), o.start(), n.oncomplete = (a) => {
            s = a.renderedBuffer.getChannelData(0), e({ sampleHash: fm(s), maxChannels: n.destination.maxChannelCount, channelCountMode: r.channelCountMode });
          }, n.startRendering();
        } catch (n) {
          console.error("Error creating audio fingerprint:", n), t(n);
        }
      }));
    })() : null;
  }, canvas: async function() {
    return new Promise(((e) => {
      const t = Array.from({ length: 3 }, (() => (function() {
        const n = document.createElement("canvas"), r = n.getContext("2d");
        if (!r) return null;
        n.width = Mi, n.height = 20;
        const o = r.createLinearGradient(0, 0, n.width, n.height);
        o.addColorStop(0, "red"), o.addColorStop(0.16666666666666666, "orange"), o.addColorStop(0.3333333333333333, "yellow"), o.addColorStop(0.5, "green"), o.addColorStop(0.6666666666666666, "blue"), o.addColorStop(0.8333333333333334, "indigo"), o.addColorStop(1, "violet"), r.fillStyle = o, r.fillRect(0, 0, n.width, n.height);
        const i = "Random Text WMwmil10Oo";
        return r.font = "23.123px Arial", r.fillStyle = "black", r.fillText(i, -5, 15), r.fillStyle = "rgba(0, 0, 255, 0.5)", r.fillText(i, -3.3, 17.7), r.beginPath(), r.moveTo(0, 0), r.lineTo(2 * n.width / 7, n.height), r.strokeStyle = "white", r.lineWidth = 2, r.stroke(), r.getImageData(0, 0, n.width, n.height);
      })())).filter(((n) => n !== null));
      t.length !== 0 ? e({ commonPixelsHash: re(Oi(t, Mi, 20).data.toString()).toString() }) : e(null);
    }));
  }, fonts: async function(e) {
    return new Promise(((t) => {
      try {
        Ba((async ({ iframe: n }) => {
          const r = n.createElement("canvas").getContext("2d");
          if (!r) return void t(null);
          const o = mm.map(((s) => Pi(r, s)));
          let i = {};
          hm.forEach(((s) => {
            const a = Pi(r, s);
            o.includes(a) || (i[s] = a);
          })), t(i);
        }));
      } catch {
        t(null);
      }
    }));
  }, hardware: function() {
    return new Promise(((e, t) => {
      const n = navigator.deviceMemory !== void 0 ? navigator.deviceMemory : 0, r = window.performance && window.performance.memory ? window.performance.memory : 0;
      e({ videocard: gm(), architecture: ym(), deviceMemory: n.toString() || "undefined", jsHeapSizeLimit: r.jsHeapSizeLimit || 0 });
    }));
  }, locales: function() {
    return new Promise(((e) => {
      e({ languages: navigator.language, timezone: Intl.DateTimeFormat().resolvedOptions().timeZone });
    }));
  }, math: function() {
    return new Promise(((e) => {
      e({ acos: Math.acos(0.5), asin: nn(Math.asin, -1, 1, 97), cos: nn(Math.cos, 0, Math.PI, 97), largeCos: Math.cos(1e20), largeSin: Math.sin(1e20), largeTan: Math.tan(1e20), sin: nn(Math.sin, -Math.PI, Math.PI, 97), tan: nn(Math.tan, 0, 2 * Math.PI, 97) });
    }));
  }, permissions: async function(e) {
    const t = e?.permissions_to_check || _m;
    return Am(await Promise.all(Array.from({ length: 3 }, (() => (async function(n) {
      const r = {};
      for (const o of n) try {
        const i = await navigator.permissions.query({ name: o });
        r[o] = i.state.toString();
      } catch {
      }
      return r;
    })(t)))), t);
  }, plugins: async function() {
    const e = [];
    if (navigator.plugins) for (let t = 0; t < navigator.plugins.length; t++) {
      const n = navigator.plugins[t];
      e.push([n.name, n.filename, n.description].join("|"));
    }
    return new Promise(((t) => {
      t({ plugins: e });
    }));
  }, screen: function() {
    return new Promise(((e) => {
      if (typeof matchMedia > "u" || typeof screen > "u") return void e(null);
      const t = { is_touchscreen: navigator.maxTouchPoints > 0, maxTouchPoints: navigator.maxTouchPoints, colorDepth: screen.colorDepth, mediaMatches: Em() };
      Li() && navigator.maxTouchPoints > 0 && (t.resolution = (function() {
        const n = window.screen.width, r = window.screen.height, o = Math.max(n, r).toString(), i = Math.min(n, r).toString();
        return `${o}x${i}`;
      })()), e(t);
    }));
  }, system: function() {
    return new Promise(((e) => {
      const t = co();
      e({ platform: window.navigator.platform, productSub: navigator.productSub, product: navigator.product, useragent: navigator.userAgent, hardwareConcurrency: navigator.hardwareConcurrency, browser: { name: t.name, version: t.version }, mobile: Li(), applePayVersion: bm(), cookieEnabled: window.navigator.cookieEnabled });
    }));
  }, webgl: async function() {
    typeof document < "u" && (Z = document.createElement("canvas"), Z.width = 200, Z.height = 100, C = Z.getContext("webgl"));
    try {
      if (!C) throw new Error("WebGL not supported");
      const e = Array.from({ length: Sm }, (() => (function() {
        try {
          if (!C) throw new Error("WebGL not supported");
          const t = `
          attribute vec2 position;
          void main() {
              gl_Position = vec4(position, 0.0, 1.0);
          }
      `, n = `
          precision mediump float;
          void main() {
              gl_FragColor = vec4(0.812, 0.195, 0.553, 0.921); // Set line color
          }
      `, r = C.createShader(C.VERTEX_SHADER), o = C.createShader(C.FRAGMENT_SHADER);
          if (!r || !o) throw new Error("Failed to create shaders");
          if (C.shaderSource(r, t), C.shaderSource(o, n), C.compileShader(r), !C.getShaderParameter(r, C.COMPILE_STATUS)) throw new Error("Vertex shader compilation failed: " + C.getShaderInfoLog(r));
          if (C.compileShader(o), !C.getShaderParameter(o, C.COMPILE_STATUS)) throw new Error("Fragment shader compilation failed: " + C.getShaderInfoLog(o));
          const i = C.createProgram();
          if (!i) throw new Error("Failed to create shader program");
          if (C.attachShader(i, r), C.attachShader(i, o), C.linkProgram(i), !C.getProgramParameter(i, C.LINK_STATUS)) throw new Error("Shader program linking failed: " + C.getProgramInfoLog(i));
          C.useProgram(i);
          const s = 137, a = new Float32Array(4 * s), c = 2 * Math.PI / s;
          for (let p = 0; p < s; p++) {
            const d = p * c;
            a[4 * p] = 0, a[4 * p + 1] = 0, a[4 * p + 2] = Math.cos(d) * (Z.width / 2), a[4 * p + 3] = Math.sin(d) * (Z.height / 2);
          }
          const u = C.createBuffer();
          C.bindBuffer(C.ARRAY_BUFFER, u), C.bufferData(C.ARRAY_BUFFER, a, C.STATIC_DRAW);
          const l = C.getAttribLocation(i, "position");
          C.enableVertexAttribArray(l), C.vertexAttribPointer(l, 2, C.FLOAT, !1, 0, 0), C.viewport(0, 0, Z.width, Z.height), C.clearColor(0, 0, 0, 1), C.clear(C.COLOR_BUFFER_BIT), C.drawArrays(C.LINES, 0, 2 * s);
          const f = new Uint8ClampedArray(Z.width * Z.height * 4);
          return C.readPixels(0, 0, Z.width, Z.height, C.RGBA, C.UNSIGNED_BYTE, f), new ImageData(f, Z.width, Z.height);
        } catch {
          return new ImageData(1, 1);
        } finally {
          C && (C.bindBuffer(C.ARRAY_BUFFER, null), C.useProgram(null), C.viewport(0, 0, C.drawingBufferWidth, C.drawingBufferHeight), C.clearColor(0, 0, 0, 0));
        }
      })()));
      return { commonPixelsHash: re(Oi(e, Z.width, Z.height).data.toString()).toString() };
    } catch {
      return { webgl: "unsupported" };
    }
  }, webrtc: async function(e) {
    return new Promise(((t) => {
      try {
        const n = window.RTCPeerConnection || window.webkitRTCPeerConnection || window.mozRTCPeerConnection;
        if (!n) return void t({ supported: !1, error: "WebRTC not supported" });
        const r = new n({ iceCandidatePoolSize: 1, iceServers: [] });
        r.createDataChannel(""), (async () => {
          try {
            const o = { offerToReceiveAudio: !0, offerToReceiveVideo: !0 }, i = await r.createOffer(o);
            await r.setLocalDescription(i);
            const s = i.sdp || "", a = [...new Set((s.match(/extmap:\d+ [^\n\r]+/g) || []).map(((g) => g.replace(/extmap:\d+ /, ""))))].sort(), c = (g) => {
              const _ = s.match(new RegExp(`m=${g} [^\\s]+ [^\\s]+ ([^\\n\\r]+)`));
              return _ ? _[1].split(" ") : [];
            }, u = (g, _) => _.map(((v) => {
              const w = new RegExp(`(rtpmap|fmtp|rtcp-fb):${v} (.+)`, "g"), T = [...s.matchAll(w)];
              if (!T.length) return null;
              const I = {};
              return T.forEach(((L) => {
                const [x, $, J] = L, ae = J.split("/");
                $ === "rtpmap" ? (I.mimeType = `${g}/${ae[0]}`, I.clockRate = +ae[1], g === "audio" && (I.channels = +ae[2] || 1)) : $ === "rtcp-fb" ? (I.feedbackSupport = I.feedbackSupport || [], I.feedbackSupport.push(J)) : $ === "fmtp" && (I.sdpFmtpLine = J);
              })), I;
            })).filter(Boolean), l = u("audio", c("audio")), f = u("video", c("video")), p = { audio: { count: l.length, hash: re(fe(l)) }, video: { count: f.length, hash: re(fe(f)) }, extensionsHash: re(fe(a)) }, d = e?.timeout || 5e3, y = Math.floor(0.9 * d), h = await new Promise(((g) => {
              const _ = setTimeout((() => {
                r.removeEventListener("icecandidate", v), r.close(), g({ supported: !0, ...p, timeout: !0 });
              }), y), v = (w) => {
                const T = w.candidate;
                T && T.candidate && (clearTimeout(_), r.removeEventListener("icecandidate", v), r.close(), g({ supported: !0, ...p, candidateType: T.type || "" }));
              };
              r.addEventListener("icecandidate", v);
            }));
            t({ details: h, hash: re(fe(h)) });
          } catch (o) {
            r.close(), t({ supported: !0, error: `WebRTC offer failed: ${o.message}` });
          }
        })();
      } catch (n) {
        t({ supported: !1, error: `WebRTC error: ${n.message}` });
      }
    }));
  }, speech: async function() {
    return new Promise(((e) => {
      try {
        if (typeof window > "u" || !window.speechSynthesis || typeof window.speechSynthesis.getVoices != "function") return void e({ supported: !1, error: "Speech Synthesis API not supported" });
        let t = !1, n = null;
        const r = (s) => {
          if (!t) {
            t = !0, n && clearTimeout(n);
            try {
              const a = s.map(((u) => {
                const l = (f) => f.replace(/\\/g, "\\\\").replace(/,/g, "\\,");
                return [l(u.voiceURI || ""), l(u.name || ""), l(u.lang || ""), u.localService ? "1" : "0", u.default ? "1" : "0"].join(",");
              }));
              a.sort();
              const c = { voiceCount: s.length, voicesHash: re(fe(a)) };
              e({ details: c, hash: re(fe(c)) });
            } catch (a) {
              e({ supported: !0, error: `Voice processing failed: ${a.message}` });
            }
          }
        }, o = window.speechSynthesis.getVoices();
        if (o.length > 0) return void r(o);
        n = setTimeout((() => {
          const s = window.speechSynthesis.getVoices();
          r(s);
        }), 800);
        const i = () => {
          window.speechSynthesis.removeEventListener("voiceschanged", i);
          const s = window.speechSynthesis.getVoices();
          r(s);
        };
        window.speechSynthesis.addEventListener("voiceschanged", i);
      } catch (t) {
        e({ supported: !1, error: `Speech Synthesis error: ${t.message}` });
      }
    }));
  } }, Cm = { intl: async function() {
    if (typeof Intl > "u") return null;
    try {
      const e = new Date(Date.UTC(2024, 0, 15, 12, 30, 45)), t = { dateFullFormat: new Intl.DateTimeFormat("en-US", { dateStyle: "full", timeZone: "UTC" }).format(e), dateMediumFormat: new Intl.DateTimeFormat("en-US", { dateStyle: "medium", timeZone: "UTC" }).format(e), timeFormat: new Intl.DateTimeFormat("en-US", { timeStyle: "long", timeZone: "UTC" }).format(e), numberFormat: new Intl.NumberFormat("en-US").format(123456.789), currencyFormat: new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" }).format(123456.789), percentFormat: new Intl.NumberFormat("en-US", { style: "percent" }).format(0.456), nonLatinDate: new Intl.DateTimeFormat("ar-EG", { timeZone: "UTC" }).format(e), nonLatinNumber: new Intl.NumberFormat("zh-Hans-CN-u-nu-hanidec").format(123456.789) }, n = Intl;
      return typeof n.ListFormat == "function" && (t.listFormat = new n.ListFormat("en", { type: "conjunction" }).format(["a", "b", "c"])), typeof n.DisplayNames == "function" && (t.displayNames = new n.DisplayNames("en", { type: "region" }).of("US") || ""), { details: t, hash: re(fe(t)) };
    } catch {
      return null;
    }
  }, mathml: async function() {
    return new Promise(((e) => {
      try {
        Ba((async ({ iframe: t }) => {
          try {
            if (!(function(s) {
              try {
                const a = s.createElement("math");
                a.innerHTML = "<mrow><mi>x</mi></mrow>", a.style.position = "absolute", a.style.visibility = "hidden", s.body.appendChild(a);
                const c = a.getBoundingClientRect();
                return s.body.removeChild(a), c.width > 0 && c.height > 0;
              } catch {
                return !1;
              }
            })(t)) return void e({ supported: !1, error: "MathML not supported" });
            const n = [Pt("integral", "<msubsup><mo>∫</mo><mi>a</mi><mi>b</mi></msubsup><mfrac><mrow><mi>f</mi><mo>(</mo><mi>x</mi><mo>)</mo></mrow><mrow><mi>g</mi><mo>(</mo><mi>x</mi><mo>)</mo></mrow></mfrac><mi>dx</mi>"), Pt("fraction", "<mfrac><mrow><mi>π</mi><mo>×</mo><msup><mi>r</mi><mn>2</mn></msup></mrow><mrow><mn>2</mn><mi>σ</mi></mrow></mfrac>"), Pt("matrix", "<mo>[</mo><mtable><mtr><mtd><mi>α</mi></mtd><mtd><mi>β</mi></mtd></mtr><mtr><mtd><mi>γ</mi></mtd><mtd><mi>δ</mi></mtd></mtr></mtable><mo>]</mo>"), wm(), ...vm()], r = [];
            let o = "";
            n.forEach(((s, a) => {
              const c = (function(u, l) {
                try {
                  const f = l.createElement("math");
                  f.innerHTML = u.replace(/<\/?math>/g, ""), f.style.whiteSpace = "nowrap", f.style.position = "absolute", f.style.visibility = "hidden", f.style.top = "-9999px", l.body.appendChild(f);
                  const p = f.getBoundingClientRect(), d = (l.defaultView || window).getComputedStyle(f), y = { dimensions: { width: p.width, height: p.height }, fontInfo: { fontFamily: d.fontFamily, fontSize: d.fontSize, fontWeight: d.fontWeight, fontStyle: d.fontStyle, lineHeight: d.lineHeight, fontVariant: d.fontVariant || "normal", fontStretch: d.fontStretch || "normal", fontSizeAdjust: d.fontSizeAdjust || "none", textRendering: d.textRendering || "auto", fontFeatureSettings: d.fontFeatureSettings || "normal", fontVariantNumeric: d.fontVariantNumeric || "normal", fontKerning: d.fontKerning || "auto" } };
                  return l.body.removeChild(f), y;
                } catch (f) {
                  return { error: f.message };
                }
              })(s, t);
              r.push({ width: c.dimensions.width, height: c.dimensions.height }), a === 0 && c.fontInfo && (o = re(fe(c.fontInfo)));
            }));
            const i = { fontStyleHash: o, dimensions: r };
            e({ details: i, hash: re(fe(i)) });
          } catch (n) {
            e({ supported: !1, error: `MathML error: ${n.message}` });
          }
        }));
      } catch (t) {
        e({ supported: !1, error: `MathML error: ${t.message}` });
      }
    }));
  }, mediadevices: async function() {
    if (typeof navigator > "u" || !navigator.mediaDevices || typeof navigator.mediaDevices.enumerateDevices != "function") return null;
    try {
      const e = await navigator.mediaDevices.enumerateDevices(), t = {};
      for (const n of e) t[n.kind] = (t[n.kind] || 0) + 1;
      return { audioinput: t.audioinput || 0, audiooutput: t.audiooutput || 0, videoinput: t.videoinput || 0 };
    } catch {
      return null;
    }
  } }, Rm = {}, Im = { timeout: "true" };
  function za(e, t) {
    var n, r;
    let o = co();
    if (o.name === "unknown" && t) {
      const u = (n = t.system) === null || n === void 0 ? void 0 : n.browser;
      u?.name && (o = { name: String(u.name), version: String(u.version || "unknown") });
    }
    const i = o.name.toLowerCase(), s = parseInt(o.version.split(".")[0] || "0", 10), a = [...e?.exclude || []], c = [.../* @__PURE__ */ new Set([...e?.stabilize || [], "always"])];
    for (const u of c) {
      const l = lm[u];
      if (l) for (const f of l) "browsers" in f && !(!((r = f.browsers) === null || r === void 0) && r.some(((p) => {
        const d = p.match(/(.+?)(>=)(\d+)/);
        return d ? i === d[1] && s >= +d[3] : i === p;
      }))) || a.push(...f.exclude);
    }
    return a;
  }
  function Wa(e, t) {
    const n = za(t, e), r = t?.include || [];
    return (function o(i, s = "") {
      const a = {};
      for (const [c, u] of Object.entries(i)) {
        const l = s ? `${s}.${c}` : c;
        if (typeof u != "object" || Array.isArray(u) || u === null) {
          const f = n.some(((d) => l.startsWith(d))), p = r.some(((d) => l.startsWith(d)));
          f && !p || (a[c] = u);
        } else {
          const f = o(u, l);
          Object.keys(f).length > 0 && (a[c] = f);
        }
      }
      return a;
    })(e);
  }
  const Va = "visitor_id";
  function qa(e) {
    return e.storage_property_name ? e.storage_property_name : e.property_name_factory(Va);
  }
  const xi = `${Fa}_${Va}`;
  function Ga(e, t) {
    try {
      localStorage.setItem(qa(t), e);
    } catch {
    }
  }
  const Ja = "cache";
  function Rr(e) {
    try {
      const t = localStorage.getItem(e.property_name_factory(Ja));
      return JSON.parse(t) || {};
    } catch {
    }
    return {};
  }
  function Om(e) {
    return e.cache_lifetime_in_ms > Ri ? Date.now() + Ri : Date.now() + e.cache_lifetime_in_ms;
  }
  class En extends Error {
    constructor(t) {
      super(`HTTP error! status: ${t}`), this.status = t;
    }
  }
  let Rt = null, Ir = null;
  async function Mm(e, t, n, r) {
    const o = await fetch(e, { method: "POST", headers: { "x-api-key": n.api_key, Authorization: "custom-authorized", "Content-Type": "application/json" }, body: JSON.stringify(t) });
    if (!o.ok) throw new En(o.status);
    const i = await o.json();
    return i.visitorId && i.visitorId !== r && Ga(i.visitorId, n), Ir = i, (function(s, a) {
      !s.cache_api_call || !s.cache_lifetime_in_ms || (function(c, u) {
        const l = { ...Rr(c), ...u };
        try {
          localStorage.setItem(c.property_name_factory(Ja), JSON.stringify(l));
        } catch {
        }
      })(s, { apiResponseExpiry: Om(s), apiResponse: a });
    })(n, i), i;
  }
  const Pm = (e, t) => {
    if (e.cache_api_call) {
      if (Ir) return Promise.resolve(Ir);
      const c = (function(u) {
        const l = Rr(u);
        if (l && l.apiResponse && l.apiResponseExpiry && Date.now() <= l.apiResponseExpiry) return l.apiResponse;
      })(e);
      if (c) return Promise.resolve(c);
      if (Rt) return Rt;
    }
    const n = `${e.api_endpoint || ja}/thumbmark`, r = (function(c) {
      try {
        const u = qa(c);
        let l = localStorage.getItem(u);
        return l || u === xi || (l = localStorage.getItem(xi), l && Ga(l, c)), l;
      } catch {
        return null;
      }
    })(e), o = { components: t, options: e, clientHash: re(fe(t)), version: "1.8.1" };
    if (r && (o.visitorId = r), e.metadata) {
      const c = typeof e.metadata == "function" ? e.metadata() : e.metadata;
      c && ((typeof c == "string" ? c.length : JSON.stringify(c).length) > 1e3 ? console.error("ThumbmarkJS: Metadata exceeds 1000 characters. Skipping metadata.") : o.metadata = c);
    }
    const i = e.timeout || 5e3, s = (async function(c, u, l, f) {
      for (let p = 0; p < 3; p++) {
        p > 0 && await new Promise(((d) => setTimeout(d, 200 * p)));
        try {
          return await Mm(c, u, l, f);
        } catch (d) {
          if (d instanceof En || p === 2) throw d;
        }
      }
      throw new Error("Unreachable");
    })(n, o, e, r).finally((() => {
      Rt = null;
    })), a = new Promise(((c) => {
      setTimeout((() => {
        const u = Rr(e);
        c(u?.apiResponse || { info: { timed_out: !0 }, ...r && { visitorId: r } });
      }), i);
    }));
    return Rt = Promise.race([s, a]), Rt;
  };
  async function Nm(e, t = {}) {
    var n, r;
    if (typeof document > "u" || typeof window > "u") return { thumbmark: "", components: {}, info: {}, version: "1.8.1", error: [{ type: "fatal", message: "Browser environment required" }] };
    try {
      const o = { ...ao, ...e }, i = [], s = o.logging && !sessionStorage.getItem("_tmjs_l") && Math.random() < 1e-4, a = { ...Tm, ...Rm, ...t }, { elapsed: c, resolvedComponents: u, errors: l } = await ki(a, o);
      i.push(...l);
      let f = {}, p = {};
      if (s || o.experimental) {
        const { elapsed: L, resolvedComponents: x, errors: $ } = await ki(Cm, o);
        f = x, p = L, i.push(...$);
      }
      const d = o.api_key ? Pm(o, u) : null;
      let y = null;
      if (d) try {
        y = await d;
      } catch (L) {
        if (L instanceof En && L.status === 403) return { error: [{ type: "api_unauthorized", message: "Invalid API key or quota exceeded" }], components: {}, info: {}, version: "1.8.1", thumbmark: "" };
        i.push({ type: L instanceof En ? "api_error" : "network_error", message: L instanceof Error ? L.message : String(L) });
      }
      !((n = y?.info) === null || n === void 0) && n.timed_out && i.push({ type: "api_timeout", message: "API request timed out" });
      const h = { ...c, ...p }, g = o.performance ? { elapsed: h } : {}, _ = Wa(y?.components || {}, o), v = { ...u, ..._ }, w = y?.info || { uniqueness: { score: "api only" } }, T = (r = y?.thumbmark) !== null && r !== void 0 ? r : re(fe(v)), I = "1.8.1";
      return s && (async function(L, x, $, J = {}, ae = []) {
        var Ee;
        const Me = `${ja}/log`, be = { thumbmark: L, components: x, experimental: J, version: "1.8.1", options: $, path: (Ee = window?.location) === null || Ee === void 0 ? void 0 : Ee.pathname, ...ae.length > 0 && { errors: ae } };
        sessionStorage.setItem("_tmjs_l", "1");
        try {
          await fetch(Me, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(be) });
        } catch {
        }
      })(T, v, o, f, i).catch((() => {
      })), { ...y?.visitorId && { visitorId: y.visitorId }, thumbmark: T, components: v, info: w, version: I, ...g, ...i.length > 0 && { error: i }, ...Object.keys(f).length > 0 && o.experimental && { experimental: f }, ...y?.requestId && { requestId: y.requestId }, ...y?.metadata && { metadata: y.metadata } };
    } catch (o) {
      return { thumbmark: "", components: {}, info: {}, version: "1.8.1", error: [{ type: "fatal", message: o instanceof Error ? o.message : String(o) }] };
    }
  }
  async function ki(e, t) {
    const n = { ...ao, ...t }, r = za(n).filter(((p) => !p.includes("."))), o = Object.entries(e).filter((([p]) => {
      var d;
      return !(!((d = n?.exclude) === null || d === void 0) && d.includes(p));
    })).filter((([p]) => !r.includes(p))).filter((([p]) => {
      var d, y, h, g;
      return !((d = n?.include) === null || d === void 0) && d.some(((_) => _.includes("."))) ? (y = n?.include) === null || y === void 0 ? void 0 : y.some(((_) => _.startsWith(p))) : ((h = n?.include) === null || h === void 0 ? void 0 : h.length) === 0 || ((g = n?.include) === null || g === void 0 ? void 0 : g.includes(p));
    })), i = o.map((([p]) => p)), s = o.map((([p, d]) => d(t))), a = await (function(p, d, y) {
      return Promise.all(p.map(((h) => {
        const g = performance.now();
        return Promise.race([h.then(((w) => ({ value: w, elapsed: performance.now() - g }))).catch(((w) => ({ value: y, elapsed: performance.now() - g, error: w instanceof Error ? w.message : String(w) }))), (_ = d, v = y, new Promise(((w) => {
          setTimeout((() => w(v)), _);
        }))).then(((w) => ({ value: w, elapsed: performance.now() - g, error: "timeout" })))]);
        var _, v;
      })));
    })(s, n?.timeout || 5e3, Im), c = {}, u = {}, l = [];
    a.forEach(((p, d) => {
      var y;
      const h = i[d];
      c[h] = (y = p.elapsed) !== null && y !== void 0 ? y : 0, p.error === "timeout" ? l.push({ type: "component_timeout", message: `Component '${h}' timed out`, component: h }) : p.error && l.push({ type: "component_error", message: p.error, component: h }), p.value != null && (u[h] = p.value);
    }));
    const f = Wa(u, n);
    return { elapsed: c, resolvedComponents: f, errors: l };
  }
  class Lm {
    constructor(t) {
      this.options = { ...ao, ...t }, this.customComponents = {};
    }
    async get(t) {
      return Nm({ ...this.options, ...t }, this.customComponents);
    }
    getVersion() {
      return "1.8.1";
    }
    includeComponent(t, n) {
      this.customComponents[t] = n;
    }
  }
  const ir = "tduid";
  function xm(e) {
    try {
      localStorage.setItem(ir, e);
      return;
    } catch {
    }
    try {
      sessionStorage.setItem(ir, e);
      return;
    } catch {
    }
    window.dataLayer = window.dataLayer || [], window.dataLayer.push({ [ir]: e });
  }
  async function km() {
    const e = G("matomo");
    window._paq = window._paq ?? [];
    const t = window._paq, { thumbmark: n } = await new Lm({ exclude: ["webrtc"] }).get();
    xm(n), t.push(["setCookieDomain", "*.acronis.com"]), t.push(["setDomains", ["*.acronis.com", "*.acronis.events", "*.trueimage.com"]]), t.push(["enableCrossDomainLinking"]), t.push(["disableCookies"]), t.push(["enableLinkTracking"]), t.push(["setTrackerUrl", `${e.baseUrl}/mat/matomo.php`]), t.push(["setSiteId", "1"]), t.push(["setUserId", n]);
    const r = document.createElement("script");
    r.async = !0, r.src = `${e.baseUrl}/mat/matomo.js`;
    const o = document.getElementsByTagName("script")[0];
    o.parentNode.insertBefore(r, o);
    const i = () => {
      function f(y) {
        return Math.round(y / 100) * 100;
      }
      const p = f(window.innerWidth), d = f(window.innerHeight);
      return `${p},${d}`;
    }, s = () => {
      const f = window.location.pathname.match(/^[/]([a-z][a-z]-[a-z][a-z])[/]/);
      return f && f[1] ? f[1] : null;
    }, a = G("ga_dl_params", !1) || {}, c = G("ga_rc_params", !1) || {}, u = G("ga_job_params", !1) || {};
    [
      { id: 2, val: dn.get("AcronisUID.en") },
      { id: 6, val: i() },
      { id: 5, val: window.devicePixelRatio?.toFixed(2) },
      { id: 14, val: s() },
      { id: 1, val: a.DL_PageType },
      { id: 3, val: a.DL_Audience },
      { id: 4, val: a.DL_Product },
      { id: 15, val: c.rcCategory },
      { id: 16, val: c.rcFeatured },
      { id: 17, val: c.rcProduct },
      { id: 18, val: c.rcType },
      { id: 19, val: u.pageDetailsA },
      { id: 20, val: u.pageDetailsB },
      { id: 21, val: u.pageDetailsC },
      { id: 22, val: $a() },
      { id: 23, val: sm() },
      { id: 24, val: am() }
    ].filter(({ val: f }) => f != null && f !== "(not set)" && f.toString().trim()).forEach((f) => t.push(["setCustomDimension", f.id, f.val])), t.push(["trackPageView"]);
  }
  function Dm() {
    Object.keys(localStorage || {}).filter((e) => e.startsWith("demoButtonClickHops")).forEach((e) => {
      const t = parseInt(localStorage.getItem(e), 10), n = Number.isNaN(t) ? 0 : t;
      localStorage.setItem(e, (n + 1).toString());
    });
  }
  const $m = "Monthly Payment", Fm = "version-5", Di = "s-purchasing-cyber-protect";
  function jm(e) {
    const t = e.pages?.page?.body?.find((s) => s.name === Di);
    if (!t || document.referrer.includes("account.acronis.com")) return !1;
    const n = t.data?.page || {};
    if (n.version !== 5) return !1;
    if (n.hasMonthlyPrice) return !0;
    const r = e.slices?.items?.[Di]?.[Fm]?.countries;
    if (!r) return !1;
    const i = G("geo", !1)?.data?.country?.code || "";
    return r.includes(i);
  }
  function Bm() {
    const e = JSON.parse(document.getElementById("app-state")?.textContent ?? "{}"), t = e.geolocation?.finalLocale, n = G("ga_dl_params", !1) || {}, r = G("ga_rc_params", !1) || {}, o = G("ga_job_params", !1) || {}, i = (window.splitsCurrentActiveSet || []).filter((c) => !!c.dimension).map((c) => [c.dimension, `${c.id}.${c.variant}`]), s = { ...Object.fromEntries(i) }, a = {
      event: "BOGO",
      touchpoint: "website",
      siteLocale: t || "(not set)",
      DL_PageType: n.DL_PageType || "(not set)",
      DL_Audience: n.DL_Audience || "(not set)",
      DL_Product: n.DL_Product || "(not set)",
      rcProduct: r.rcProduct || "(not set)",
      rcFeatured: r.rcFeatured || "(not set)",
      rcCategory: r.rcCategory || "(not set)",
      rcType: r.rcType || "(not set)",
      ...o,
      ...s,
      ...jm(e) && { pageDetailsA: $m }
    };
    window.dataLayer.push(a);
  }
  const Um = {
    DZ: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    AO: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    BJ: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    BW: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    BF: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    CM: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    TD: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    KM: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    CG: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    CD: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    CI: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    ET: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    GA: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    GM: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    GH: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    GN: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    KE: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    LS: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    LY: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    MW: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    ML: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    MU: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    YT: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    MA: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    MZ: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    NA: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    NG: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    RE: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    RW: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    SN: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    ZA: ["Europe, Israel & Africa", "Africa", "South Africa", "South Africa"],
    TZ: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    TN: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    UG: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    ZM: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    ZW: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    EG: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    BH: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    IQ: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    JO: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    KW: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    LB: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    OM: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    PK: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    PS: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    QA: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    SA: ["APJ & ME", "Middle East", "Middle East", "Saudi Arabia"],
    AE: ["APJ & ME", "Middle East", "Middle East", "United Arab Emirates"],
    YE: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    AT: ["Europe, Israel & Africa", "DACH", "DACH", "Austria"],
    DE: ["Europe, Israel & Africa", "DACH", "DACH", "Germany"],
    LI: ["Europe, Israel & Africa", "DACH", "DACH", "Liechtenstein"],
    CH: ["Europe, Israel & Africa", "DACH", "DACH", "Switzerland"],
    IL: ["Europe, Israel & Africa", "Israel", "Israel", "Israel"],
    BE: ["Europe, Israel & Africa", "BENELUX", "BENELUX", "Belgium - Luxembourg"],
    LU: ["Europe, Israel & Africa", "BENELUX", "BENELUX", "Belgium - Luxembourg"],
    NL: ["Europe, Israel & Africa", "BENELUX", "BENELUX", "Netherlands"],
    DK: ["Europe, Israel & Africa", "Northern Europe", "Nordics", "Nordics"],
    FO: ["Europe, Israel & Africa", "Northern Europe", "Nordics", "Nordics"],
    FI: ["Europe, Israel & Africa", "Northern Europe", "Nordics", "Nordics"],
    IS: ["Europe, Israel & Africa", "Northern Europe", "Nordics", "Nordics"],
    NO: ["Europe, Israel & Africa", "Northern Europe", "Nordics", "Nordics"],
    SE: ["Europe, Israel & Africa", "Northern Europe", "Nordics", "Nordics"],
    IE: ["Europe, Israel & Africa", "Northern Europe", "UK - Ireland", "Ireland"],
    GB: ["Europe, Israel & Africa", "Northern Europe", "UK - Ireland", "United Kingdom"],
    AL: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    AM: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    BA: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    HR: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    KZ: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    MK: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    MD: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    MN: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    ME: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    RS: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    SI: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    BG: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    CY: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    GR: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    MT: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    RO: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    TR: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Turkey"],
    CZ: ["Europe, Israel & Africa", "Rest of Europe", "Eastern Europe", "Rest of Eastern Europe"],
    EE: ["Europe, Israel & Africa", "Rest of Europe", "Eastern Europe", "Rest of Eastern Europe"],
    HU: ["Europe, Israel & Africa", "Rest of Europe", "Eastern Europe", "Rest of Eastern Europe"],
    LV: ["Europe, Israel & Africa", "Rest of Europe", "Eastern Europe", "Rest of Eastern Europe"],
    LT: ["Europe, Israel & Africa", "Rest of Europe", "Eastern Europe", "Rest of Eastern Europe"],
    PL: ["Europe, Israel & Africa", "Rest of Europe", "Eastern Europe", "Poland"],
    SK: ["Europe, Israel & Africa", "Rest of Europe", "Eastern Europe", "Rest of Eastern Europe"],
    UA: ["Europe, Israel & Africa", "Rest of Europe", "Eastern Europe", "Rest of Eastern Europe"],
    FR: ["Europe, Israel & Africa", "Rest of Europe", "France", "France"],
    GI: ["Europe, Israel & Africa", "Rest of Europe", "Iberia", "Rest of Iberia"],
    PT: ["Europe, Israel & Africa", "Rest of Europe", "Iberia", "Rest of Iberia"],
    ES: ["Europe, Israel & Africa", "Rest of Europe", "Iberia", "Spain"],
    IT: ["Europe, Israel & Africa", "Rest of Europe", "Italy", "Italy"],
    SM: ["Europe, Israel & Africa", "Rest of Europe", "Italy", "Italy"],
    AU: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    FJ: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    NC: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    NZ: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    PG: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    WS: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    ID: ["APJ & ME", "APAC", "South East Asia", "Indonesia"],
    MY: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    PH: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    TH: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    KH: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    MM: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    VN: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    CN: ["APJ & ME", "APAC", "GCR", "GCR"],
    HK: ["APJ & ME", "APAC", "GCR", "GCR"],
    TW: ["APJ & ME", "APAC", "GCR", "GCR"],
    IN: ["APJ & ME", "APAC", "India and South Asia", "India"],
    BD: ["APJ & ME", "APAC", "India and South Asia", "South Asia"],
    MV: ["APJ & ME", "APAC", "India and South Asia", "South Asia"],
    NP: ["APJ & ME", "APAC", "India and South Asia", "South Asia"],
    LK: ["APJ & ME", "APAC", "India and South Asia", "South Asia"],
    KR: ["APJ & ME", "APAC", "Korea", "Korea"],
    SG: ["APJ & ME", "APAC", "Singapore", "Singapore"],
    JP: ["APJ & ME", "Japan", "Japan", "Japan"],
    BR: ["Americas", "LATAM", "Brazil", "Brazil"],
    AW: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    BS: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    BB: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    BM: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    CR: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    DM: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    DO: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    SV: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    GF: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    GP: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    GT: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    HN: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    JM: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    MQ: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    NI: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    PA: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    LC: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    VC: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    SR: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    TT: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    VE: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    CO: ["Americas", "LATAM", "Northern LATAM", "Colombia"],
    MX: ["Americas", "LATAM", "Northern LATAM", "Mexico"],
    AR: ["Americas", "LATAM", "Southern LATAM", "Rest of Southern LATAM"],
    BO: ["Americas", "LATAM", "Southern LATAM", "Rest of Southern LATAM"],
    CL: ["Americas", "LATAM", "Southern LATAM", "Chile"],
    EC: ["Americas", "LATAM", "Southern LATAM", "Rest of Southern LATAM"],
    PY: ["Americas", "LATAM", "Southern LATAM", "Rest of Southern LATAM"],
    PE: ["Americas", "LATAM", "Southern LATAM", "Peru"],
    UY: ["Americas", "LATAM", "Southern LATAM", "Rest of Southern LATAM"],
    CA: ["Americas", "North America", "North America", "Canada"],
    PR: ["Americas", "North America", "North America", "United States"],
    US: ["Americas", "North America", "North America", "United States"],
    AI: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    AG: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    BZ: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    KY: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    CU: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    CW: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    FK: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    GD: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    GY: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    HT: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    MS: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    AN: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    BL: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    KN: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    MF: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    PM: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    BQ: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    SX: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    TC: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    UM: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    VG: ["Americas", "LATAM", "Northern LATAM", "Caribbean and Central America"],
    AQ: ["Americas", "LATAM", "Southern LATAM", "Rest of Southern LATAM"],
    GS: ["Americas", "LATAM", "Southern LATAM", "Rest of Southern LATAM"],
    VI: ["Americas", "North America", "North America", "United States"],
    AS: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    CK: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    PF: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    GU: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    HM: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    KI: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    MH: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    FM: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    NR: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    NU: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    NF: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    MP: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    PW: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    PN: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    SB: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    TL: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    TK: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    TO: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    VU: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    WF: ["APJ & ME", "APAC", "ANZ", "ANZ"],
    BN: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    CX: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    CC: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    TP: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    LA: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    TV: ["APJ & ME", "APAC", "South East Asia", "Rest of South East Asia"],
    MO: ["APJ & ME", "APAC", "GCR", "GCR"],
    BT: ["APJ & ME", "APAC", "India and South Asia", "South Asia"],
    IO: ["APJ & ME", "APAC", "India and South Asia", "South Asia"],
    KP: ["APJ & ME", "APAC", "Korea", "Korea"],
    AC: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    BV: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    BI: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    CV: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    CF: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    DJ: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    GQ: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    ER: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    TF: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    GW: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    LR: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    MG: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    MR: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    NE: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    SH: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    ST: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    SC: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    SL: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    SO: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    SD: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    SZ: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    TG: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    EH: ["Europe, Israel & Africa", "Africa", "Rest of Africa", "Rest of Africa"],
    AF: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    IR: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    SY: ["APJ & ME", "Middle East", "Middle East", "Rest of Middle East"],
    AX: ["Europe, Israel & Africa", "Northern Europe", "Nordics", "Nordics"],
    GL: ["Europe, Israel & Africa", "Northern Europe", "Nordics", "Nordics"],
    SJ: ["Europe, Israel & Africa", "Northern Europe", "Nordics", "Nordics"],
    GG: ["Europe, Israel & Africa", "Northern Europe", "UK - Ireland", "United Kingdom"],
    IM: ["Europe, Israel & Africa", "Northern Europe", "UK - Ireland", "United Kingdom"],
    JE: ["Europe, Israel & Africa", "Northern Europe", "UK - Ireland", "United Kingdom"],
    AZ: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    BY: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    GE: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    XK: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    KG: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    TJ: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    TM: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    UZ: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    YU: ["Europe, Israel & Africa", "Rest of Europe", "Balkans - Turkey - Mediterranean - CIS", "Balkans - Mediterranean - CIS"],
    AD: ["Europe, Israel & Africa", "Rest of Europe", "France", "France"],
    MC: ["Europe, Israel & Africa", "Rest of Europe", "France", "France"],
    IC: ["Europe, Israel & Africa", "Rest of Europe", "Iberia", "Spain"],
    VA: ["Europe, Israel & Africa", "Rest of Europe", "Italy", "Italy"],
    RU: ["Russia", "Russia", "Russia", "Russia"]
  };
  function Hm() {
    const e = G("geo", !1);
    if (!e || !e.data?.country?.code)
      return;
    const t = Um[e.data.country.code];
    if (!t) {
      pe.warn("No descriptor for country", e.data.country.code);
      return;
    }
    const n = {
      country: e.data.country.name,
      geography: t[0],
      region: t[1],
      subRegion: t[2],
      budgetRegion: t[3]
    };
    window.dataLayer.push(n);
  }
  const Ka = "acronis_ab_storage";
  function $i() {
    return !window.localStorage || !window.localStorage.getItem;
  }
  function zm() {
    return !!window.crypto?.getRandomValues;
  }
  function Wm() {
    return Math.floor(Math.random() * 100);
  }
  function Vm() {
    const e = new Uint32Array(1);
    crypto.getRandomValues(e);
    let t = e[0] / 4294967296;
    return t = Math.floor(t * 100), t;
  }
  const qm = zm() ? Vm : Wm;
  function Gm() {
    try {
      const e = window.localStorage.getItem(Ka) || "{}";
      return JSON.parse(e);
    } catch {
      return {};
    }
  }
  const at = Gm();
  function Fi() {
    const e = JSON.stringify(at, null, 2);
    window.localStorage.setItem(Ka, e);
  }
  function Jm(e) {
    return e in at;
  }
  function Km(e) {
    return at[e];
  }
  function Zm(e, t) {
    at[e] = t;
  }
  function Ym(e) {
    const t = qm();
    let n = 0;
    for (const r of e)
      if (n += r.size, t < n)
        return r.id;
    return null;
  }
  function Xm(e) {
    return !!(e.id && e.variants.find((n) => n.isControl) && e.variants.reduce((n, r) => n + r.size, 0) === 100);
  }
  function Qm(e) {
    if (!e.urlsAllowed || !e.urlsAllowed.length)
      return !0;
    for (const t of e.urlsAllowed)
      try {
        if (new RegExp(t.reg, t.flags || "i").test(window.location.toString()))
          return !0;
      } catch {
        pe.error(t, "[AB] Failed to construct urlsAllowed regexp");
      }
    return !1;
  }
  function eg() {
    const e = G("splits"), n = e.items.filter(Xm).map((i) => {
      const s = i.id;
      if ($i() || !Qm(i)) {
        const c = i.variants.find((u) => u.isControl);
        return {
          id: s,
          variant: c.id,
          dimension: null
        };
      }
      if (Jm(s))
        return {
          id: s,
          variant: Km(s),
          dimension: i.dimension
        };
      const a = {
        id: s,
        variant: Ym(i.variants),
        dimension: i.dimension
      };
      return Zm(s, a.variant), Fi(), a;
    }), r = document.querySelector("html");
    if (n.forEach((i) => {
      i.dimension && r.classList.add(`split-${i.id}-${i.variant}`);
    }), window.splitsCurrentActiveSet = n, $i()) {
      pe.info("[AB] Control mode");
      return;
    }
    const o = new Set(e.items.map((i) => i.id));
    Object.keys(at).filter((i) => !o.has(i)).forEach((i) => {
      delete at[i];
    }), Fi();
  }
  const tg = async (e, t) => {
    try {
      if (typeof document > "u") return;
      const n = await fetch(t).then((a) => a.text()), r = document.createElement("div");
      r.innerHTML = n;
      const o = r.querySelector("svg");
      if (!o) return;
      let i = document.getElementById(e);
      if (!i) {
        i = document.createElement("div"), i.id = e, i.className = "hidden", i.style.cssText = "position:absolute;width:0;height:0;overflow:hidden;", i.appendChild(o), document.body.prepend(i);
        return;
      }
      const s = i.querySelector("svg");
      if (!s) {
        i.appendChild(o);
        return;
      }
      o.querySelectorAll("symbol").forEach((a) => {
        try {
          document.getElementById(a.id) || s.appendChild(a);
        } catch {
        }
      });
    } catch (n) {
      pe.error({ error: n });
    }
  };
  async function ng() {
    const e = G("svg_symbols", !1);
    e?.length && await tg("svg-sprite-critical", `/dist/svg/?q=${e.join(",")}`);
  }
  function rg() {
    window.VWO_OUTCOME = "pending";
    const e = "VWO_REDIRECT";
    G("vwo_opts", !1)?.enabled || (window.VWO_OUTCOME = "regular"), window.sessionStorage.getItem(e) === "true" && (window.sessionStorage.removeItem(e), window.VWO_OUTCOME = "regular"), window._vwo_evq = window._vwo_evq || [];
    const r = window._vwo_evq.push;
    window._vwo_evq.push = (...o) => {
      const i = o[0];
      if (i[0] === "rD") {
        const s = i[1];
        window.VWO_OUTCOME = s ? "redirect" : "regular", s && (window.dataLayer = window.dataLayer || [], window.dataLayer.push({ vwoRedirect: "true" }), window.sessionStorage.setItem(e, "true"));
      }
      return r.apply(window._vwo_evq, [].slice.call(o));
    };
  }
  function og() {
    if (typeof window._vwo_code?.init != "function") {
      window.VWO_OUTCOME = "regular";
      return;
    }
    window._vwo_code.init();
  }
  const M = typeof __SENTRY_DEBUG__ > "u" || __SENTRY_DEBUG__, F = globalThis, ze = "10.50.0";
  function Nn() {
    return Ln(F), F;
  }
  function Ln(e) {
    const t = e.__SENTRY__ = e.__SENTRY__ || {};
    return t.version = t.version || ze, t[ze] = t[ze] || {};
  }
  function mt(e, t, n = F) {
    const r = n.__SENTRY__ = n.__SENTRY__ || {}, o = r[ze] = r[ze] || {};
    return o[e] || (o[e] = t());
  }
  const ig = [
    "debug",
    "info",
    "warn",
    "error",
    "log",
    "assert",
    "trace"
  ], sg = "Sentry Logger ", bn = {};
  function gt(e) {
    if (!("console" in F))
      return e();
    const t = F.console, n = {}, r = Object.keys(bn);
    r.forEach((o) => {
      const i = bn[o];
      n[o] = t[o], t[o] = i;
    });
    try {
      return e();
    } finally {
      r.forEach((o) => {
        t[o] = n[o];
      });
    }
  }
  function ag() {
    lo().enabled = !0;
  }
  function cg() {
    lo().enabled = !1;
  }
  function Za() {
    return lo().enabled;
  }
  function ug(...e) {
    uo("log", ...e);
  }
  function lg(...e) {
    uo("warn", ...e);
  }
  function fg(...e) {
    uo("error", ...e);
  }
  function uo(e, ...t) {
    M && Za() && gt(() => {
      F.console[e](`${sg}[${e}]:`, ...t);
    });
  }
  function lo() {
    return M ? mt("loggerSettings", () => ({ enabled: !1 })) : { enabled: !1 };
  }
  const O = {
    /** Enable logging. */
    enable: ag,
    /** Disable logging. */
    disable: cg,
    /** Check if logging is enabled. */
    isEnabled: Za,
    /** Log a message. */
    log: ug,
    /** Log a warning. */
    warn: lg,
    /** Log an error. */
    error: fg
  }, Ya = 50, qe = "?", ji = /\(error: (.*)\)/, Bi = /captureMessage|captureException/;
  function Xa(...e) {
    const t = e.sort((n, r) => n[0] - r[0]).map((n) => n[1]);
    return (n, r = 0, o = 0) => {
      const i = [], s = n.split(`
`);
      for (let a = r; a < s.length; a++) {
        let c = s[a];
        c.length > 1024 && (c = c.slice(0, 1024));
        const u = ji.test(c) ? c.replace(ji, "$1") : c;
        if (!u.includes("Error: ")) {
          for (const l of t) {
            const f = l(u);
            if (f) {
              i.push(f);
              break;
            }
          }
          if (i.length >= Ya + o)
            break;
        }
      }
      return pg(i.slice(o));
    };
  }
  function dg(e) {
    return Array.isArray(e) ? Xa(...e) : e;
  }
  function pg(e) {
    if (!e.length)
      return [];
    const t = Array.from(e);
    return /sentryWrapped/.test(rn(t).function || "") && t.pop(), t.reverse(), Bi.test(rn(t).function || "") && (t.pop(), Bi.test(rn(t).function || "") && t.pop()), t.slice(0, Ya).map((n) => ({
      ...n,
      filename: n.filename || rn(t).filename,
      function: n.function || qe
    }));
  }
  function rn(e) {
    return e[e.length - 1] || {};
  }
  const sr = "<anonymous>";
  function Le(e) {
    try {
      return !e || typeof e != "function" ? sr : e.name || sr;
    } catch {
      return sr;
    }
  }
  function Ui(e) {
    const t = e.exception;
    if (t) {
      const n = [];
      try {
        return t.values.forEach((r) => {
          r.stacktrace.frames && n.push(...r.stacktrace.frames);
        }), n;
      } catch {
        return;
      }
    }
  }
  function Qa(e) {
    return "__v_isVNode" in e && e.__v_isVNode ? "[VueVNode]" : "[VueViewModel]";
  }
  const pn = {}, Hi = {};
  function Xe(e, t) {
    pn[e] = pn[e] || [], pn[e].push(t);
  }
  function Qe(e, t) {
    if (!Hi[e]) {
      Hi[e] = !0;
      try {
        t();
      } catch (n) {
        M && O.error(`Error while instrumenting ${e}`, n);
      }
    }
  }
  function de(e, t) {
    const n = e && pn[e];
    if (n)
      for (const r of n)
        try {
          r(t);
        } catch (o) {
          M && O.error(
            `Error while triggering instrumentation handler.
Type: ${e}
Name: ${Le(r)}
Error:`,
            o
          );
        }
  }
  let ar = null;
  function hg(e) {
    const t = "error";
    Xe(t, e), Qe(t, mg);
  }
  function mg() {
    ar = F.onerror, F.onerror = function(e, t, n, r, o) {
      return de("error", {
        column: r,
        error: o,
        line: n,
        msg: e,
        url: t
      }), ar ? ar.apply(this, arguments) : !1;
    }, F.onerror.__SENTRY_INSTRUMENTED__ = !0;
  }
  let cr = null;
  function gg(e) {
    const t = "unhandledrejection";
    Xe(t, e), Qe(t, yg);
  }
  function yg() {
    cr = F.onunhandledrejection, F.onunhandledrejection = function(e) {
      return de("unhandledrejection", e), cr ? cr.apply(this, arguments) : !0;
    }, F.onunhandledrejection.__SENTRY_INSTRUMENTED__ = !0;
  }
  const ec = Object.prototype.toString;
  function xn(e) {
    switch (ec.call(e)) {
      case "[object Error]":
      case "[object Exception]":
      case "[object DOMException]":
      case "[object WebAssembly.Exception]":
        return !0;
      default:
        return xe(e, Error);
    }
  }
  function yt(e, t) {
    return ec.call(e) === `[object ${t}]`;
  }
  function tc(e) {
    return yt(e, "ErrorEvent");
  }
  function zi(e) {
    return yt(e, "DOMError");
  }
  function Ag(e) {
    return yt(e, "DOMException");
  }
  function Te(e) {
    return yt(e, "String");
  }
  function fo(e) {
    return typeof e == "object" && e !== null && "__sentry_template_string__" in e && "__sentry_template_values__" in e;
  }
  function kn(e) {
    return e === null || fo(e) || typeof e != "object" && typeof e != "function";
  }
  function kt(e) {
    return yt(e, "Object");
  }
  function Dn(e) {
    return typeof Event < "u" && xe(e, Event);
  }
  function _g(e) {
    return typeof Element < "u" && xe(e, Element);
  }
  function Eg(e) {
    return yt(e, "RegExp");
  }
  function zt(e) {
    return !!(e?.then && typeof e.then == "function");
  }
  function bg(e) {
    return kt(e) && "nativeEvent" in e && "preventDefault" in e && "stopPropagation" in e;
  }
  function xe(e, t) {
    try {
      return e instanceof t;
    } catch {
      return !1;
    }
  }
  function nc(e) {
    return !!(typeof e == "object" && e !== null && (e.__isVue || e._isVue || e.__v_isVNode));
  }
  function rc(e) {
    return typeof Request < "u" && xe(e, Request);
  }
  const po = F, Sg = 80;
  function oc(e, t = {}) {
    if (!e)
      return "<unknown>";
    try {
      let n = e;
      const r = 5, o = [];
      let i = 0, s = 0;
      const a = " > ", c = a.length;
      let u;
      const l = Array.isArray(t) ? t : t.keyAttrs, f = !Array.isArray(t) && t.maxStringLength || Sg;
      for (; n && i++ < r && (u = wg(n, l), !(u === "html" || i > 1 && s + o.length * c + u.length >= f)); )
        o.push(u), s += u.length, n = n.parentNode;
      return o.reverse().join(a);
    } catch {
      return "<unknown>";
    }
  }
  function wg(e, t) {
    const n = e, r = [];
    if (!n?.tagName)
      return "";
    if (po.HTMLElement && n instanceof HTMLElement && n.dataset) {
      if (n.dataset.sentryComponent)
        return n.dataset.sentryComponent;
      if (n.dataset.sentryElement)
        return n.dataset.sentryElement;
    }
    r.push(n.tagName.toLowerCase());
    const o = t?.length ? t.filter((i) => n.getAttribute(i)).map((i) => [i, n.getAttribute(i)]) : null;
    if (o?.length)
      o.forEach((i) => {
        r.push(`[${i[0]}="${i[1]}"]`);
      });
    else {
      n.id && r.push(`#${n.id}`);
      const i = n.className;
      if (i && Te(i)) {
        const s = i.split(/\s+/);
        for (const a of s)
          r.push(`.${a}`);
      }
    }
    for (const i of ["aria-label", "type", "name", "title", "alt"]) {
      const s = n.getAttribute(i);
      s && r.push(`[${i}="${s}"]`);
    }
    return r.join("");
  }
  function ho() {
    try {
      return po.document.location.href;
    } catch {
      return "";
    }
  }
  function vg(e, t = 5) {
    if (!po.HTMLElement)
      return null;
    let n = e;
    for (let r = 0; r < t; r++) {
      if (!n)
        return null;
      if (n instanceof HTMLElement) {
        if (n.dataset.sentryComponent)
          return n.dataset.sentryComponent;
        if (n.dataset.sentryElement)
          return n.dataset.sentryElement;
      }
      n = n.parentNode;
    }
    return null;
  }
  function oe(e, t, n) {
    if (!(t in e))
      return;
    const r = e[t];
    if (typeof r != "function")
      return;
    const o = n(r);
    typeof o == "function" && ic(o, r);
    try {
      e[t] = o;
    } catch {
      M && O.log(`Failed to replace method "${t}" in object`, e);
    }
  }
  function ke(e, t, n) {
    try {
      Object.defineProperty(e, t, {
        // enumerable: false, // the default, so we can save on bundle size by not explicitly setting it
        value: n,
        writable: !0,
        configurable: !0
      });
    } catch {
      M && O.log(`Failed to add non-enumerable property "${t}" to object`, e);
    }
  }
  function ic(e, t) {
    try {
      const n = t.prototype || {};
      e.prototype = t.prototype = n, ke(e, "__sentry_original__", t);
    } catch {
    }
  }
  function mo(e) {
    return e.__sentry_original__;
  }
  function sc(e) {
    if (xn(e))
      return {
        message: e.message,
        name: e.name,
        stack: e.stack,
        ...Vi(e)
      };
    if (Dn(e)) {
      const t = {
        type: e.type,
        target: Wi(e.target),
        currentTarget: Wi(e.currentTarget),
        ...Vi(e)
      };
      return typeof CustomEvent < "u" && xe(e, CustomEvent) && (t.detail = e.detail), t;
    } else
      return e;
  }
  function Wi(e) {
    try {
      return _g(e) ? oc(e) : Object.prototype.toString.call(e);
    } catch {
      return "<unknown>";
    }
  }
  function Vi(e) {
    return typeof e == "object" && e !== null ? Object.fromEntries(Object.entries(e)) : {};
  }
  function Tg(e) {
    const t = Object.keys(sc(e));
    return t.sort(), t[0] ? t.join(", ") : "[object has no keys]";
  }
  let nt;
  function $n(e) {
    if (nt !== void 0)
      return nt ? nt(e) : e();
    const t = /* @__PURE__ */ Symbol.for("__SENTRY_SAFE_RANDOM_ID_WRAPPER__"), n = F;
    return t in n && typeof n[t] == "function" ? (nt = n[t], nt(e)) : (nt = null, e());
  }
  function Sn() {
    return $n(() => Math.random());
  }
  function Fn() {
    return $n(() => Date.now());
  }
  function Or(e, t = 0) {
    return typeof e != "string" || t === 0 || e.length <= t ? e : `${e.slice(0, t)}...`;
  }
  function qi(e, t) {
    if (!Array.isArray(e))
      return "";
    const n = [];
    for (let r = 0; r < e.length; r++) {
      const o = e[r];
      try {
        nc(o) ? n.push(Qa(o)) : n.push(String(o));
      } catch {
        n.push("[value cannot be serialized]");
      }
    }
    return n.join(t);
  }
  function hn(e, t, n = !1) {
    return Te(e) ? Eg(t) ? t.test(e) : Te(t) ? n ? e === t : e.includes(t) : typeof t == "function" ? t(e) : !1 : !1;
  }
  function jn(e, t = [], n = !1) {
    return t.some((r) => hn(e, r, n));
  }
  function Cg() {
    const e = F;
    return e.crypto || e.msCrypto;
  }
  let ur;
  function Rg() {
    return Sn() * 16;
  }
  function le(e = Cg()) {
    try {
      if (e?.randomUUID)
        return $n(() => e.randomUUID()).replace(/-/g, "");
    } catch {
    }
    return ur || (ur = "10000000100040008000" + 1e11), ur.replace(
      /[018]/g,
      (t) => (
        // eslint-disable-next-line no-bitwise
        (t ^ (Rg() & 15) >> t / 4).toString(16)
      )
    );
  }
  function ac(e) {
    return e.exception?.values?.[0];
  }
  function Be(e) {
    const { message: t, event_id: n } = e;
    if (t)
      return t;
    const r = ac(e);
    return r ? r.type && r.value ? `${r.type}: ${r.value}` : r.type || r.value || n || "<unknown>" : n || "<unknown>";
  }
  function Mr(e, t, n) {
    const r = e.exception = e.exception || {}, o = r.values = r.values || [], i = o[0] = o[0] || {};
    i.value || (i.value = t || ""), i.type || (i.type = "Error");
  }
  function ct(e, t) {
    const n = ac(e);
    if (!n)
      return;
    const r = { type: "generic", handled: !0 }, o = n.mechanism;
    if (n.mechanism = { ...r, ...o, ...t }, t && "data" in t) {
      const i = { ...o?.data, ...t.data };
      n.mechanism.data = i;
    }
  }
  function Gi(e) {
    if (Ig(e))
      return !0;
    try {
      ke(e, "__sentry_captured__", !0);
    } catch {
    }
    return !1;
  }
  function Ig(e) {
    try {
      return e.__sentry_captured__;
    } catch {
    }
  }
  const cc = 1e3;
  function Wt() {
    return Fn() / cc;
  }
  function Og() {
    const { performance: e } = F;
    if (!e?.now || !e.timeOrigin)
      return Wt;
    const t = e.timeOrigin;
    return () => (t + $n(() => e.now())) / cc;
  }
  let Ji;
  function Ce() {
    return (Ji ?? (Ji = Og()))();
  }
  function Mg(e) {
    const t = Ce(), n = {
      sid: le(),
      init: !0,
      timestamp: t,
      started: t,
      duration: 0,
      status: "ok",
      errors: 0,
      ignoreDuration: !1,
      toJSON: () => Ng(n)
    };
    return e && ut(n, e), n;
  }
  function ut(e, t = {}) {
    if (t.user && (!e.ipAddress && t.user.ip_address && (e.ipAddress = t.user.ip_address), !e.did && !t.did && (e.did = t.user.id || t.user.email || t.user.username)), e.timestamp = t.timestamp || Ce(), t.abnormal_mechanism && (e.abnormal_mechanism = t.abnormal_mechanism), t.ignoreDuration && (e.ignoreDuration = t.ignoreDuration), t.sid && (e.sid = t.sid.length === 32 ? t.sid : le()), t.init !== void 0 && (e.init = t.init), !e.did && t.did && (e.did = `${t.did}`), typeof t.started == "number" && (e.started = t.started), e.ignoreDuration)
      e.duration = void 0;
    else if (typeof t.duration == "number")
      e.duration = t.duration;
    else {
      const n = e.timestamp - e.started;
      e.duration = n >= 0 ? n : 0;
    }
    t.release && (e.release = t.release), t.environment && (e.environment = t.environment), !e.ipAddress && t.ipAddress && (e.ipAddress = t.ipAddress), !e.userAgent && t.userAgent && (e.userAgent = t.userAgent), typeof t.errors == "number" && (e.errors = t.errors), t.status && (e.status = t.status);
  }
  function Pg(e, t) {
    let n = {};
    e.status === "ok" && (n = { status: "exited" }), ut(e, n);
  }
  function Ng(e) {
    return {
      sid: `${e.sid}`,
      init: e.init,
      // Make sure that sec is converted to ms for date constructor
      started: new Date(e.started * 1e3).toISOString(),
      timestamp: new Date(e.timestamp * 1e3).toISOString(),
      status: e.status,
      errors: e.errors,
      did: typeof e.did == "number" || typeof e.did == "string" ? `${e.did}` : void 0,
      duration: e.duration,
      abnormal_mechanism: e.abnormal_mechanism,
      attrs: {
        release: e.release,
        environment: e.environment,
        ip_address: e.ipAddress,
        user_agent: e.userAgent
      }
    };
  }
  function Vt(e, t, n = 2) {
    if (!t || typeof t != "object" || n <= 0)
      return t;
    if (e && Object.keys(t).length === 0)
      return e;
    const r = { ...e };
    for (const o in t)
      Object.prototype.hasOwnProperty.call(t, o) && (r[o] = Vt(r[o], t[o], n - 1));
    return r;
  }
  function Ki() {
    return le();
  }
  function uc() {
    return le().substring(16);
  }
  const Pr = "_sentrySpan";
  function Zi(e, t) {
    t ? ke(e, Pr, t) : delete e[Pr];
  }
  function Yi(e) {
    return e[Pr];
  }
  const Lg = 100;
  class Re {
    /** Flag if notifying is happening. */
    /** Callback for client to receive scope changes. */
    /** Callback list that will be called during event processing. */
    /** Array of breadcrumbs. */
    /** User */
    /** Tags */
    /** Attributes */
    /** Extra */
    /** Contexts */
    /** Attachments */
    /** Propagation Context for distributed tracing */
    /**
     * A place to stash data which is needed at some point in the SDK's event processing pipeline but which shouldn't get
     * sent to Sentry
     */
    /** Fingerprint */
    /** Severity */
    /**
     * Transaction Name
     *
     * IMPORTANT: The transaction name on the scope has nothing to do with root spans/transaction objects.
     * It's purpose is to assign a transaction to the scope that's added to non-transaction events.
     */
    /** Session */
    /** The client on this scope */
    /** Contains the last event id of a captured event.  */
    /** Conversation ID */
    // NOTE: Any field which gets added here should get added not only to the constructor but also to the `clone` method.
    constructor() {
      this._notifyingListeners = !1, this._scopeListeners = [], this._eventProcessors = [], this._breadcrumbs = [], this._attachments = [], this._user = {}, this._tags = {}, this._attributes = {}, this._extra = {}, this._contexts = {}, this._sdkProcessingMetadata = {}, this._propagationContext = {
        traceId: Ki(),
        sampleRand: Sn()
      };
    }
    /**
     * Clone all data from this scope into a new scope.
     */
    clone() {
      const t = new Re();
      return t._breadcrumbs = [...this._breadcrumbs], t._tags = { ...this._tags }, t._attributes = { ...this._attributes }, t._extra = { ...this._extra }, t._contexts = { ...this._contexts }, this._contexts.flags && (t._contexts.flags = {
        values: [...this._contexts.flags.values]
      }), t._user = this._user, t._level = this._level, t._session = this._session, t._transactionName = this._transactionName, t._fingerprint = this._fingerprint, t._eventProcessors = [...this._eventProcessors], t._attachments = [...this._attachments], t._sdkProcessingMetadata = { ...this._sdkProcessingMetadata }, t._propagationContext = { ...this._propagationContext }, t._client = this._client, t._lastEventId = this._lastEventId, t._conversationId = this._conversationId, Zi(t, Yi(this)), t;
    }
    /**
     * Update the client assigned to this scope.
     * Note that not every scope will have a client assigned - isolation scopes & the global scope will generally not have a client,
     * as well as manually created scopes.
     */
    setClient(t) {
      this._client = t;
    }
    /**
     * Set the ID of the last captured error event.
     * This is generally only captured on the isolation scope.
     */
    setLastEventId(t) {
      this._lastEventId = t;
    }
    /**
     * Get the client assigned to this scope.
     */
    getClient() {
      return this._client;
    }
    /**
     * Get the ID of the last captured error event.
     * This is generally only available on the isolation scope.
     */
    lastEventId() {
      return this._lastEventId;
    }
    /**
     * @inheritDoc
     */
    addScopeListener(t) {
      this._scopeListeners.push(t);
    }
    /**
     * Add an event processor that will be called before an event is sent.
     */
    addEventProcessor(t) {
      return this._eventProcessors.push(t), this;
    }
    /**
     * Set the user for this scope.
     * Set to `null` to unset the user.
     */
    setUser(t) {
      return this._user = t || {
        email: void 0,
        id: void 0,
        ip_address: void 0,
        username: void 0
      }, this._session && ut(this._session, { user: t }), this._notifyScopeListeners(), this;
    }
    /**
     * Get the user from this scope.
     */
    getUser() {
      return this._user;
    }
    /**
     * Set the conversation ID for this scope.
     * Set to `null` to unset the conversation ID.
     */
    setConversationId(t) {
      return this._conversationId = t || void 0, this._notifyScopeListeners(), this;
    }
    /**
     * Set an object that will be merged into existing tags on the scope,
     * and will be sent as tags data with the event.
     */
    setTags(t) {
      return this._tags = {
        ...this._tags,
        ...t
      }, this._notifyScopeListeners(), this;
    }
    /**
     * Set a single tag that will be sent as tags data with the event.
     */
    setTag(t, n) {
      return this.setTags({ [t]: n });
    }
    /**
     * Sets attributes onto the scope.
     *
     * These attributes are currently applied to logs and metrics.
     * In the future, they will also be applied to spans.
     *
     * Important: For now, only strings, numbers and boolean attributes are supported, despite types allowing for
     * more complex attribute types. We'll add this support in the future but already specify the wider type to
     * avoid a breaking change in the future.
     *
     * @param newAttributes - The attributes to set on the scope. You can either pass in key-value pairs, or
     * an object with a `value` and an optional `unit` (if applicable to your attribute).
     *
     * @example
     * ```typescript
     * scope.setAttributes({
     *   is_admin: true,
     *   payment_selection: 'credit_card',
     *   render_duration: { value: 'render_duration', unit: 'ms' },
     * });
     * ```
     */
    setAttributes(t) {
      return this._attributes = {
        ...this._attributes,
        ...t
      }, this._notifyScopeListeners(), this;
    }
    /**
     * Sets an attribute onto the scope.
     *
     * These attributes are currently applied to logs and metrics.
     * In the future, they will also be applied to spans.
     *
     * Important: For now, only strings, numbers and boolean attributes are supported, despite types allowing for
     * more complex attribute types. We'll add this support in the future but already specify the wider type to
     * avoid a breaking change in the future.
     *
     * @param key - The attribute key.
     * @param value - the attribute value. You can either pass in a raw value, or an attribute
     * object with a `value` and an optional `unit` (if applicable to your attribute).
     *
     * @example
     * ```typescript
     * scope.setAttribute('is_admin', true);
     * scope.setAttribute('render_duration', { value: 'render_duration', unit: 'ms' });
     * ```
     */
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    setAttribute(t, n) {
      return this.setAttributes({ [t]: n });
    }
    /**
     * Removes the attribute with the given key from the scope.
     *
     * @param key - The attribute key.
     *
     * @example
     * ```typescript
     * scope.removeAttribute('is_admin');
     * ```
     */
    removeAttribute(t) {
      return t in this._attributes && (delete this._attributes[t], this._notifyScopeListeners()), this;
    }
    /**
     * Set an object that will be merged into existing extra on the scope,
     * and will be sent as extra data with the event.
     */
    setExtras(t) {
      return this._extra = {
        ...this._extra,
        ...t
      }, this._notifyScopeListeners(), this;
    }
    /**
     * Set a single key:value extra entry that will be sent as extra data with the event.
     */
    setExtra(t, n) {
      return this._extra = { ...this._extra, [t]: n }, this._notifyScopeListeners(), this;
    }
    /**
     * Sets the fingerprint on the scope to send with the events.
     * @param {string[]} fingerprint Fingerprint to group events in Sentry.
     */
    setFingerprint(t) {
      return this._fingerprint = t, this._notifyScopeListeners(), this;
    }
    /**
     * Sets the level on the scope for future events.
     */
    setLevel(t) {
      return this._level = t, this._notifyScopeListeners(), this;
    }
    /**
     * Sets the transaction name on the scope so that the name of e.g. taken server route or
     * the page location is attached to future events.
     *
     * IMPORTANT: Calling this function does NOT change the name of the currently active
     * root span. If you want to change the name of the active root span, use
     * `Sentry.updateSpanName(rootSpan, 'new name')` instead.
     *
     * By default, the SDK updates the scope's transaction name automatically on sensible
     * occasions, such as a page navigation or when handling a new request on the server.
     */
    setTransactionName(t) {
      return this._transactionName = t, this._notifyScopeListeners(), this;
    }
    /**
     * Sets context data with the given name.
     * Data passed as context will be normalized. You can also pass `null` to unset the context.
     * Note that context data will not be merged - calling `setContext` will overwrite an existing context with the same key.
     */
    setContext(t, n) {
      return n === null ? delete this._contexts[t] : this._contexts[t] = n, this._notifyScopeListeners(), this;
    }
    /**
     * Set the session for the scope.
     */
    setSession(t) {
      return t ? this._session = t : delete this._session, this._notifyScopeListeners(), this;
    }
    /**
     * Get the session from the scope.
     */
    getSession() {
      return this._session;
    }
    /**
     * Updates the scope with provided data. Can work in three variations:
     * - plain object containing updatable attributes
     * - Scope instance that'll extract the attributes from
     * - callback function that'll receive the current scope as an argument and allow for modifications
     */
    update(t) {
      if (!t)
        return this;
      const n = typeof t == "function" ? t(this) : t, r = n instanceof Re ? n.getScopeData() : kt(n) ? t : void 0, {
        tags: o,
        attributes: i,
        extra: s,
        user: a,
        contexts: c,
        level: u,
        fingerprint: l = [],
        propagationContext: f,
        conversationId: p
      } = r || {};
      return this._tags = { ...this._tags, ...o }, this._attributes = { ...this._attributes, ...i }, this._extra = { ...this._extra, ...s }, this._contexts = { ...this._contexts, ...c }, a && Object.keys(a).length && (this._user = a), u && (this._level = u), l.length && (this._fingerprint = l), f && (this._propagationContext = f), p && (this._conversationId = p), this;
    }
    /**
     * Clears the current scope and resets its properties.
     * Note: The client will not be cleared.
     */
    clear() {
      return this._breadcrumbs = [], this._tags = {}, this._attributes = {}, this._extra = {}, this._user = {}, this._contexts = {}, this._level = void 0, this._transactionName = void 0, this._fingerprint = void 0, this._session = void 0, this._conversationId = void 0, Zi(this, void 0), this._attachments = [], this.setPropagationContext({
        traceId: Ki(),
        sampleRand: Sn()
      }), this._notifyScopeListeners(), this;
    }
    /**
     * Adds a breadcrumb to the scope.
     * By default, the last 100 breadcrumbs are kept.
     */
    addBreadcrumb(t, n) {
      const r = typeof n == "number" ? n : Lg;
      if (r <= 0)
        return this;
      const o = {
        timestamp: Wt(),
        ...t,
        // Breadcrumb messages can theoretically be infinitely large and they're held in memory so we truncate them not to leak (too much) memory
        message: t.message ? Or(t.message, 2048) : t.message
      };
      return this._breadcrumbs.push(o), this._breadcrumbs.length > r && (this._breadcrumbs = this._breadcrumbs.slice(-r), this._client?.recordDroppedEvent("buffer_overflow", "log_item")), this._notifyScopeListeners(), this;
    }
    /**
     * Get the last breadcrumb of the scope.
     */
    getLastBreadcrumb() {
      return this._breadcrumbs[this._breadcrumbs.length - 1];
    }
    /**
     * Clear all breadcrumbs from the scope.
     */
    clearBreadcrumbs() {
      return this._breadcrumbs = [], this._notifyScopeListeners(), this;
    }
    /**
     * Add an attachment to the scope.
     */
    addAttachment(t) {
      return this._attachments.push(t), this;
    }
    /**
     * Clear all attachments from the scope.
     */
    clearAttachments() {
      return this._attachments = [], this;
    }
    /**
     * Get the data of this scope, which should be applied to an event during processing.
     */
    getScopeData() {
      return {
        breadcrumbs: this._breadcrumbs,
        attachments: this._attachments,
        contexts: this._contexts,
        tags: this._tags,
        attributes: this._attributes,
        extra: this._extra,
        user: this._user,
        level: this._level,
        fingerprint: this._fingerprint || [],
        eventProcessors: this._eventProcessors,
        propagationContext: this._propagationContext,
        sdkProcessingMetadata: this._sdkProcessingMetadata,
        transactionName: this._transactionName,
        span: Yi(this),
        conversationId: this._conversationId
      };
    }
    /**
     * Add data which will be accessible during event processing but won't get sent to Sentry.
     */
    setSDKProcessingMetadata(t) {
      return this._sdkProcessingMetadata = Vt(this._sdkProcessingMetadata, t, 2), this;
    }
    /**
     * Add propagation context to the scope, used for distributed tracing
     */
    setPropagationContext(t) {
      return this._propagationContext = t, this;
    }
    /**
     * Get propagation context from the scope, used for distributed tracing
     */
    getPropagationContext() {
      return this._propagationContext;
    }
    /**
     * Capture an exception for this scope.
     *
     * @returns {string} The id of the captured Sentry event.
     */
    captureException(t, n) {
      const r = n?.event_id || le();
      if (!this._client)
        return M && O.warn("No client configured on scope - will not capture exception!"), r;
      const o = new Error("Sentry syntheticException");
      return this._client.captureException(
        t,
        {
          originalException: t,
          syntheticException: o,
          ...n,
          event_id: r
        },
        this
      ), r;
    }
    /**
     * Capture a message for this scope.
     *
     * @returns {string} The id of the captured message.
     */
    captureMessage(t, n, r) {
      const o = r?.event_id || le();
      if (!this._client)
        return M && O.warn("No client configured on scope - will not capture message!"), o;
      const i = r?.syntheticException ?? new Error(t);
      return this._client.captureMessage(
        t,
        n,
        {
          originalException: t,
          syntheticException: i,
          ...r,
          event_id: o
        },
        this
      ), o;
    }
    /**
     * Capture a Sentry event for this scope.
     *
     * @returns {string} The id of the captured event.
     */
    captureEvent(t, n) {
      const r = t.event_id || n?.event_id || le();
      return this._client ? (this._client.captureEvent(t, { ...n, event_id: r }, this), r) : (M && O.warn("No client configured on scope - will not capture event!"), r);
    }
    /**
     * This will be called on every set call.
     */
    _notifyScopeListeners() {
      this._notifyingListeners || (this._notifyingListeners = !0, this._scopeListeners.forEach((t) => {
        t(this);
      }), this._notifyingListeners = !1);
    }
  }
  function xg() {
    return mt("defaultCurrentScope", () => new Re());
  }
  function kg() {
    return mt("defaultIsolationScope", () => new Re());
  }
  const Xi = (e) => e instanceof Promise && !e[lc], lc = /* @__PURE__ */ Symbol("chained PromiseLike"), Dg = (e, t, n) => {
    const r = e.then(
      (o) => (t(o), o),
      (o) => {
        throw n(o), o;
      }
    );
    return Xi(r) && Xi(e) ? r : $g(e, r);
  }, $g = (e, t) => {
    let n = !1;
    for (const r in e) {
      if (r in t) continue;
      n = !0;
      const o = e[r];
      typeof o == "function" ? Object.defineProperty(t, r, {
        value: (...i) => o.apply(e, i),
        enumerable: !0,
        configurable: !0,
        writable: !0
      }) : t[r] = o;
    }
    return n && Object.assign(t, { [lc]: !0 }), t;
  };
  class Fg {
    constructor(t, n) {
      let r;
      t ? r = t : r = new Re();
      let o;
      n ? o = n : o = new Re(), this._stack = [{ scope: r }], this._isolationScope = o;
    }
    /**
     * Fork a scope for the stack.
     */
    withScope(t) {
      const n = this._pushScope();
      let r;
      try {
        r = t(n);
      } catch (o) {
        throw this._popScope(), o;
      }
      return zt(r) ? Dg(
        r,
        () => this._popScope(),
        () => this._popScope()
      ) : (this._popScope(), r);
    }
    /**
     * Get the client of the stack.
     */
    getClient() {
      return this.getStackTop().client;
    }
    /**
     * Returns the scope of the top stack.
     */
    getScope() {
      return this.getStackTop().scope;
    }
    /**
     * Get the isolation scope for the stack.
     */
    getIsolationScope() {
      return this._isolationScope;
    }
    /**
     * Returns the topmost scope layer in the order domain > local > process.
     */
    getStackTop() {
      return this._stack[this._stack.length - 1];
    }
    /**
     * Push a scope to the stack.
     */
    _pushScope() {
      const t = this.getScope().clone();
      return this._stack.push({
        client: this.getClient(),
        scope: t
      }), t;
    }
    /**
     * Pop a scope from the stack.
     */
    _popScope() {
      return this._stack.length <= 1 ? !1 : !!this._stack.pop();
    }
  }
  function lt() {
    const e = Nn(), t = Ln(e);
    return t.stack = t.stack || new Fg(xg(), kg());
  }
  function jg(e) {
    return lt().withScope(e);
  }
  function Bg(e, t) {
    const n = lt();
    return n.withScope(() => (n.getStackTop().scope = e, t(e)));
  }
  function Qi(e) {
    return lt().withScope(() => e(lt().getIsolationScope()));
  }
  function Ug() {
    return {
      withIsolationScope: Qi,
      withScope: jg,
      withSetScope: Bg,
      withSetIsolationScope: (e, t) => Qi(t),
      getCurrentScope: () => lt().getScope(),
      getIsolationScope: () => lt().getIsolationScope()
    };
  }
  function go(e) {
    const t = Ln(e);
    return t.acs ? t.acs : Ug();
  }
  function Oe() {
    const e = Nn();
    return go(e).getCurrentScope();
  }
  function et() {
    const e = Nn();
    return go(e).getIsolationScope();
  }
  function Hg() {
    return mt("globalScope", () => new Re());
  }
  function zg(...e) {
    const t = Nn(), n = go(t);
    if (e.length === 2) {
      const [r, o] = e;
      return r ? n.withSetScope(r, o) : n.withScope(o);
    }
    return n.withScope(e[0]);
  }
  function X() {
    return Oe().getClient();
  }
  function Wg(e) {
    const t = e.getPropagationContext(), { traceId: n, parentSpanId: r, propagationSpanId: o } = t, i = {
      trace_id: n,
      span_id: o || uc()
    };
    return r && (i.parent_span_id = r), i;
  }
  const Vg = "sentry.source", qg = "sentry.sample_rate", Gg = "sentry.previous_trace_sample_rate", Jg = "sentry.op", Kg = "sentry.origin", fc = "sentry.profile_id", dc = "sentry.exclusive_time", Zg = "gen_ai.conversation.id", Yg = 0, Xg = 1;
  function Qg(e) {
    if (e) {
      if (typeof e == "object" && "deref" in e && typeof e.deref == "function")
        try {
          return e.deref();
        } catch {
          return;
        }
      return e;
    }
  }
  const ey = "_sentryScope", ty = "_sentryIsolationScope";
  function pc(e) {
    const t = e;
    return {
      scope: t[ey],
      isolationScope: Qg(t[ty])
    };
  }
  const es = "sentry-";
  function ny(e) {
    const t = ry(e);
    if (!t)
      return;
    const n = Object.entries(t).reduce((r, [o, i]) => {
      if (o.startsWith(es)) {
        const s = o.slice(es.length);
        r[s] = i;
      }
      return r;
    }, {});
    if (Object.keys(n).length > 0)
      return n;
  }
  function ry(e) {
    if (!(!e || !Te(e) && !Array.isArray(e)))
      return Array.isArray(e) ? e.reduce((t, n) => {
        const r = ts(n);
        return Object.entries(r).forEach(([o, i]) => {
          t[o] = i;
        }), t;
      }, {}) : ts(e);
  }
  function ts(e) {
    return e.split(",").map((t) => {
      const n = t.indexOf("=");
      if (n === -1)
        return [];
      const r = t.slice(0, n), o = t.slice(n + 1);
      return [r, o].map((i) => {
        try {
          return decodeURIComponent(i.trim());
        } catch {
          return;
        }
      });
    }).reduce((t, [n, r]) => (n && r && (t[n] = r), t), {});
  }
  const oy = /^o(\d+)\./, iy = /^(?:(\w+):)\/\/(?:(\w+)(?::(\w+)?)?@)((?:\[[:.%\w]+\]|[\w.-]+))(?::(\d+))?\/(.+)/;
  function sy(e) {
    return e === "http" || e === "https";
  }
  function qt(e, t = !1) {
    const { host: n, path: r, pass: o, port: i, projectId: s, protocol: a, publicKey: c } = e;
    return `${a}://${c}${t && o ? `:${o}` : ""}@${n}${i ? `:${i}` : ""}/${r && `${r}/`}${s}`;
  }
  function ay(e) {
    const t = iy.exec(e);
    if (!t) {
      gt(() => {
        console.error(`Invalid Sentry Dsn: ${e}`);
      });
      return;
    }
    const [n, r, o = "", i = "", s = "", a = ""] = t.slice(1);
    let c = "", u = a;
    const l = u.split("/");
    if (l.length > 1 && (c = l.slice(0, -1).join("/"), u = l.pop()), u) {
      const f = u.match(/^\d+/);
      f && (u = f[0]);
    }
    return hc({ host: i, pass: o, path: c, projectId: u, port: s, protocol: n, publicKey: r });
  }
  function hc(e) {
    return {
      protocol: e.protocol,
      publicKey: e.publicKey || "",
      pass: e.pass || "",
      host: e.host,
      port: e.port || "",
      path: e.path || "",
      projectId: e.projectId
    };
  }
  function cy(e) {
    if (!M)
      return !0;
    const { port: t, projectId: n, protocol: r } = e;
    return ["protocol", "publicKey", "host", "projectId"].find((s) => e[s] ? !1 : (O.error(`Invalid Sentry Dsn: ${s} missing`), !0)) ? !1 : n.match(/^\d+$/) ? sy(r) ? t && isNaN(parseInt(t, 10)) ? (O.error(`Invalid Sentry Dsn: Invalid port ${t}`), !1) : !0 : (O.error(`Invalid Sentry Dsn: Invalid protocol ${r}`), !1) : (O.error(`Invalid Sentry Dsn: Invalid projectId ${n}`), !1);
  }
  function uy(e) {
    return e.match(oy)?.[1];
  }
  function ly(e) {
    const t = e.getOptions(), { host: n } = e.getDsn() || {};
    let r;
    return t.orgId ? r = String(t.orgId) : n && (r = uy(n)), r;
  }
  function fy(e) {
    const t = typeof e == "string" ? ay(e) : hc(e);
    if (!(!t || !cy(t)))
      return t;
  }
  function dy(e) {
    if (typeof e == "boolean")
      return Number(e);
    const t = typeof e == "string" ? parseFloat(e) : e;
    if (!(typeof t != "number" || isNaN(t) || t < 0 || t > 1))
      return t;
  }
  const mc = 1;
  let ns = !1;
  function py(e) {
    const { spanId: t, traceId: n, isRemote: r } = e.spanContext(), o = r ? t : Bn(e).parent_span_id, i = pc(e).scope, s = r ? i?.getPropagationContext().propagationSpanId || uc() : t;
    return {
      parent_span_id: o,
      span_id: s,
      trace_id: n
    };
  }
  function hy(e) {
    if (e && e.length > 0)
      return e.map(({ context: { spanId: t, traceId: n, traceFlags: r, ...o }, attributes: i }) => ({
        span_id: t,
        trace_id: n,
        sampled: r === mc,
        attributes: i,
        ...o
      }));
  }
  function rs(e) {
    return typeof e == "number" ? os(e) : Array.isArray(e) ? e[0] + e[1] / 1e9 : e instanceof Date ? os(e.getTime()) : Ce();
  }
  function os(e) {
    return e > 9999999999 ? e / 1e3 : e;
  }
  function Bn(e) {
    if (yy(e))
      return e.getSpanJSON();
    const { spanId: t, traceId: n } = e.spanContext();
    if (gy(e)) {
      const { attributes: r, startTime: o, name: i, endTime: s, status: a, links: c } = e;
      return {
        span_id: t,
        trace_id: n,
        data: r,
        description: i,
        parent_span_id: my(e),
        start_timestamp: rs(o),
        // This is [0,0] by default in OTEL, in which case we want to interpret this as no end time
        timestamp: rs(s) || void 0,
        status: _y(a),
        op: r[Jg],
        origin: r[Kg],
        links: hy(c)
      };
    }
    return {
      span_id: t,
      trace_id: n,
      start_timestamp: 0,
      data: {}
    };
  }
  function my(e) {
    return "parentSpanId" in e ? e.parentSpanId : "parentSpanContext" in e ? e.parentSpanContext?.spanId : void 0;
  }
  function gy(e) {
    const t = e;
    return !!t.attributes && !!t.startTime && !!t.name && !!t.endTime && !!t.status;
  }
  function yy(e) {
    return typeof e.getSpanJSON == "function";
  }
  function Ay(e) {
    const { traceFlags: t } = e.spanContext();
    return t === mc;
  }
  function _y(e) {
    if (!(!e || e.code === Yg))
      return e.code === Xg ? "ok" : e.message || "internal_error";
  }
  const Ey = "_sentryRootSpan", gc = by;
  function by(e) {
    return e[Ey] || e;
  }
  function is() {
    ns || (gt(() => {
      console.warn(
        "[Sentry] Returning null from `beforeSendSpan` is disallowed. To drop certain spans, configure the respective integrations directly or use `ignoreSpans`."
      );
    }), ns = !0);
  }
  function Sy(e) {
    if (typeof __SENTRY_TRACING__ == "boolean" && !__SENTRY_TRACING__)
      return !1;
    const t = X()?.getOptions();
    return !!t && // Note: This check is `!= null`, meaning "nullish". `0` is not "nullish", `undefined` and `null` are. (This comment was brought to you by 15 minutes of questioning life)
    (t.tracesSampleRate != null || !!t.tracesSampler);
  }
  function ss(e) {
    O.log(`Ignoring span ${e.op} - ${e.description} because it matches \`ignoreSpans\`.`);
  }
  function as(e, t) {
    if (!t?.length || !e.description)
      return !1;
    for (const n of t) {
      if (vy(n)) {
        if (hn(e.description, n))
          return M && ss(e), !0;
        continue;
      }
      if (!n.name && !n.op)
        continue;
      const r = n.name ? hn(e.description, n.name) : !0, o = n.op ? e.op && hn(e.op, n.op) : !0;
      if (r && o)
        return M && ss(e), !0;
    }
    return !1;
  }
  function wy(e, t) {
    const n = t.parent_span_id, r = t.span_id;
    if (n)
      for (const o of e)
        o.parent_span_id === r && (o.parent_span_id = n);
  }
  function vy(e) {
    return typeof e == "string" || e instanceof RegExp;
  }
  const yo = "production", Ty = "_frozenDsc";
  function yc(e, t) {
    const n = t.getOptions(), { publicKey: r } = t.getDsn() || {}, o = {
      environment: n.environment || yo,
      release: n.release,
      public_key: r,
      trace_id: e,
      org_id: ly(t)
    };
    return t.emit("createDsc", o), o;
  }
  function Cy(e, t) {
    const n = t.getPropagationContext();
    return n.dsc || yc(n.traceId, e);
  }
  function Ry(e) {
    const t = X();
    if (!t)
      return {};
    const n = gc(e), r = Bn(n), o = r.data, i = n.spanContext().traceState, s = i?.get("sentry.sample_rate") ?? o[qg] ?? o[Gg];
    function a(y) {
      return (typeof s == "number" || typeof s == "string") && (y.sample_rate = `${s}`), y;
    }
    const c = n[Ty];
    if (c)
      return a(c);
    const u = i?.get("sentry.dsc"), l = u && ny(u);
    if (l)
      return a(l);
    const f = yc(e.spanContext().traceId, t), p = o[Vg] ?? o["sentry.span.source"], d = r.description;
    return p !== "url" && d && (f.transaction = d), Sy() && (f.sampled = String(Ay(n)), f.sample_rand = // In OTEL we store the sample rand on the trace state because we cannot access scopes for NonRecordingSpans
    // The Sentry OTEL SpanSampler takes care of writing the sample rand on the root span
    i?.get("sentry.sample_rand") ?? // On all other platforms we can actually get the scopes from a root span (we use this as a fallback)
    pc(n).scope?.getPropagationContext().sampleRand.toString()), a(f), t.emit("createDsc", f, n), f;
  }
  function Iy(e) {
    return !!e && typeof e == "function" && "_streamed" in e && !!e._streamed;
  }
  function we(e, t = 100, n = 1 / 0) {
    try {
      return Nr("", e, t, n);
    } catch (r) {
      return { ERROR: `**non-serializable** (${r})` };
    }
  }
  function Ac(e, t = 3, n = 100 * 1024) {
    const r = we(e, t);
    return Ny(r) > n ? Ac(e, t - 1, n) : r;
  }
  function Nr(e, t, n = 1 / 0, r = 1 / 0, o = Ly()) {
    const [i, s] = o;
    if (t == null || // this matches null and undefined -> eqeq not eqeqeq
    ["boolean", "string"].includes(typeof t) || typeof t == "number" && Number.isFinite(t))
      return t;
    const a = Oy(e, t);
    if (!a.startsWith("[object "))
      return a;
    if (t.__sentry_skip_normalization__)
      return t;
    const c = typeof t.__sentry_override_normalization_depth__ == "number" ? t.__sentry_override_normalization_depth__ : n;
    if (c === 0)
      return a.replace("object ", "");
    if (i(t))
      return "[Circular ~]";
    const u = t;
    if (u && typeof u.toJSON == "function")
      try {
        const d = u.toJSON();
        return Nr("", d, c - 1, r, o);
      } catch {
      }
    const l = Array.isArray(t) ? [] : {};
    let f = 0;
    const p = sc(t);
    for (const d in p) {
      if (!Object.prototype.hasOwnProperty.call(p, d))
        continue;
      if (f >= r) {
        l[d] = "[MaxProperties ~]";
        break;
      }
      const y = p[d];
      l[d] = Nr(d, y, c - 1, r, o), f++;
    }
    return s(t), l;
  }
  function Oy(e, t) {
    try {
      if (e === "domain" && t && typeof t == "object" && t._events)
        return "[Domain]";
      if (e === "domainEmitter")
        return "[DomainEmitter]";
      if (typeof global < "u" && t === global)
        return "[Global]";
      if (typeof window < "u" && t === window)
        return "[Window]";
      if (typeof document < "u" && t === document)
        return "[Document]";
      if (nc(t))
        return Qa(t);
      if (bg(t))
        return "[SyntheticEvent]";
      if (typeof t == "number" && !Number.isFinite(t))
        return `[${t}]`;
      if (typeof t == "function")
        return `[Function: ${Le(t)}]`;
      if (typeof t == "symbol")
        return `[${String(t)}]`;
      if (typeof t == "bigint")
        return `[BigInt: ${String(t)}]`;
      const n = My(t);
      return /^HTML(\w*)Element$/.test(n) ? `[HTMLElement: ${n}]` : `[object ${n}]`;
    } catch (n) {
      return `**non-serializable** (${n})`;
    }
  }
  function My(e) {
    const t = Object.getPrototypeOf(e);
    return t?.constructor ? t.constructor.name : "null prototype";
  }
  function Py(e) {
    return ~-encodeURI(e).split(/%..|./).length;
  }
  function Ny(e) {
    return Py(JSON.stringify(e));
  }
  function Ly() {
    const e = /* @__PURE__ */ new WeakSet();
    function t(r) {
      return e.has(r) ? !0 : (e.add(r), !1);
    }
    function n(r) {
      e.delete(r);
    }
    return [t, n];
  }
  function At(e, t = []) {
    return [e, t];
  }
  function xy(e, t) {
    const [n, r] = e;
    return [n, [...r, t]];
  }
  function Lr(e, t) {
    const n = e[1];
    for (const r of n) {
      const o = r[0].type;
      if (t(r, o))
        return !0;
    }
    return !1;
  }
  function ky(e, t) {
    return Lr(e, (n, r) => t.includes(r));
  }
  function xr(e) {
    const t = Ln(F);
    return t.encodePolyfill ? t.encodePolyfill(e) : new TextEncoder().encode(e);
  }
  function Dy(e) {
    const [t, n] = e;
    let r = JSON.stringify(t);
    function o(i) {
      typeof r == "string" ? r = typeof i == "string" ? r + i : [xr(r), i] : r.push(typeof i == "string" ? xr(i) : i);
    }
    for (const i of n) {
      const [s, a] = i;
      if (o(`
${JSON.stringify(s)}
`), typeof a == "string" || a instanceof Uint8Array)
        o(a);
      else {
        let c;
        try {
          c = JSON.stringify(a);
        } catch {
          c = JSON.stringify(we(a));
        }
        o(c);
      }
    }
    return typeof r == "string" ? r : $y(r);
  }
  function $y(e) {
    const t = e.reduce((o, i) => o + i.length, 0), n = new Uint8Array(t);
    let r = 0;
    for (const o of e)
      n.set(o, r), r += o.length;
    return n;
  }
  function Fy(e) {
    const t = typeof e.data == "string" ? xr(e.data) : e.data;
    return [
      {
        type: "attachment",
        length: t.length,
        filename: e.filename,
        content_type: e.contentType,
        attachment_type: e.attachmentType
      },
      t
    ];
  }
  const _c = {
    sessions: "session",
    event: "error",
    client_report: "internal",
    user_report: "default",
    profile_chunk: "profile",
    replay_event: "replay",
    replay_recording: "replay",
    check_in: "monitor",
    raw_security: "security",
    log: "log_item",
    trace_metric: "metric"
  };
  function jy(e) {
    return e in _c;
  }
  function cs(e) {
    return jy(e) ? _c[e] : e;
  }
  function Ec(e) {
    if (!e?.sdk)
      return;
    const { name: t, version: n } = e.sdk;
    return { name: t, version: n };
  }
  function By(e, t, n, r) {
    const o = e.sdkProcessingMetadata?.dynamicSamplingContext;
    return {
      event_id: e.event_id,
      sent_at: (/* @__PURE__ */ new Date()).toISOString(),
      ...t && { sdk: t },
      ...!!n && r && { dsn: qt(r) },
      ...o && {
        trace: o
      }
    };
  }
  function Uy(e, t) {
    if (!t)
      return e;
    const n = e.sdk || {};
    return e.sdk = {
      ...n,
      name: n.name || t.name,
      version: n.version || t.version,
      integrations: [...e.sdk?.integrations || [], ...t.integrations || []],
      packages: [...e.sdk?.packages || [], ...t.packages || []],
      settings: e.sdk?.settings || t.settings ? {
        ...e.sdk?.settings,
        ...t.settings
      } : void 0
    }, e;
  }
  function Hy(e, t, n, r) {
    const o = Ec(n), i = {
      sent_at: (/* @__PURE__ */ new Date()).toISOString(),
      ...o && { sdk: o },
      ...!!r && t && { dsn: qt(t) }
    }, s = "aggregates" in e ? [{ type: "sessions" }, e] : [{ type: "session" }, e.toJSON()];
    return At(i, [s]);
  }
  function zy(e, t, n, r) {
    const o = Ec(n), i = e.type && e.type !== "replay_event" ? e.type : "event";
    Uy(e, n?.sdk);
    const s = By(e, o, r, t);
    return delete e.sdkProcessingMetadata, At(s, [[{ type: i }, e]]);
  }
  function Wy(e, t) {
    const { fingerprint: n, span: r, breadcrumbs: o, sdkProcessingMetadata: i } = t;
    Vy(e, t), r && Jy(e, r), Ky(e, n), qy(e, o), Gy(e, i);
  }
  function us(e, t) {
    const {
      extra: n,
      tags: r,
      attributes: o,
      user: i,
      contexts: s,
      level: a,
      sdkProcessingMetadata: c,
      breadcrumbs: u,
      fingerprint: l,
      eventProcessors: f,
      attachments: p,
      propagationContext: d,
      transactionName: y,
      span: h
    } = t;
    It(e, "extra", n), It(e, "tags", r), It(e, "attributes", o), It(e, "user", i), It(e, "contexts", s), e.sdkProcessingMetadata = Vt(e.sdkProcessingMetadata, c, 2), a && (e.level = a), y && (e.transactionName = y), h && (e.span = h), u.length && (e.breadcrumbs = [...e.breadcrumbs, ...u]), l.length && (e.fingerprint = [...e.fingerprint, ...l]), f.length && (e.eventProcessors = [...e.eventProcessors, ...f]), p.length && (e.attachments = [...e.attachments, ...p]), e.propagationContext = { ...e.propagationContext, ...d };
  }
  function It(e, t, n) {
    e[t] = Vt(e[t], n, 1);
  }
  function bc(e, t) {
    const n = Hg().getScopeData();
    return e && us(n, e.getScopeData()), t && us(n, t.getScopeData()), n;
  }
  function Vy(e, t) {
    const { extra: n, tags: r, user: o, contexts: i, level: s, transactionName: a } = t;
    Object.keys(n).length && (e.extra = { ...n, ...e.extra }), Object.keys(r).length && (e.tags = { ...r, ...e.tags }), Object.keys(o).length && (e.user = { ...o, ...e.user }), Object.keys(i).length && (e.contexts = { ...i, ...e.contexts }), s && (e.level = s), a && e.type !== "transaction" && (e.transaction = a);
  }
  function qy(e, t) {
    const n = [...e.breadcrumbs || [], ...t];
    e.breadcrumbs = n.length ? n : void 0;
  }
  function Gy(e, t) {
    e.sdkProcessingMetadata = {
      ...e.sdkProcessingMetadata,
      ...t
    };
  }
  function Jy(e, t) {
    e.contexts = {
      trace: py(t),
      ...e.contexts
    }, e.sdkProcessingMetadata = {
      dynamicSamplingContext: Ry(t),
      ...e.sdkProcessingMetadata
    };
    const n = gc(t), r = Bn(n).description;
    r && !e.transaction && e.type === "transaction" && (e.transaction = r);
  }
  function Ky(e, t) {
    e.fingerprint = e.fingerprint ? Array.isArray(e.fingerprint) ? e.fingerprint : [e.fingerprint] : [], t && (e.fingerprint = e.fingerprint.concat(t)), e.fingerprint.length || delete e.fingerprint;
  }
  const lr = 0, ls = 1, fs = 2;
  function Gt(e) {
    return new Dt((t) => {
      t(e);
    });
  }
  function Ao(e) {
    return new Dt((t, n) => {
      n(e);
    });
  }
  class Dt {
    constructor(t) {
      this._state = lr, this._handlers = [], this._runExecutor(t);
    }
    /** @inheritdoc */
    then(t, n) {
      return new Dt((r, o) => {
        this._handlers.push([
          !1,
          (i) => {
            if (!t)
              r(i);
            else
              try {
                r(t(i));
              } catch (s) {
                o(s);
              }
          },
          (i) => {
            if (!n)
              o(i);
            else
              try {
                r(n(i));
              } catch (s) {
                o(s);
              }
          }
        ]), this._executeHandlers();
      });
    }
    /** @inheritdoc */
    catch(t) {
      return this.then((n) => n, t);
    }
    /** @inheritdoc */
    finally(t) {
      return new Dt((n, r) => {
        let o, i;
        return this.then(
          (s) => {
            i = !1, o = s, t && t();
          },
          (s) => {
            i = !0, o = s, t && t();
          }
        ).then(() => {
          if (i) {
            r(o);
            return;
          }
          n(o);
        });
      });
    }
    /** Excute the resolve/reject handlers. */
    _executeHandlers() {
      if (this._state === lr)
        return;
      const t = this._handlers.slice();
      this._handlers = [], t.forEach((n) => {
        n[0] || (this._state === ls && n[1](this._value), this._state === fs && n[2](this._value), n[0] = !0);
      });
    }
    /** Run the executor for the SyncPromise. */
    _runExecutor(t) {
      const n = (i, s) => {
        if (this._state === lr) {
          if (zt(s)) {
            s.then(r, o);
            return;
          }
          this._state = i, this._value = s, this._executeHandlers();
        }
      }, r = (i) => {
        n(ls, i);
      }, o = (i) => {
        n(fs, i);
      };
      try {
        t(r, o);
      } catch (i) {
        o(i);
      }
    }
  }
  function Zy(e, t, n, r = 0) {
    try {
      const o = kr(t, n, e, r);
      return zt(o) ? o : Gt(o);
    } catch (o) {
      return Ao(o);
    }
  }
  function kr(e, t, n, r) {
    const o = n[r];
    if (!e || !o)
      return e;
    const i = o({ ...e }, t);
    return M && i === null && O.log(`Event processor "${o.id || "?"}" dropped event`), zt(i) ? i.then((s) => kr(s, t, n, r + 1)) : kr(i, t, n, r + 1);
  }
  let je, ds, ps, Pe;
  function Yy(e) {
    const t = F._sentryDebugIds, n = F._debugIds;
    if (!t && !n)
      return {};
    const r = t ? Object.keys(t) : [], o = n ? Object.keys(n) : [];
    if (Pe && r.length === ds && o.length === ps)
      return Pe;
    ds = r.length, ps = o.length, Pe = {}, je || (je = {});
    const i = (s, a) => {
      for (const c of s) {
        const u = a[c], l = je?.[c];
        if (l && Pe && u)
          Pe[l[0]] = u, je && (je[c] = [l[0], u]);
        else if (u) {
          const f = e(c);
          for (let p = f.length - 1; p >= 0; p--) {
            const y = f[p]?.filename;
            if (y && Pe && je) {
              Pe[y] = u, je[c] = [y, u];
              break;
            }
          }
        }
      }
    };
    return t && i(r, t), n && i(o, n), Pe;
  }
  function Xy(e, t, n, r, o, i) {
    const { normalizeDepth: s = 3, normalizeMaxBreadth: a = 1e3 } = e, c = {
      ...t,
      event_id: t.event_id || n.event_id || le(),
      timestamp: t.timestamp || Wt()
    }, u = n.integrations || e.integrations.map((_) => _.name);
    Qy(c, e), nA(c, u), o && o.emit("applyFrameMetadata", t), t.type === void 0 && eA(c, e.stackParser);
    const l = oA(r, n.captureContext);
    n.mechanism && ct(c, n.mechanism);
    const f = o ? o.getEventProcessors() : [], p = bc(i, l), d = [...n.attachments || [], ...p.attachments];
    d.length && (n.attachments = d), Wy(c, p);
    const y = [
      ...f,
      // Run scope event processors _after_ all other processors
      ...p.eventProcessors
    ];
    return (n.data && n.data.__sentry__ === !0 ? Gt(c) : Zy(y, c, n)).then((_) => (_ && tA(_), typeof s == "number" && s > 0 ? rA(_, s, a) : _));
  }
  function Qy(e, t) {
    const { environment: n, release: r, dist: o, maxValueLength: i } = t;
    e.environment = e.environment || n || yo, !e.release && r && (e.release = r), !e.dist && o && (e.dist = o);
    const s = e.request;
    s?.url && i && (s.url = Or(s.url, i)), i && e.exception?.values?.forEach((a) => {
      a.value && (a.value = Or(a.value, i));
    });
  }
  function eA(e, t) {
    const n = Yy(t);
    e.exception?.values?.forEach((r) => {
      r.stacktrace?.frames?.forEach((o) => {
        o.filename && (o.debug_id = n[o.filename]);
      });
    });
  }
  function tA(e) {
    const t = {};
    if (e.exception?.values?.forEach((r) => {
      r.stacktrace?.frames?.forEach((o) => {
        o.debug_id && (o.abs_path ? t[o.abs_path] = o.debug_id : o.filename && (t[o.filename] = o.debug_id), delete o.debug_id);
      });
    }), Object.keys(t).length === 0)
      return;
    e.debug_meta = e.debug_meta || {}, e.debug_meta.images = e.debug_meta.images || [];
    const n = e.debug_meta.images;
    Object.entries(t).forEach(([r, o]) => {
      n.push({
        type: "sourcemap",
        code_file: r,
        debug_id: o
      });
    });
  }
  function nA(e, t) {
    t.length > 0 && (e.sdk = e.sdk || {}, e.sdk.integrations = [...e.sdk.integrations || [], ...t]);
  }
  function rA(e, t, n) {
    if (!e)
      return null;
    const r = {
      ...e,
      ...e.breadcrumbs && {
        breadcrumbs: e.breadcrumbs.map((o) => ({
          ...o,
          ...o.data && {
            data: we(o.data, t, n)
          }
        }))
      },
      ...e.user && {
        user: we(e.user, t, n)
      },
      ...e.contexts && {
        contexts: we(e.contexts, t, n)
      },
      ...e.extra && {
        extra: we(e.extra, t, n)
      }
    };
    return e.contexts?.trace && r.contexts && (r.contexts.trace = e.contexts.trace, e.contexts.trace.data && (r.contexts.trace.data = we(e.contexts.trace.data, t, n))), e.spans && (r.spans = e.spans.map((o) => ({
      ...o,
      ...o.data && {
        data: we(o.data, t, n)
      }
    }))), e.contexts?.flags && r.contexts && (r.contexts.flags = we(e.contexts.flags, 3, n)), r;
  }
  function oA(e, t) {
    if (!t)
      return e;
    const n = e ? e.clone() : new Re();
    return n.update(t), n;
  }
  function iA(e, t) {
    return Oe().captureException(e, void 0);
  }
  function Sc(e, t) {
    return Oe().captureEvent(e, t);
  }
  function hs(e) {
    const t = et(), { user: n } = bc(t, Oe()), { userAgent: r } = F.navigator || {}, o = Mg({
      user: n,
      ...r && { userAgent: r },
      ...e
    }), i = t.getSession();
    return i?.status === "ok" && ut(i, { status: "exited" }), wc(), t.setSession(o), o;
  }
  function wc() {
    const e = et(), n = Oe().getSession() || e.getSession();
    n && Pg(n), vc(), e.setSession();
  }
  function vc() {
    const e = et(), t = X(), n = e.getSession();
    n && t && t.captureSession(n);
  }
  function fr(e = !1) {
    if (e) {
      wc();
      return;
    }
    vc();
  }
  const sA = "7";
  function aA(e) {
    const t = e.protocol ? `${e.protocol}:` : "", n = e.port ? `:${e.port}` : "";
    return `${t}//${e.host}${n}${e.path ? `/${e.path}` : ""}/api/`;
  }
  function cA(e) {
    return `${aA(e)}${e.projectId}/envelope/`;
  }
  function uA(e, t) {
    const n = {
      sentry_version: sA
    };
    return e.publicKey && (n.sentry_key = e.publicKey), t && (n.sentry_client = `${t.name}/${t.version}`), new URLSearchParams(n).toString();
  }
  function lA(e, t, n) {
    return t || `${cA(e)}?${uA(e, n)}`;
  }
  const ms = [];
  function fA(e) {
    const t = {};
    return e.forEach((n) => {
      const { name: r } = n, o = t[r];
      o && !o.isDefaultInstance && n.isDefaultInstance || (t[r] = n);
    }), Object.values(t);
  }
  function dA(e) {
    const t = e.defaultIntegrations || [], n = e.integrations;
    t.forEach((o) => {
      o.isDefaultInstance = !0;
    });
    let r;
    if (Array.isArray(n))
      r = [...t, ...n];
    else if (typeof n == "function") {
      const o = n(t);
      r = Array.isArray(o) ? o : [o];
    } else
      r = t;
    return fA(r);
  }
  function pA(e, t) {
    const n = {};
    return t.forEach((r) => {
      r?.beforeSetup && r.beforeSetup(e);
    }), t.forEach((r) => {
      r && Tc(e, r, n);
    }), n;
  }
  function gs(e, t) {
    for (const n of t)
      n?.afterAllSetup && n.afterAllSetup(e);
  }
  function Tc(e, t, n) {
    if (n[t.name]) {
      M && O.log(`Integration skipped because it was already installed: ${t.name}`);
      return;
    }
    if (n[t.name] = t, !ms.includes(t.name) && typeof t.setupOnce == "function" && (t.setupOnce(), ms.push(t.name)), t.setup && typeof t.setup == "function" && t.setup(e), typeof t.preprocessEvent == "function") {
      const r = t.preprocessEvent.bind(t);
      e.on("preprocessEvent", (o, i) => r(o, i, e));
    }
    if (typeof t.processEvent == "function") {
      const r = t.processEvent.bind(t), o = Object.assign((i, s) => r(i, s, e), {
        id: t.name
      });
      e.addEventProcessor(o);
    }
    ["processSpan", "processSegmentSpan"].forEach((r) => {
      const o = t[r];
      typeof o == "function" && e.on(r, (i) => o.call(t, i, e));
    }), M && O.log(`Integration installed: ${t.name}`);
  }
  function hA(e) {
    return [
      {
        type: "log",
        item_count: e.length,
        content_type: "application/vnd.sentry.items.log+json"
      },
      {
        items: e
      }
    ];
  }
  function mA(e, t, n, r) {
    const o = {};
    return t?.sdk && (o.sdk = {
      name: t.sdk.name,
      version: t.sdk.version
    }), n && r && (o.dsn = qt(r)), At(o, [hA(e)]);
  }
  function Dr(e, t) {
    const n = t ?? gA(e) ?? [];
    if (n.length === 0)
      return;
    const r = e.getOptions(), o = mA(n, r._metadata, r.tunnel, e.getDsn());
    Cc().set(e, []), e.emit("flushLogs"), e.sendEnvelope(o);
  }
  function gA(e) {
    return Cc().get(e);
  }
  function Cc() {
    return mt("clientToLogBufferMap", () => /* @__PURE__ */ new WeakMap());
  }
  function yA(e) {
    return [
      {
        type: "trace_metric",
        item_count: e.length,
        content_type: "application/vnd.sentry.items.trace-metric+json"
      },
      {
        items: e
      }
    ];
  }
  function AA(e, t, n, r) {
    const o = {};
    return t?.sdk && (o.sdk = {
      name: t.sdk.name,
      version: t.sdk.version
    }), n && r && (o.dsn = qt(r)), At(o, [yA(e)]);
  }
  function Rc(e, t) {
    const n = t ?? _A(e) ?? [];
    if (n.length === 0)
      return;
    const r = e.getOptions(), o = AA(n, r._metadata, r.tunnel, e.getDsn());
    Ic().set(e, []), e.emit("flushMetrics"), e.sendEnvelope(o);
  }
  function _A(e) {
    return Ic().get(e);
  }
  function Ic() {
    return mt("clientToMetricBufferMap", () => /* @__PURE__ */ new WeakMap());
  }
  function Oc(e) {
    return typeof e == "object" && typeof e.unref == "function" && e.unref(), e;
  }
  const _o = /* @__PURE__ */ Symbol.for("SentryBufferFullError");
  function Eo(e = 100) {
    const t = /* @__PURE__ */ new Set();
    function n() {
      return t.size < e;
    }
    function r(s) {
      t.delete(s);
    }
    function o(s) {
      if (!n())
        return Ao(_o);
      const a = s();
      return t.add(a), a.then(
        () => r(a),
        () => r(a)
      ), a;
    }
    function i(s) {
      if (!t.size)
        return Gt(!0);
      const a = Promise.allSettled(Array.from(t)).then(() => !0);
      if (!s)
        return a;
      const c = [
        a,
        new Promise((u) => Oc(setTimeout(() => u(!1), s)))
      ];
      return Promise.race(c);
    }
    return {
      get $() {
        return Array.from(t);
      },
      add: o,
      drain: i
    };
  }
  const EA = 60 * 1e3;
  function bA(e, t = Fn()) {
    const n = parseInt(`${e}`, 10);
    if (!isNaN(n))
      return n * 1e3;
    const r = Date.parse(`${e}`);
    return isNaN(r) ? EA : r - t;
  }
  function SA(e, t) {
    return e[t] || e.all || 0;
  }
  function wA(e, t, n = Fn()) {
    return SA(e, t) > n;
  }
  function vA(e, { statusCode: t, headers: n }, r = Fn()) {
    const o = {
      ...e
    }, i = n?.["x-sentry-rate-limits"], s = n?.["retry-after"];
    if (i)
      for (const a of i.trim().split(",")) {
        const [c, u, , , l] = a.split(":", 5), f = parseInt(c, 10), p = (isNaN(f) ? 60 : f) * 1e3;
        if (!u)
          o.all = r + p;
        else
          for (const d of u.split(";"))
            d === "metric_bucket" ? (!l || l.split(";").includes("custom")) && (o[d] = r + p) : o[d] = r + p;
      }
    else s ? o.all = r + bA(s, r) : t === 429 && (o.all = r + 60 * 1e3);
    return o;
  }
  const Mc = 64;
  function TA(e, t, n = Eo(
    e.bufferSize || Mc
  )) {
    let r = {};
    const o = (s) => n.drain(s);
    function i(s) {
      const a = [];
      if (Lr(s, (f, p) => {
        const d = cs(p);
        wA(r, d) ? e.recordDroppedEvent("ratelimit_backoff", d) : a.push(f);
      }), a.length === 0)
        return Promise.resolve({});
      const c = At(s[0], a), u = (f) => {
        if (ky(c, ["client_report"])) {
          M && O.warn(`Dropping client report. Will not send outcomes (reason: ${f}).`);
          return;
        }
        Lr(c, (p, d) => {
          e.recordDroppedEvent(f, cs(d));
        });
      }, l = () => t({ body: Dy(c) }).then(
        (f) => f.statusCode === 413 ? (M && O.error(
          "Sentry responded with status code 413. Envelope was discarded due to exceeding size limits."
        ), u("send_error"), f) : (M && f.statusCode !== void 0 && (f.statusCode < 200 || f.statusCode >= 300) && O.warn(`Sentry responded with status code ${f.statusCode} to sent event.`), r = vA(r, f), f),
        (f) => {
          throw u("network_error"), M && O.error("Encountered error running transport request:", f), f;
        }
      );
      return n.add(l).then(
        (f) => f,
        (f) => {
          if (f === _o)
            return M && O.error("Skipped sending event because buffer is full."), u("queue_overflow"), Promise.resolve({});
          throw f;
        }
      );
    }
    return {
      send: i,
      flush: o
    };
  }
  function CA(e, t, n) {
    const r = [
      { type: "client_report" },
      {
        timestamp: Wt(),
        discarded_events: e
      }
    ];
    return At(t ? { dsn: t } : {}, [r]);
  }
  function Pc(e) {
    const t = [];
    e.message && t.push(e.message);
    try {
      const n = e.exception.values[e.exception.values.length - 1];
      n?.value && (t.push(n.value), n.type && t.push(`${n.type}: ${n.value}`));
    } catch {
    }
    return t;
  }
  function RA(e) {
    const { trace_id: t, parent_span_id: n, span_id: r, status: o, origin: i, data: s, op: a } = e.contexts?.trace ?? {};
    return {
      data: s ?? {},
      description: e.transaction,
      op: a,
      parent_span_id: n,
      span_id: r ?? "",
      start_timestamp: e.start_timestamp ?? 0,
      status: o,
      timestamp: e.timestamp,
      trace_id: t ?? "",
      origin: i,
      profile_id: s?.[fc],
      exclusive_time: s?.[dc],
      measurements: e.measurements,
      is_segment: !0
    };
  }
  function IA(e) {
    return {
      type: "transaction",
      timestamp: e.timestamp,
      start_timestamp: e.start_timestamp,
      transaction: e.description,
      contexts: {
        trace: {
          trace_id: e.trace_id,
          span_id: e.span_id,
          parent_span_id: e.parent_span_id,
          op: e.op,
          status: e.status,
          origin: e.origin,
          data: {
            ...e.data,
            ...e.profile_id && { [fc]: e.profile_id },
            ...e.exclusive_time && { [dc]: e.exclusive_time }
          }
        }
      },
      measurements: e.measurements
    };
  }
  const ys = "Not capturing exception because it's already been captured.", As = "Discarded session because of missing or non-string release", Nc = /* @__PURE__ */ Symbol.for("SentryInternalError"), Lc = /* @__PURE__ */ Symbol.for("SentryDoNotSendEventError"), OA = 5e3;
  function mn(e) {
    return {
      message: e,
      [Nc]: !0
    };
  }
  function dr(e) {
    return {
      message: e,
      [Lc]: !0
    };
  }
  function _s(e) {
    return !!e && typeof e == "object" && Nc in e;
  }
  function Es(e) {
    return !!e && typeof e == "object" && Lc in e;
  }
  function bs(e, t, n, r, o) {
    let i = 0, s, a = !1;
    e.on(n, () => {
      i = 0, clearTimeout(s), a = !1;
    }), e.on(t, (c) => {
      i += r(c), i >= 8e5 ? o(e) : a || (a = !0, s = Oc(
        setTimeout(() => {
          o(e);
        }, OA)
      ));
    }), e.on("flush", () => {
      o(e);
    });
  }
  class MA {
    /** Options passed to the SDK. */
    /** The client Dsn, if specified in options. Without this Dsn, the SDK will be disabled. */
    /** Array of set up integrations. */
    /** Number of calls being processed */
    /** Holds flushable  */
    // eslint-disable-next-line @typescript-eslint/ban-types
    /**
     * Initializes this client instance.
     *
     * @param options Options for the client.
     */
    constructor(t) {
      if (this._options = t, this._integrations = {}, this._numProcessing = 0, this._outcomes = {}, this._hooks = {}, this._eventProcessors = [], this._promiseBuffer = Eo(t.transportOptions?.bufferSize ?? Mc), t.dsn ? this._dsn = fy(t.dsn) : M && O.warn("No DSN provided, client will not send events."), this._dsn) {
        const r = lA(
          this._dsn,
          t.tunnel,
          t._metadata ? t._metadata.sdk : void 0
        );
        this._transport = t.transport({
          tunnel: this._options.tunnel,
          recordDroppedEvent: this.recordDroppedEvent.bind(this),
          ...t.transportOptions,
          url: r
        });
      }
      this._options.enableLogs = this._options.enableLogs ?? this._options._experiments?.enableLogs, this._options.enableLogs && bs(this, "afterCaptureLog", "flushLogs", xA, Dr), (this._options.enableMetrics ?? this._options._experiments?.enableMetrics ?? !0) && bs(
        this,
        "afterCaptureMetric",
        "flushMetrics",
        LA,
        Rc
      );
    }
    /**
     * Captures an exception event and sends it to Sentry.
     *
     * Unlike `captureException` exported from every SDK, this method requires that you pass it the current scope.
     */
    captureException(t, n, r) {
      const o = le();
      if (Gi(t))
        return M && O.log(ys), o;
      const i = {
        event_id: o,
        ...n
      };
      return this._process(
        () => this.eventFromException(t, i).then((s) => this._captureEvent(s, i, r)).then((s) => s),
        "error"
      ), i.event_id;
    }
    /**
     * Captures a message event and sends it to Sentry.
     *
     * Unlike `captureMessage` exported from every SDK, this method requires that you pass it the current scope.
     */
    captureMessage(t, n, r, o) {
      const i = {
        event_id: le(),
        ...r
      }, s = fo(t) ? t : String(t), a = kn(t), c = a ? this.eventFromMessage(s, n, i) : this.eventFromException(t, i);
      return this._process(
        () => c.then((u) => this._captureEvent(u, i, o)),
        a ? "unknown" : "error"
      ), i.event_id;
    }
    /**
     * Captures a manually created event and sends it to Sentry.
     *
     * Unlike `captureEvent` exported from every SDK, this method requires that you pass it the current scope.
     */
    captureEvent(t, n, r) {
      const o = le();
      if (n?.originalException && Gi(n.originalException))
        return M && O.log(ys), o;
      const i = {
        event_id: o,
        ...n
      }, s = t.sdkProcessingMetadata || {}, a = s.capturedSpanScope, c = s.capturedSpanIsolationScope, u = Ss(t.type);
      return this._process(
        () => this._captureEvent(t, i, a || r, c),
        u
      ), i.event_id;
    }
    /**
     * Captures a session.
     */
    captureSession(t) {
      this.sendSession(t), ut(t, { init: !1 });
    }
    /**
     * Create a cron monitor check in and send it to Sentry. This method is not available on all clients.
     *
     * @param checkIn An object that describes a check in.
     * @param upsertMonitorConfig An optional object that describes a monitor config. Use this if you want
     * to create a monitor automatically when sending a check in.
     * @param scope An optional scope containing event metadata.
     * @returns A string representing the id of the check in.
     */
    /**
     * Get the current Dsn.
     */
    getDsn() {
      return this._dsn;
    }
    /**
     * Get the current options.
     */
    getOptions() {
      return this._options;
    }
    /**
     * Get the SDK metadata.
     * @see SdkMetadata
     */
    getSdkMetadata() {
      return this._options._metadata;
    }
    /**
     * Returns the transport that is used by the client.
     * Please note that the transport gets lazy initialized so it will only be there once the first event has been sent.
     */
    getTransport() {
      return this._transport;
    }
    /**
     * Wait for all events to be sent or the timeout to expire, whichever comes first.
     *
     * @param timeout Maximum time in ms the client should wait for events to be flushed. Omitting this parameter will
     *   cause the client to wait until all events are sent before resolving the promise.
     * @returns A promise that will resolve with `true` if all events are sent before the timeout, or `false` if there are
     * still events in the queue when the timeout is reached.
     */
    // @ts-expect-error - PromiseLike is a subset of Promise
    async flush(t) {
      const n = this._transport;
      if (!n)
        return !0;
      this.emit("flush");
      const r = await this._isClientDoneProcessing(t), o = await n.flush(t);
      return r && o;
    }
    /**
     * Flush the event queue and set the client to `enabled = false`. See {@link Client.flush}.
     *
     * @param {number} timeout Maximum time in ms the client should wait before shutting down. Omitting this parameter will cause
     *   the client to wait until all events are sent before disabling itself.
     * @returns {Promise<boolean>} A promise which resolves to `true` if the flush completes successfully before the timeout, or `false` if
     * it doesn't.
     */
    // @ts-expect-error - PromiseLike is a subset of Promise
    async close(t) {
      Dr(this);
      const n = await this.flush(t);
      return this.getOptions().enabled = !1, this.emit("close"), n;
    }
    /**
     * Get all installed event processors.
     */
    getEventProcessors() {
      return this._eventProcessors;
    }
    /**
     * Adds an event processor that applies to any event processed by this client.
     */
    addEventProcessor(t) {
      this._eventProcessors.push(t);
    }
    /**
     * Initialize this client.
     * Call this after the client was set on a scope.
     */
    init() {
      (this._isEnabled() || // Force integrations to be setup even if no DSN was set when we have
      // Spotlight enabled. This is particularly important for browser as we
      // don't support the `spotlight` option there and rely on the users
      // adding the `spotlightBrowserIntegration()` to their integrations which
      // wouldn't get initialized with the check below when there's no DSN set.
      this._options.integrations.some(({ name: t }) => t.startsWith("Spotlight"))) && this._setupIntegrations();
    }
    /**
     * Gets an installed integration by its name.
     *
     * @returns {Integration|undefined} The installed integration or `undefined` if no integration with that `name` was installed.
     */
    getIntegrationByName(t) {
      return this._integrations[t];
    }
    /**
     * Add an integration to the client.
     * This can be used to e.g. lazy load integrations.
     * In most cases, this should not be necessary,
     * and you're better off just passing the integrations via `integrations: []` at initialization time.
     * However, if you find the need to conditionally load & add an integration, you can use `addIntegration` to do so.
     */
    addIntegration(t) {
      const n = this._integrations[t.name];
      !n && t.beforeSetup && t.beforeSetup(this), Tc(this, t, this._integrations), n || gs(this, [t]);
    }
    /**
     * Send a fully prepared event to Sentry.
     */
    sendEvent(t, n = {}) {
      this.emit("beforeSendEvent", t, n);
      let r = zy(t, this._dsn, this._options._metadata, this._options.tunnel);
      for (const o of n.attachments || [])
        r = xy(r, Fy(o));
      this.sendEnvelope(r).then((o) => this.emit("afterSendEvent", t, o));
    }
    /**
     * Send a session or session aggregrates to Sentry.
     */
    sendSession(t) {
      const { release: n, environment: r = yo } = this._options;
      if ("aggregates" in t) {
        const i = t.attrs || {};
        if (!i.release && !n) {
          M && O.warn(As);
          return;
        }
        i.release = i.release || n, i.environment = i.environment || r, t.attrs = i;
      } else {
        if (!t.release && !n) {
          M && O.warn(As);
          return;
        }
        t.release = t.release || n, t.environment = t.environment || r;
      }
      this.emit("beforeSendSession", t);
      const o = Hy(t, this._dsn, this._options._metadata, this._options.tunnel);
      this.sendEnvelope(o);
    }
    /**
     * Record on the client that an event got dropped (ie, an event that will not be sent to Sentry).
     */
    recordDroppedEvent(t, n, r = 1) {
      if (this._options.sendClientReports) {
        const o = `${t}:${n}`;
        M && O.log(`Recording outcome: "${o}"${r > 1 ? ` (${r} times)` : ""}`), this._outcomes[o] = (this._outcomes[o] || 0) + r;
      }
    }
    /* eslint-disable @typescript-eslint/unified-signatures */
    /**
     * Register a callback for whenever a span is started.
     * Receives the span as argument.
     * @returns {() => void} A function that, when executed, removes the registered callback.
     */
    /**
     * Register a hook on this client.
     */
    on(t, n) {
      const r = this._hooks[t] = this._hooks[t] || /* @__PURE__ */ new Set(), o = (...i) => n(...i);
      return r.add(o), () => {
        r.delete(o);
      };
    }
    /** Fire a hook whenever a span starts. */
    /**
     * Emit a hook that was previously registered via `on()`.
     */
    emit(t, ...n) {
      const r = this._hooks[t];
      r && r.forEach((o) => o(...n));
    }
    /**
     * Send an envelope to Sentry.
     */
    // @ts-expect-error - PromiseLike is a subset of Promise
    async sendEnvelope(t) {
      if (this.emit("beforeEnvelope", t), this._isEnabled() && this._transport)
        try {
          return await this._transport.send(t);
        } catch (n) {
          return M && O.error("Error while sending envelope:", n), {};
        }
      return M && O.error("Transport disabled"), {};
    }
    /**
     * Disposes of the client and releases all resources.
     *
     * Subclasses should override this method to clean up their own resources.
     * After calling dispose(), the client should not be used anymore.
     */
    dispose() {
    }
    /* eslint-enable @typescript-eslint/unified-signatures */
    /** Setup integrations for this client. */
    _setupIntegrations() {
      const { integrations: t } = this._options;
      this._integrations = pA(this, t), gs(this, t);
    }
    /** Updates existing session based on the provided event */
    _updateSessionFromEvent(t, n) {
      let r = n.level === "fatal", o = !1;
      const i = n.exception?.values;
      if (i) {
        o = !0, r = !1;
        for (const c of i)
          if (c.mechanism?.handled === !1) {
            r = !0;
            break;
          }
      }
      const s = t.status === "ok";
      (s && t.errors === 0 || s && r) && (ut(t, {
        ...r && { status: "crashed" },
        errors: t.errors || Number(o || r)
      }), this.captureSession(t));
    }
    /**
     * Determine if the client is finished processing. Returns a promise because it will wait `timeout` ms before saying
     * "no" (resolving to `false`) in order to give the client a chance to potentially finish first.
     *
     * @param timeout The time, in ms, after which to resolve to `false` if the client is still busy. Passing `0` (or not
     * passing anything) will make the promise wait as long as it takes for processing to finish before resolving to
     * `true`.
     * @returns A promise which will resolve to `true` if processing is already done or finishes before the timeout, and
     * `false` otherwise
     */
    async _isClientDoneProcessing(t) {
      let n = 0;
      for (; !t || n < t; ) {
        if (await new Promise((r) => setTimeout(r, 1)), !this._numProcessing)
          return !0;
        n++;
      }
      return !1;
    }
    /** Determines whether this SDK is enabled and a transport is present. */
    _isEnabled() {
      return this.getOptions().enabled !== !1 && this._transport !== void 0;
    }
    /**
     * Adds common information to events.
     *
     * The information includes release and environment from `options`,
     * breadcrumbs and context (extra, tags and user) from the scope.
     *
     * Information that is already present in the event is never overwritten. For
     * nested objects, such as the context, keys are merged.
     *
     * @param event The original event.
     * @param hint May contain additional information about the original exception.
     * @param currentScope A scope containing event metadata.
     * @returns A new event with more information.
     */
    _prepareEvent(t, n, r, o) {
      const i = this.getOptions(), s = Object.keys(this._integrations);
      return !n.integrations && s?.length && (n.integrations = s), this.emit("preprocessEvent", t, n), t.type || o.setLastEventId(t.event_id || n.event_id), Xy(i, t, n, r, this, o).then((a) => {
        if (a === null)
          return a;
        this.emit("postprocessEvent", a, n), a.contexts = {
          trace: { ...a.contexts?.trace, ...Wg(r) },
          ...a.contexts
        };
        const c = Cy(this, r);
        return a.sdkProcessingMetadata = {
          dynamicSamplingContext: c,
          ...a.sdkProcessingMetadata
        }, a;
      });
    }
    /**
     * Processes the event and logs an error in case of rejection
     * @param event
     * @param hint
     * @param scope
     */
    _captureEvent(t, n = {}, r = Oe(), o = et()) {
      return M && $r(t) && O.log(`Captured error event \`${Pc(t)[0] || "<unknown>"}\``), this._processEvent(t, n, r, o).then(
        (i) => i.event_id,
        (i) => {
          M && (Es(i) ? O.log(i.message) : _s(i) ? O.warn(i.message) : O.warn(i));
        }
      );
    }
    /**
     * Processes an event (either error or message) and sends it to Sentry.
     *
     * This also adds breadcrumbs and context information to the event. However,
     * platform specific meta data (such as the User's IP address) must be added
     * by the SDK implementor.
     *
     *
     * @param event The event to send to Sentry.
     * @param hint May contain additional information about the original exception.
     * @param currentScope A scope containing event metadata.
     * @returns A SyncPromise that resolves with the event or rejects in case event was/will not be send.
     */
    _processEvent(t, n, r, o) {
      const i = this.getOptions(), { sampleRate: s } = i, a = xc(t), c = $r(t), l = `before send for type \`${t.type || "error"}\``, f = typeof s > "u" ? void 0 : dy(s);
      if (c && typeof f == "number" && Sn() > f)
        return this.recordDroppedEvent("sample_rate", "error"), Ao(
          dr(
            `Discarding event because it's not included in the random sample (sampling rate = ${s})`
          )
        );
      const p = Ss(t.type);
      return this._prepareEvent(t, n, r, o).then((d) => {
        if (d === null)
          throw this.recordDroppedEvent("event_processor", p), dr("An event processor returned `null`, will not send event.");
        if (n.data?.__sentry__ === !0)
          return d;
        const h = NA(this, i, d, n);
        return PA(h, l);
      }).then((d) => {
        if (d === null) {
          if (this.recordDroppedEvent("before_send", p), a) {
            const _ = 1 + (t.spans || []).length;
            this.recordDroppedEvent("before_send", "span", _);
          }
          throw dr(`${l} returned \`null\`, will not send event.`);
        }
        const y = r.getSession() || o.getSession();
        if (c && y && this._updateSessionFromEvent(y, d), a) {
          const g = d.sdkProcessingMetadata?.spanCountBeforeProcessing || 0, _ = d.spans ? d.spans.length : 0, v = g - _;
          v > 0 && this.recordDroppedEvent("before_send", "span", v);
        }
        const h = d.transaction_info;
        if (a && h && d.transaction !== t.transaction) {
          const g = "custom";
          d.transaction_info = {
            ...h,
            source: g
          };
        }
        return this.sendEvent(d, n), d;
      }).then(null, (d) => {
        throw Es(d) || _s(d) ? d : (this.captureException(d, {
          mechanism: {
            handled: !1,
            type: "internal"
          },
          data: {
            __sentry__: !0
          },
          originalException: d
        }), mn(
          `Event processing pipeline threw an error, original event will not be sent. Details have been sent as a new event.
Reason: ${d}`
        ));
      });
    }
    /**
     * Occupies the client with processing and event
     */
    _process(t, n) {
      this._numProcessing++, this._promiseBuffer.add(t).then(
        (r) => (this._numProcessing--, r),
        (r) => (this._numProcessing--, r === _o && this.recordDroppedEvent("queue_overflow", n), r)
      );
    }
    /**
     * Clears outcomes on this client and returns them.
     */
    _clearOutcomes() {
      const t = this._outcomes;
      return this._outcomes = {}, Object.entries(t).map(([n, r]) => {
        const [o, i] = n.split(":");
        return {
          reason: o,
          category: i,
          quantity: r
        };
      });
    }
    /**
     * Sends client reports as an envelope.
     */
    _flushOutcomes() {
      M && O.log("Flushing outcomes...");
      const t = this._clearOutcomes();
      if (t.length === 0) {
        M && O.log("No outcomes to send");
        return;
      }
      if (!this._dsn) {
        M && O.log("No dsn provided, will not send outcomes");
        return;
      }
      M && O.log("Sending outcomes:", t);
      const n = CA(t, this._options.tunnel && qt(this._dsn));
      this.sendEnvelope(n);
    }
    /**
     * Creates an {@link Event} from all inputs to `captureException` and non-primitive inputs to `captureMessage`.
     */
  }
  function Ss(e) {
    return e === "replay_event" ? "replay" : e || "error";
  }
  function PA(e, t) {
    const n = `${t} must return \`null\` or a valid event.`;
    if (zt(e))
      return e.then(
        (r) => {
          if (!kt(r) && r !== null)
            throw mn(n);
          return r;
        },
        (r) => {
          throw mn(`${t} rejected with ${r}`);
        }
      );
    if (!kt(e) && e !== null)
      throw mn(n);
    return e;
  }
  function NA(e, t, n, r) {
    const { beforeSend: o, beforeSendTransaction: i, ignoreSpans: s } = t, a = !Iy(t.beforeSendSpan) && t.beforeSendSpan;
    let c = n;
    if ($r(c) && o)
      return o(c, r);
    if (xc(c)) {
      if (a || s) {
        const u = RA(c);
        if (s?.length && as(u, s))
          return null;
        if (a) {
          const l = a(u);
          l ? c = Vt(n, IA(l)) : is();
        }
        if (c.spans) {
          const l = [], f = c.spans;
          for (const d of f) {
            if (s?.length && as(d, s)) {
              wy(f, d);
              continue;
            }
            if (a) {
              const y = a(d);
              y ? l.push(y) : (is(), l.push(d));
            } else
              l.push(d);
          }
          const p = c.spans.length - l.length;
          p && e.recordDroppedEvent("before_send", "span", p), c.spans = l;
        }
      }
      if (i) {
        if (c.spans) {
          const u = c.spans.length;
          c.sdkProcessingMetadata = {
            ...n.sdkProcessingMetadata,
            spanCountBeforeProcessing: u
          };
        }
        return i(c, r);
      }
    }
    return c;
  }
  function $r(e) {
    return e.type === void 0;
  }
  function xc(e) {
    return e.type === "transaction";
  }
  function LA(e) {
    let t = 0;
    return e.name && (t += e.name.length * 2), t += 8, t + kc(e.attributes);
  }
  function xA(e) {
    let t = 0;
    return e.message && (t += e.message.length * 2), t + kc(e.attributes);
  }
  function kc(e) {
    if (!e)
      return 0;
    let t = 0;
    return Object.values(e).forEach((n) => {
      Array.isArray(n) ? t += n.length * ws(n[0]) : kn(n) ? t += ws(n) : t += 100;
    }), t;
  }
  function ws(e) {
    return typeof e == "string" ? e.length * 2 : typeof e == "number" ? 8 : typeof e == "boolean" ? 4 : 0;
  }
  function kA(e) {
    return xn(e) && "__sentry_fetch_url_host__" in e && typeof e.__sentry_fetch_url_host__ == "string";
  }
  function vs(e) {
    return kA(e) ? `${e.message} (${e.__sentry_fetch_url_host__})` : e.message;
  }
  function DA(e, t) {
    t.debug === !0 && (M ? O.enable() : gt(() => {
      console.warn("[Sentry] Cannot initialize SDK with `debug` option using a non-debug bundle.");
    })), Oe().update(t.initialScope);
    const r = new e(t);
    return $A(r), r.init(), r;
  }
  function $A(e) {
    Oe().setClient(e);
  }
  function pr(e) {
    if (!e)
      return {};
    const t = e.match(/^(([^:/?#]+):)?(\/\/([^/?#]*))?([^?#]*)(\?([^#]*))?(#(.*))?$/);
    if (!t)
      return {};
    const n = t[6] || "", r = t[8] || "";
    return {
      host: t[4],
      path: t[5],
      protocol: t[2],
      search: n,
      hash: r,
      relative: t[5] + n + r
      // everything minus origin
    };
  }
  function FA(e, t = !0) {
    if (e.startsWith("data:")) {
      const n = e.match(/^data:([^;,]+)/), r = n ? n[1] : "text/plain", o = e.includes(";base64,"), i = e.indexOf(",");
      let s = "";
      if (t && i !== -1) {
        const a = e.slice(i + 1);
        s = a.length > 10 ? `${a.slice(0, 10)}... [truncated]` : a;
      }
      return `data:${r}${o ? ",base64" : ""}${s ? `,${s}` : ""}`;
    }
    return e;
  }
  function jA(e) {
    "aggregates" in e ? e.attrs?.ip_address === void 0 && (e.attrs = {
      ...e.attrs,
      ip_address: "{{auto}}"
    }) : e.ipAddress === void 0 && (e.ipAddress = "{{auto}}");
  }
  function BA(e, t, n = [t], r = "npm") {
    const o = (e._metadata = e._metadata || {}).sdk = e._metadata.sdk || {};
    o.name || (o.name = `sentry.javascript.${t}`, o.packages = n.map((i) => ({
      name: `${r}:@sentry/${i}`,
      version: ze
    })), o.version = ze);
  }
  const UA = 100;
  function Ge(e, t) {
    const n = X(), r = et();
    if (!n) return;
    const { beforeBreadcrumb: o = null, maxBreadcrumbs: i = UA } = n.getOptions();
    if (i <= 0) return;
    const a = { timestamp: Wt(), ...e }, c = o ? gt(() => o(a, t)) : a;
    c !== null && (n.emit && n.emit("beforeAddBreadcrumb", c, t), r.addBreadcrumb(c, i));
  }
  let Ts;
  const HA = "FunctionToString", Cs = /* @__PURE__ */ new WeakMap(), zA = (() => ({
    name: HA,
    setupOnce() {
      Ts = Function.prototype.toString;
      try {
        Function.prototype.toString = function(...e) {
          const t = mo(this), n = Cs.has(X()) && t !== void 0 ? t : this;
          return Ts.apply(n, e);
        };
      } catch {
      }
    },
    setup(e) {
      Cs.set(e, !0);
    }
  })), WA = zA, VA = [
    /^Script error\.?$/,
    /^Javascript error: Script error\.? on line 0$/,
    /^ResizeObserver loop completed with undelivered notifications.$/,
    // The browser logs this when a ResizeObserver handler takes a bit longer. Usually this is not an actual issue though. It indicates slowness.
    /^Cannot redefine property: googletag$/,
    // This is thrown when google tag manager is used in combination with an ad blocker
    /^Can't find variable: gmo$/,
    // Error from Google Search App https://issuetracker.google.com/issues/396043331
    /^undefined is not an object \(evaluating 'a\.[A-Z]'\)$/,
    // Random error that happens but not actionable or noticeable to end-users.
    /can't redefine non-configurable property "solana"/,
    // Probably a browser extension or custom browser (Brave) throwing this error
    /vv\(\)\.getRestrictions is not a function/,
    // Error thrown by GTM, seemingly not affecting end-users
    /Can't find variable: _AutofillCallbackHandler/,
    // Unactionable error in instagram webview https://developers.facebook.com/community/threads/320013549791141/
    /Object Not Found Matching Id:\d+, MethodName:simulateEvent/,
    // unactionable error from CEFSharp, a .NET library that embeds chromium in .NET apps
    /^Java exception was raised during method invocation$/
    // error from Facebook Mobile browser (https://github.com/getsentry/sentry-javascript/issues/15065)
  ], qA = "EventFilters", GA = (e = {}) => {
    let t;
    return {
      name: qA,
      setup(n) {
        const r = n.getOptions();
        t = Rs(e, r);
      },
      processEvent(n, r, o) {
        if (!t) {
          const i = o.getOptions();
          t = Rs(e, i);
        }
        return KA(n, t) ? null : n;
      }
    };
  }, JA = ((e = {}) => ({
    ...GA(e),
    name: "InboundFilters"
  }));
  function Rs(e = {}, t = {}) {
    return {
      allowUrls: [...e.allowUrls || [], ...t.allowUrls || []],
      denyUrls: [...e.denyUrls || [], ...t.denyUrls || []],
      ignoreErrors: [
        ...e.ignoreErrors || [],
        ...t.ignoreErrors || [],
        ...e.disableErrorDefaults ? [] : VA
      ],
      ignoreTransactions: [...e.ignoreTransactions || [], ...t.ignoreTransactions || []]
    };
  }
  function KA(e, t) {
    if (e.type) {
      if (e.type === "transaction" && YA(e, t.ignoreTransactions))
        return M && O.warn(
          `Event dropped due to being matched by \`ignoreTransactions\` option.
Event: ${Be(e)}`
        ), !0;
    } else {
      if (ZA(e, t.ignoreErrors))
        return M && O.warn(
          `Event dropped due to being matched by \`ignoreErrors\` option.
Event: ${Be(e)}`
        ), !0;
      if (t_(e))
        return M && O.warn(
          `Event dropped due to not having an error message, error type or stacktrace.
Event: ${Be(
            e
          )}`
        ), !0;
      if (XA(e, t.denyUrls))
        return M && O.warn(
          `Event dropped due to being matched by \`denyUrls\` option.
Event: ${Be(
            e
          )}.
Url: ${wn(e)}`
        ), !0;
      if (!QA(e, t.allowUrls))
        return M && O.warn(
          `Event dropped due to not being matched by \`allowUrls\` option.
Event: ${Be(
            e
          )}.
Url: ${wn(e)}`
        ), !0;
    }
    return !1;
  }
  function ZA(e, t) {
    return t?.length ? Pc(e).some((n) => jn(n, t)) : !1;
  }
  function YA(e, t) {
    if (!t?.length)
      return !1;
    const n = e.transaction;
    return n ? jn(n, t) : !1;
  }
  function XA(e, t) {
    if (!t?.length)
      return !1;
    const n = wn(e);
    return n ? jn(n, t) : !1;
  }
  function QA(e, t) {
    if (!t?.length)
      return !0;
    const n = wn(e);
    return n ? jn(n, t) : !0;
  }
  function e_(e = []) {
    for (let t = e.length - 1; t >= 0; t--) {
      const n = e[t];
      if (n && n.filename !== "<anonymous>" && n.filename !== "[native code]")
        return n.filename || null;
    }
    return null;
  }
  function wn(e) {
    try {
      const n = [...e.exception?.values ?? []].reverse().find((r) => r.mechanism?.parent_id === void 0 && r.stacktrace?.frames?.length)?.stacktrace?.frames;
      return n ? e_(n) : null;
    } catch {
      return M && O.error(`Cannot extract url for event ${Be(e)}`), null;
    }
  }
  function t_(e) {
    return e.exception?.values?.length ? (
      // No top-level message
      !e.message && // There are no exception values that have a stacktrace, a non-generic-Error type or value
      !e.exception.values.some((t) => t.stacktrace || t.type && t.type !== "Error" || t.value)
    ) : !1;
  }
  function n_(e, t, n, r, o, i) {
    if (!o.exception?.values || !i || !xe(i.originalException, Error))
      return;
    const s = o.exception.values.length > 0 ? o.exception.values[o.exception.values.length - 1] : void 0;
    s && (o.exception.values = Fr(
      e,
      t,
      r,
      i.originalException,
      n,
      o.exception.values,
      s,
      0
    ));
  }
  function Fr(e, t, n, r, o, i, s, a) {
    if (i.length >= n + 1)
      return i;
    let c = [...i];
    if (xe(r[o], Error)) {
      Is(s, a, r);
      const u = e(t, r[o]), l = c.length;
      Os(u, o, l, a), c = Fr(
        e,
        t,
        n,
        r[o],
        o,
        [u, ...c],
        u,
        l
      );
    }
    return Dc(r) && r.errors.forEach((u, l) => {
      if (xe(u, Error)) {
        Is(s, a, r);
        const f = e(t, u), p = c.length;
        Os(f, `errors[${l}]`, p, a), c = Fr(
          e,
          t,
          n,
          u,
          o,
          [f, ...c],
          f,
          p
        );
      }
    }), c;
  }
  function Dc(e) {
    return Array.isArray(e.errors);
  }
  function Is(e, t, n) {
    e.mechanism = {
      handled: !0,
      type: "auto.core.linked_errors",
      ...Dc(n) && { is_exception_group: !0 },
      ...e.mechanism,
      exception_id: t
    };
  }
  function Os(e, t, n, r) {
    e.mechanism = {
      handled: !0,
      ...e.mechanism,
      type: "chained",
      source: t,
      exception_id: n,
      parent_id: r
    };
  }
  function r_(e) {
    const t = "console";
    Xe(t, e), Qe(t, o_);
  }
  function o_() {
    "console" in F && ig.forEach(function(e) {
      e in F.console && oe(F.console, e, function(t) {
        return bn[e] = t, function(...n) {
          de("console", { args: n, level: e }), bn[e]?.apply(F.console, n);
        };
      });
    });
  }
  function i_(e) {
    return e === "warn" ? "warning" : ["fatal", "error", "warning", "log", "info", "debug"].includes(e) ? e : "log";
  }
  const s_ = "Dedupe", a_ = (() => {
    let e;
    return {
      name: s_,
      processEvent(t) {
        if (t.type)
          return t;
        try {
          if (u_(t, e))
            return M && O.warn("Event dropped due to being a duplicate of previously captured event."), null;
        } catch {
        }
        return e = t;
      }
    };
  }), c_ = a_;
  function u_(e, t) {
    return t ? !!(l_(e, t) || f_(e, t)) : !1;
  }
  function l_(e, t) {
    const n = e.message, r = t.message;
    return !(!n && !r || n && !r || !n && r || n !== r || !Fc(e, t) || !$c(e, t));
  }
  function f_(e, t) {
    const n = Ms(t), r = Ms(e);
    return !(!n || !r || n.type !== r.type || n.value !== r.value || !Fc(e, t) || !$c(e, t));
  }
  function $c(e, t) {
    let n = Ui(e), r = Ui(t);
    if (!n && !r)
      return !0;
    if (n && !r || !n && r || (n = n, r = r, r.length !== n.length))
      return !1;
    for (let o = 0; o < r.length; o++) {
      const i = r[o], s = n[o];
      if (i.filename !== s.filename || i.lineno !== s.lineno || i.colno !== s.colno || i.function !== s.function)
        return !1;
    }
    return !0;
  }
  function Fc(e, t) {
    let n = e.fingerprint, r = t.fingerprint;
    if (!n && !r)
      return !0;
    if (n && !r || !n && r)
      return !1;
    n = n, r = r;
    try {
      return n.join("") === r.join("");
    } catch {
      return !1;
    }
  }
  function Ms(e) {
    return e.exception?.values?.[0];
  }
  const d_ = "ConversationId", p_ = (() => ({
    name: d_,
    setup(e) {
      e.on("spanStart", (t) => {
        const n = Oe().getScopeData(), r = et().getScopeData(), o = n.conversationId || r.conversationId;
        if (o) {
          const { op: i, data: s, description: a } = Bn(t);
          if (!i?.startsWith("gen_ai.") && !s["ai.operationId"] && !a?.startsWith("ai."))
            return;
          t.setAttribute(Zg, o);
        }
      });
    }
  })), h_ = p_;
  function jc(e) {
    if (e !== void 0)
      return e >= 400 && e < 500 ? "warning" : e >= 500 ? "error" : void 0;
  }
  const $t = F;
  function m_() {
    return "history" in $t && !!$t.history;
  }
  function g_() {
    if (!("fetch" in $t))
      return !1;
    try {
      return new Headers(), new Request("data:,"), new Response(), !0;
    } catch {
      return !1;
    }
  }
  function jr(e) {
    return e && /^function\s+\w+\(\)\s+\{\s+\[native code\]\s+\}$/.test(e.toString());
  }
  function y_() {
    if (typeof EdgeRuntime == "string")
      return !0;
    if (!g_())
      return !1;
    if (jr($t.fetch))
      return !0;
    let e = !1;
    const t = $t.document;
    if (t && typeof t.createElement == "function")
      try {
        const n = t.createElement("iframe");
        n.hidden = !0, t.head.appendChild(n), n.contentWindow?.fetch && (e = jr(n.contentWindow.fetch)), t.head.removeChild(n);
      } catch (n) {
        M && O.warn("Could not create sandbox iframe for pure fetch check, bailing to window.fetch: ", n);
      }
    return e;
  }
  function A_(e, t) {
    const n = "fetch";
    Xe(n, e), Qe(n, () => __(void 0, t));
  }
  function __(e, t = !1) {
    t && !y_() || oe(F, "fetch", function(n) {
      return function(...r) {
        const o = new Error(), { method: i, url: s } = E_(r), a = {
          args: r,
          fetchData: {
            method: i,
            url: s
          },
          startTimestamp: Ce() * 1e3,
          // // Adding the error to be able to fingerprint the failed fetch event in HttpClient instrumentation
          virtualError: o,
          headers: b_(r)
        };
        return de("fetch", {
          ...a
        }), n.apply(F, r).then(
          async (c) => (de("fetch", {
            ...a,
            endTimestamp: Ce() * 1e3,
            response: c
          }), c),
          (c) => {
            de("fetch", {
              ...a,
              endTimestamp: Ce() * 1e3,
              error: c
            }), xn(c) && c.stack === void 0 && (c.stack = o.stack, ke(c, "framesToPop", 1));
            const l = X()?.getOptions().enhanceFetchErrorMessages ?? "always";
            if (l !== !1 && c instanceof TypeError && (c.message === "Failed to fetch" || c.message === "Load failed" || c.message === "NetworkError when attempting to fetch resource."))
              try {
                const d = new URL(a.fetchData.url).host;
                l === "always" ? c.message = `${c.message} (${d})` : ke(c, "__sentry_fetch_url_host__", d);
              } catch {
              }
            throw c;
          }
        );
      };
    });
  }
  function gn(e, t) {
    return !!e && typeof e == "object" && !!e[t];
  }
  function Ps(e) {
    return typeof e == "string" ? e : e ? gn(e, "url") ? e.url : e.toString ? e.toString() : "" : "";
  }
  function E_(e) {
    if (e.length === 0)
      return { method: "GET", url: "" };
    if (e.length === 2) {
      const [n, r] = e;
      return {
        url: Ps(n),
        method: gn(r, "method") ? String(r.method).toUpperCase() : (
          // Request object as first argument
          rc(n) && gn(n, "method") ? String(n.method).toUpperCase() : "GET"
        )
      };
    }
    const t = e[0];
    return {
      url: Ps(t),
      method: gn(t, "method") ? String(t.method).toUpperCase() : "GET"
    };
  }
  function b_(e) {
    const [t, n] = e;
    try {
      if (typeof n == "object" && n !== null && "headers" in n && n.headers)
        return new Headers(n.headers);
      if (rc(t))
        return new Headers(t.headers);
    } catch {
    }
  }
  function S_() {
    return "npm";
  }
  const H = F;
  let Br = 0;
  function Bc() {
    return Br > 0;
  }
  function w_() {
    Br++, setTimeout(() => {
      Br--;
    });
  }
  function ft(e, t = {}) {
    function n(o) {
      return typeof o == "function";
    }
    if (!n(e))
      return e;
    try {
      const o = e.__sentry_wrapped__;
      if (o)
        return typeof o == "function" ? o : e;
      if (mo(e))
        return e;
    } catch {
      return e;
    }
    const r = function(...o) {
      try {
        const i = o.map((s) => ft(s, t));
        return e.apply(this, i);
      } catch (i) {
        throw w_(), zg((s) => {
          s.addEventProcessor((a) => (t.mechanism && (Mr(a, void 0), ct(a, t.mechanism)), a.extra = {
            ...a.extra,
            arguments: o
          }, a)), iA(i);
        }), i;
      }
    };
    try {
      for (const o in e)
        Object.prototype.hasOwnProperty.call(e, o) && (r[o] = e[o]);
    } catch {
    }
    ic(r, e), ke(e, "__sentry_wrapped__", r);
    try {
      Object.getOwnPropertyDescriptor(r, "name").configurable && Object.defineProperty(r, "name", {
        get() {
          return e.name;
        }
      });
    } catch {
    }
    return r;
  }
  function v_() {
    const e = ho(), { referrer: t } = H.document || {}, { userAgent: n } = H.navigator || {}, r = {
      ...t && { Referer: t },
      ...n && { "User-Agent": n }
    };
    return {
      url: e,
      headers: r
    };
  }
  function bo(e, t) {
    const n = So(e, t), r = {
      type: O_(t),
      value: M_(t)
    };
    return n.length && (r.stacktrace = { frames: n }), r.type === void 0 && r.value === "" && (r.value = "Unrecoverable error caught"), r;
  }
  function T_(e, t, n, r) {
    const i = X()?.getOptions().normalizeDepth, s = k_(t), a = {
      __serialized__: Ac(t, i)
    };
    if (s)
      return {
        exception: {
          values: [bo(e, s)]
        },
        extra: a
      };
    const c = {
      exception: {
        values: [
          {
            type: Dn(t) ? t.constructor.name : r ? "UnhandledRejection" : "Error",
            value: L_(t, { isUnhandledRejection: r })
          }
        ]
      },
      extra: a
    };
    if (n) {
      const u = So(e, n);
      u.length && (c.exception.values[0].stacktrace = { frames: u });
    }
    return c;
  }
  function hr(e, t) {
    return {
      exception: {
        values: [bo(e, t)]
      }
    };
  }
  function So(e, t) {
    const n = t.stacktrace || t.stack || "", r = R_(t), o = I_(t);
    try {
      return e(n, r, o);
    } catch {
    }
    return [];
  }
  const C_ = /Minified React error #\d+;/i;
  function R_(e) {
    return e && C_.test(e.message) ? 1 : 0;
  }
  function I_(e) {
    return typeof e.framesToPop == "number" ? e.framesToPop : 0;
  }
  function Uc(e) {
    return typeof WebAssembly < "u" && typeof WebAssembly.Exception < "u" ? e instanceof WebAssembly.Exception : !1;
  }
  function O_(e) {
    const t = e?.name;
    return !t && Uc(e) ? e.message && Array.isArray(e.message) && e.message.length == 2 ? e.message[0] : "WebAssembly.Exception" : t;
  }
  function M_(e) {
    const t = e?.message;
    return Uc(e) ? Array.isArray(e.message) && e.message.length == 2 ? e.message[1] : "wasm exception" : t ? t.error && typeof t.error.message == "string" ? vs(t.error) : vs(e) : "No error message";
  }
  function P_(e, t, n, r) {
    const o = n?.syntheticException || void 0, i = wo(e, t, o, r);
    return ct(i), i.level = "error", n?.event_id && (i.event_id = n.event_id), Gt(i);
  }
  function N_(e, t, n = "info", r, o) {
    const i = r?.syntheticException || void 0, s = Ur(e, t, i, o);
    return s.level = n, r?.event_id && (s.event_id = r.event_id), Gt(s);
  }
  function wo(e, t, n, r, o) {
    let i;
    if (tc(t) && t.error)
      return hr(e, t.error);
    if (zi(t) || Ag(t)) {
      const s = t;
      if ("stack" in t)
        i = hr(e, t);
      else {
        const a = s.name || (zi(s) ? "DOMError" : "DOMException"), c = s.message ? `${a}: ${s.message}` : a;
        i = Ur(e, c, n, r), Mr(i, c);
      }
      return "code" in s && (i.tags = { ...i.tags, "DOMException.code": `${s.code}` }), i;
    }
    return xn(t) ? hr(e, t) : kt(t) || Dn(t) ? (i = T_(e, t, n, o), ct(i, {
      synthetic: !0
    }), i) : (i = Ur(e, t, n, r), Mr(i, `${t}`), ct(i, {
      synthetic: !0
    }), i);
  }
  function Ur(e, t, n, r) {
    const o = {};
    if (r && n) {
      const i = So(e, n);
      i.length && (o.exception = {
        values: [{ value: t, stacktrace: { frames: i } }]
      }), ct(o, { synthetic: !0 });
    }
    if (fo(t)) {
      const { __sentry_template_string__: i, __sentry_template_values__: s } = t;
      return o.logentry = {
        message: i,
        params: s
      }, o;
    }
    return o.message = t, o;
  }
  function L_(e, { isUnhandledRejection: t }) {
    const n = Tg(e), r = t ? "promise rejection" : "exception";
    return tc(e) ? `Event \`ErrorEvent\` captured as ${r} with message \`${e.message}\`` : Dn(e) ? `Event \`${x_(e)}\` (type=${e.type}) captured as ${r}` : `Object captured as ${r} with keys: ${n}`;
  }
  function x_(e) {
    try {
      const t = Object.getPrototypeOf(e);
      return t ? t.constructor.name : void 0;
    } catch {
    }
  }
  function k_(e) {
    return Object.values(e).find((t) => t instanceof Error);
  }
  class D_ extends MA {
    /**
     * Creates a new Browser SDK instance.
     *
     * @param options Configuration options for this SDK.
     */
    constructor(t) {
      const n = $_(t), r = H.SENTRY_SDK_SOURCE || S_();
      BA(n, "browser", ["browser"], r), n._metadata?.sdk && (n._metadata.sdk.settings = {
        infer_ip: n.sendDefaultPii ? "auto" : "never",
        // purposefully allowing already passed settings to override the default
        ...n._metadata.sdk.settings
      }), super(n);
      const {
        sendDefaultPii: o,
        sendClientReports: i,
        enableLogs: s,
        _experiments: a,
        enableMetrics: c
      } = this._options, u = c ?? a?.enableMetrics ?? !0;
      H.document && (i || s || u) && H.document.addEventListener("visibilitychange", () => {
        H.document.visibilityState === "hidden" && (i && this._flushOutcomes(), s && Dr(this), u && Rc(this));
      }), o && this.on("beforeSendSession", jA);
    }
    /**
     * @inheritDoc
     */
    eventFromException(t, n) {
      return P_(this._options.stackParser, t, n, this._options.attachStacktrace);
    }
    /**
     * @inheritDoc
     */
    eventFromMessage(t, n = "info", r) {
      return N_(this._options.stackParser, t, n, r, this._options.attachStacktrace);
    }
    /**
     * @inheritDoc
     */
    _prepareEvent(t, n, r, o) {
      return t.platform = t.platform || "javascript", super._prepareEvent(t, n, r, o);
    }
  }
  function $_(e) {
    return {
      release: typeof __SENTRY_RELEASE__ == "string" ? __SENTRY_RELEASE__ : H.SENTRY_RELEASE?.id,
      // This supports the variable that sentry-webpack-plugin injects
      sendClientReports: !0,
      // We default this to true, as it is the safer scenario
      parentSpanIsAlwaysRootSpan: !0,
      ...e
    };
  }
  const F_ = typeof __SENTRY_DEBUG__ > "u" || __SENTRY_DEBUG__, Q = F, j_ = 1e3;
  let Ns, Hr, zr;
  function B_(e) {
    Xe("dom", e), Qe("dom", U_);
  }
  function U_() {
    if (!Q.document)
      return;
    const e = de.bind(null, "dom"), t = Ls(e, !0);
    Q.document.addEventListener("click", t, !1), Q.document.addEventListener("keypress", t, !1), ["EventTarget", "Node"].forEach((n) => {
      const o = Q[n]?.prototype;
      o?.hasOwnProperty?.("addEventListener") && (oe(o, "addEventListener", function(i) {
        return function(s, a, c) {
          if (s === "click" || s == "keypress")
            try {
              const u = this.__sentry_instrumentation_handlers__ = this.__sentry_instrumentation_handlers__ || {}, l = u[s] = u[s] || { refCount: 0 };
              if (!l.handler) {
                const f = Ls(e);
                l.handler = f, i.call(this, s, f, c);
              }
              l.refCount++;
            } catch {
            }
          return i.call(this, s, a, c);
        };
      }), oe(
        o,
        "removeEventListener",
        function(i) {
          return function(s, a, c) {
            if (s === "click" || s == "keypress")
              try {
                const u = this.__sentry_instrumentation_handlers__ || {}, l = u[s];
                l && (l.refCount--, l.refCount <= 0 && (i.call(this, s, l.handler, c), l.handler = void 0, delete u[s]), Object.keys(u).length === 0 && delete this.__sentry_instrumentation_handlers__);
              } catch {
              }
            return i.call(this, s, a, c);
          };
        }
      ));
    });
  }
  function H_(e) {
    if (e.type !== Hr)
      return !1;
    try {
      if (!e.target || e.target._sentryId !== zr)
        return !1;
    } catch {
    }
    return !0;
  }
  function z_(e, t) {
    return e !== "keypress" ? !1 : t?.tagName ? !(t.tagName === "INPUT" || t.tagName === "TEXTAREA" || t.isContentEditable) : !0;
  }
  function Ls(e, t = !1) {
    return (n) => {
      if (!n || n._sentryCaptured)
        return;
      const r = W_(n);
      if (z_(n.type, r))
        return;
      ke(n, "_sentryCaptured", !0), r && !r._sentryId && ke(r, "_sentryId", le());
      const o = n.type === "keypress" ? "input" : n.type;
      H_(n) || (e({ event: n, name: o, global: t }), Hr = n.type, zr = r ? r._sentryId : void 0), clearTimeout(Ns), Ns = Q.setTimeout(() => {
        zr = void 0, Hr = void 0;
      }, j_);
    };
  }
  function W_(e) {
    try {
      return e.target;
    } catch {
      return null;
    }
  }
  let on;
  function Hc(e) {
    const t = "history";
    Xe(t, e), Qe(t, V_);
  }
  function V_() {
    if (Q.addEventListener("popstate", () => {
      const t = Q.location.href, n = on;
      if (on = t, n === t)
        return;
      de("history", { from: n, to: t });
    }), !m_())
      return;
    function e(t) {
      return function(...n) {
        const r = n.length > 2 ? n[2] : void 0;
        if (r) {
          const o = on, i = q_(String(r));
          if (on = i, o === i)
            return t.apply(this, n);
          de("history", { from: o, to: i });
        }
        return t.apply(this, n);
      };
    }
    oe(Q.history, "pushState", e), oe(Q.history, "replaceState", e);
  }
  function q_(e) {
    try {
      return new URL(e, Q.location.origin).toString();
    } catch {
      return e;
    }
  }
  const yn = {};
  function G_(e) {
    const t = yn[e];
    if (t)
      return t;
    let n = Q[e];
    if (jr(n))
      return yn[e] = n.bind(Q);
    const r = Q.document;
    if (r && typeof r.createElement == "function")
      try {
        const o = r.createElement("iframe");
        o.hidden = !0, r.head.appendChild(o);
        const i = o.contentWindow;
        i?.[e] && (n = i[e]), r.head.removeChild(o);
      } catch (o) {
        F_ && O.warn(`Could not create sandbox iframe for ${e} check, bailing to window.${e}: `, o);
      }
    return n && (yn[e] = n.bind(Q));
  }
  function J_(e) {
    yn[e] = void 0;
  }
  const Ot = "__sentry_xhr_v3__";
  function K_(e) {
    Xe("xhr", e), Qe("xhr", Z_);
  }
  function Z_() {
    if (!Q.XMLHttpRequest)
      return;
    const e = XMLHttpRequest.prototype;
    e.open = new Proxy(e.open, {
      apply(t, n, r) {
        const o = new Error(), i = Ce() * 1e3, s = Te(r[0]) ? r[0].toUpperCase() : void 0, a = Y_(r[1]);
        if (!s || !a)
          return t.apply(n, r);
        n[Ot] = {
          method: s,
          url: a,
          request_headers: {}
        }, s === "POST" && a.match(/sentry_key/) && (n.__sentry_own_request__ = !0);
        const c = () => {
          const u = n[Ot];
          if (u && n.readyState === 4) {
            try {
              u.status_code = n.status;
            } catch {
            }
            const l = {
              endTimestamp: Ce() * 1e3,
              startTimestamp: i,
              xhr: n,
              virtualError: o
            };
            de("xhr", l);
          }
        };
        return "onreadystatechange" in n && typeof n.onreadystatechange == "function" ? n.onreadystatechange = new Proxy(n.onreadystatechange, {
          apply(u, l, f) {
            return c(), u.apply(l, f);
          }
        }) : n.addEventListener("readystatechange", c), n.setRequestHeader = new Proxy(n.setRequestHeader, {
          apply(u, l, f) {
            const [p, d] = f, y = l[Ot];
            return y && Te(p) && Te(d) && (y.request_headers[p.toLowerCase()] = d), u.apply(l, f);
          }
        }), t.apply(n, r);
      }
    }), e.send = new Proxy(e.send, {
      apply(t, n, r) {
        const o = n[Ot];
        if (!o)
          return t.apply(n, r);
        r[0] !== void 0 && (o.body = r[0]);
        const i = {
          startTimestamp: Ce() * 1e3,
          xhr: n
        };
        return de("xhr", i), t.apply(n, r);
      }
    });
  }
  function Y_(e) {
    if (Te(e))
      return e;
    try {
      return e.toString();
    } catch {
    }
  }
  const X_ = 40;
  function Q_(e, t = G_("fetch")) {
    let n = 0, r = 0;
    async function o(i) {
      const s = i.body.length;
      n += s, r++;
      const a = {
        body: i.body,
        method: "POST",
        referrerPolicy: "strict-origin",
        headers: e.headers,
        // Outgoing requests are usually cancelled when navigating to a different page, causing a "TypeError: Failed to
        // fetch" error and sending a "network_error" client-outcome - in Chrome, the request status shows "(cancelled)".
        // The `keepalive` flag keeps outgoing requests alive, even when switching pages. We want this since we're
        // frequently sending events right before the user is switching pages (eg. when finishing navigation transactions).
        // Gotchas:
        // - `keepalive` isn't supported by Firefox
        // - As per spec (https://fetch.spec.whatwg.org/#http-network-or-cache-fetch):
        //   If the sum of contentLength and inflightKeepaliveBytes is greater than 64 kibibytes, then return a network error.
        //   We will therefore only activate the flag when we're below that limit.
        // There is also a limit of requests that can be open at the same time, so we also limit this to 15
        // See https://github.com/getsentry/sentry-javascript/pull/7553 for details
        keepalive: n <= 6e4 && r < 15,
        ...e.fetchOptions
      };
      try {
        const c = await t(e.url, a);
        return {
          statusCode: c.status,
          headers: {
            "x-sentry-rate-limits": c.headers.get("X-Sentry-Rate-Limits"),
            "retry-after": c.headers.get("Retry-After")
          }
        };
      } catch (c) {
        throw J_("fetch"), c;
      } finally {
        n -= s, r--;
      }
    }
    return TA(
      e,
      o,
      Eo(e.bufferSize || X_)
    );
  }
  const Un = typeof __SENTRY_DEBUG__ > "u" || __SENTRY_DEBUG__, eE = 30, tE = 50;
  function Wr(e, t, n, r) {
    const o = {
      filename: e,
      function: t === "<anonymous>" ? qe : t,
      in_app: !0
      // All browser frames are considered in_app
    };
    return n !== void 0 && (o.lineno = n), r !== void 0 && (o.colno = r), o;
  }
  const nE = /^\s*at (\S+?)(?::(\d+))(?::(\d+))\s*$/i, rE = /^\s*at (?:(.+?\)(?: \[.+\])?|.*?) ?\((?:address at )?)?(?:async )?((?:<anonymous>|[-a-z]+:|.*bundle|\/)?.*?)(?::(\d+))?(?::(\d+))?\)?\s*$/i, oE = /\((\S*)(?::(\d+))(?::(\d+))\)/, iE = /at (.+?) ?\(data:(.+?),/, sE = (e) => {
    const t = e.match(iE);
    if (t)
      return {
        filename: `<data:${t[2]}>`,
        function: t[1]
      };
    const n = nE.exec(e);
    if (n) {
      const [, o, i, s] = n;
      return Wr(o, qe, +i, +s);
    }
    const r = rE.exec(e);
    if (r) {
      if (r[2]?.indexOf("eval") === 0) {
        const a = oE.exec(r[2]);
        a && (r[2] = a[1], r[3] = a[2], r[4] = a[3]);
      }
      const [i, s] = zc(r[1] || qe, r[2]);
      return Wr(s, i, r[3] ? +r[3] : void 0, r[4] ? +r[4] : void 0);
    }
  }, aE = [eE, sE], cE = /^\s*(.*?)(?:\((.*?)\))?(?:^|@)?((?:[-a-z]+)?:\/.*?|\[native code\]|[^@]*(?:bundle|\d+\.js)|\/[\w\-. /=]+)(?::(\d+))?(?::(\d+))?\s*$/i, uE = /(\S+) line (\d+)(?: > eval line \d+)* > eval/i, lE = (e) => {
    const t = cE.exec(e);
    if (t) {
      if (t[3] && t[3].indexOf(" > eval") > -1) {
        const i = uE.exec(t[3]);
        i && (t[1] = t[1] || "eval", t[3] = i[1], t[4] = i[2], t[5] = "");
      }
      let r = t[3], o = t[1] || qe;
      return [o, r] = zc(o, r), Wr(r, o, t[4] ? +t[4] : void 0, t[5] ? +t[5] : void 0);
    }
  }, fE = [tE, lE], dE = [aE, fE], pE = Xa(...dE), zc = (e, t) => {
    const n = e.indexOf("safari-extension") !== -1, r = e.indexOf("safari-web-extension") !== -1;
    return n || r ? [
      e.indexOf("@") !== -1 ? e.split("@")[0] : qe,
      n ? `safari-extension:${t}` : `safari-web-extension:${t}`
    ] : [e, t];
  }, sn = 1024, hE = "Breadcrumbs", mE = ((e = {}) => {
    const t = {
      console: !0,
      dom: !0,
      fetch: !0,
      history: !0,
      sentry: !0,
      xhr: !0,
      ...e
    };
    return {
      name: hE,
      setup(n) {
        t.console && r_(_E(n)), t.dom && B_(AE(n, t.dom)), t.xhr && K_(EE(n)), t.fetch && A_(bE(n)), t.history && Hc(SE(n)), t.sentry && n.on("beforeSendEvent", yE(n));
      }
    };
  }), gE = mE;
  function yE(e) {
    return function(n) {
      X() === e && Ge(
        {
          category: `sentry.${n.type === "transaction" ? "transaction" : "event"}`,
          event_id: n.event_id,
          level: n.level,
          message: Be(n)
        },
        {
          event: n
        }
      );
    };
  }
  function AE(e, t) {
    return function(r) {
      if (X() !== e)
        return;
      let o, i, s = typeof t == "object" ? t.serializeAttribute : void 0, a = typeof t == "object" && typeof t.maxStringLength == "number" ? t.maxStringLength : void 0;
      a && a > sn && (Un && O.warn(
        `\`dom.maxStringLength\` cannot exceed ${sn}, but a value of ${a} was configured. Sentry will use ${sn} instead.`
      ), a = sn), typeof s == "string" && (s = [s]);
      try {
        const u = r.event, l = wE(u) ? u.target : u;
        o = oc(l, { keyAttrs: s, maxStringLength: a }), i = vg(l);
      } catch {
        o = "<unknown>";
      }
      if (o.length === 0)
        return;
      const c = {
        category: `ui.${r.name}`,
        message: o
      };
      i && (c.data = { "ui.component_name": i }), Ge(c, {
        event: r.event,
        name: r.name,
        global: r.global
      });
    };
  }
  function _E(e) {
    return function(n) {
      if (X() !== e)
        return;
      const r = {
        category: "console",
        data: {
          arguments: n.args,
          logger: "console"
        },
        level: i_(n.level),
        message: qi(n.args, " ")
      };
      if (n.level === "assert")
        if (n.args[0] === !1)
          r.message = `Assertion failed: ${qi(n.args.slice(1), " ") || "console.assert"}`, r.data.arguments = n.args.slice(1);
        else
          return;
      Ge(r, {
        input: n.args,
        level: n.level
      });
    };
  }
  function EE(e) {
    return function(n) {
      if (X() !== e)
        return;
      const { startTimestamp: r, endTimestamp: o } = n, i = n.xhr[Ot];
      if (!r || !o || !i)
        return;
      const { method: s, url: a, status_code: c, body: u } = i, l = {
        method: s,
        url: a,
        status_code: c
      }, f = {
        xhr: n.xhr,
        input: u,
        startTimestamp: r,
        endTimestamp: o
      }, p = {
        category: "xhr",
        data: l,
        type: "http",
        level: jc(c)
      };
      e.emit("beforeOutgoingRequestBreadcrumb", p, f), Ge(p, f);
    };
  }
  function bE(e) {
    return function(n) {
      if (X() !== e)
        return;
      const { startTimestamp: r, endTimestamp: o } = n;
      if (o && !(n.fetchData.url.match(/sentry_key/) && n.fetchData.method === "POST"))
        if (n.error) {
          const i = {
            data: n.error,
            input: n.args,
            startTimestamp: r,
            endTimestamp: o
          }, s = {
            category: "fetch",
            data: n.fetchData,
            level: "error",
            type: "http"
          };
          e.emit("beforeOutgoingRequestBreadcrumb", s, i), Ge(s, i);
        } else {
          const i = n.response, s = {
            ...n.fetchData,
            status_code: i?.status
          }, a = {
            input: n.args,
            response: i,
            startTimestamp: r,
            endTimestamp: o
          }, c = {
            category: "fetch",
            data: s,
            type: "http",
            level: jc(s.status_code)
          };
          e.emit("beforeOutgoingRequestBreadcrumb", c, a), Ge(c, a);
        }
    };
  }
  function SE(e) {
    return function(n) {
      if (X() !== e)
        return;
      let r = n.from, o = n.to;
      const i = pr(H.location.href);
      let s = r ? pr(r) : void 0;
      const a = pr(o);
      s?.path || (s = i), i.protocol === a.protocol && i.host === a.host && (o = a.relative), i.protocol === s.protocol && i.host === s.host && (r = s.relative), Ge({
        category: "navigation",
        data: {
          from: r,
          to: o
        }
      });
    };
  }
  function wE(e) {
    return !!e && !!e.target;
  }
  const vE = "EventTarget,Window,Node,ApplicationCache,AudioTrackList,BroadcastChannel,ChannelMergerNode,CryptoOperation,EventSource,FileReader,HTMLUnknownElement,IDBDatabase,IDBRequest,IDBTransaction,KeyOperation,MediaController,MessagePort,ModalWindow,Notification,SVGElementInstance,Screen,SharedWorker,TextTrack,TextTrackCue,TextTrackList,WebSocket,WebSocketWorker,Worker,XMLHttpRequest,XMLHttpRequestEventTarget,XMLHttpRequestUpload".split(
    ","
  ), TE = "BrowserApiErrors", CE = ((e = {}) => {
    const t = {
      XMLHttpRequest: !0,
      eventTarget: !0,
      requestAnimationFrame: !0,
      setInterval: !0,
      setTimeout: !0,
      unregisterOriginalCallbacks: !1,
      ...e
    };
    return {
      name: TE,
      // TODO: This currently only works for the first client this is setup
      // We may want to adjust this to check for client etc.
      setupOnce() {
        t.setTimeout && oe(H, "setTimeout", xs), t.setInterval && oe(H, "setInterval", xs), t.requestAnimationFrame && oe(H, "requestAnimationFrame", IE), t.XMLHttpRequest && "XMLHttpRequest" in H && oe(XMLHttpRequest.prototype, "send", OE);
        const n = t.eventTarget;
        n && (Array.isArray(n) ? n : vE).forEach((o) => ME(o, t));
      }
    };
  }), RE = CE;
  function xs(e) {
    return function(...t) {
      const n = t[0];
      return t[0] = ft(n, {
        mechanism: {
          handled: !1,
          type: `auto.browser.browserapierrors.${Le(e)}`
        }
      }), e.apply(this, t);
    };
  }
  function IE(e) {
    return function(t) {
      return e.apply(this, [
        ft(t, {
          mechanism: {
            data: {
              handler: Le(e)
            },
            handled: !1,
            type: "auto.browser.browserapierrors.requestAnimationFrame"
          }
        })
      ]);
    };
  }
  function OE(e) {
    return function(...t) {
      const n = this;
      return ["onload", "onerror", "onprogress", "onreadystatechange"].forEach((o) => {
        o in n && typeof n[o] == "function" && oe(n, o, function(i) {
          const s = {
            mechanism: {
              data: {
                handler: Le(i)
              },
              handled: !1,
              type: `auto.browser.browserapierrors.xhr.${o}`
            }
          }, a = mo(i);
          return a && (s.mechanism.data.handler = Le(a)), ft(i, s);
        });
      }), e.apply(this, t);
    };
  }
  function ME(e, t) {
    const r = H[e]?.prototype;
    r?.hasOwnProperty?.("addEventListener") && (oe(r, "addEventListener", function(o) {
      return function(i, s, a) {
        try {
          PE(s) && (s.handleEvent = ft(s.handleEvent, {
            mechanism: {
              data: {
                handler: Le(s),
                target: e
              },
              handled: !1,
              type: "auto.browser.browserapierrors.handleEvent"
            }
          }));
        } catch {
        }
        return t.unregisterOriginalCallbacks && NE(this, i, s), o.apply(this, [
          i,
          ft(s, {
            mechanism: {
              data: {
                handler: Le(s),
                target: e
              },
              handled: !1,
              type: "auto.browser.browserapierrors.addEventListener"
            }
          }),
          a
        ]);
      };
    }), oe(r, "removeEventListener", function(o) {
      return function(i, s, a) {
        try {
          const c = s.__sentry_wrapped__;
          c && o.call(this, i, c, a);
        } catch {
        }
        return o.call(this, i, s, a);
      };
    }));
  }
  function PE(e) {
    return typeof e.handleEvent == "function";
  }
  function NE(e, t, n) {
    e && typeof e == "object" && "removeEventListener" in e && typeof e.removeEventListener == "function" && e.removeEventListener(t, n);
  }
  const LE = (e = {}) => {
    const t = e.lifecycle ?? "route";
    return {
      name: "BrowserSession",
      setupOnce() {
        if (typeof H.document > "u") {
          Un && O.warn("Using the `browserSessionIntegration` in non-browser environments is not supported.");
          return;
        }
        hs({ ignoreDuration: !0 }), fr();
        const n = et();
        let r = n.getUser();
        n.addScopeListener((o) => {
          const i = o.getUser();
          (r?.id !== i?.id || r?.ip_address !== i?.ip_address) && (fr(), r = i);
        }), t === "route" && Hc(({ from: o, to: i }) => {
          o !== i && (hs({ ignoreDuration: !0 }), fr());
        });
      }
    };
  }, xE = "CultureContext", kE = (() => ({
    name: xE,
    preprocessEvent(e) {
      const t = ks();
      t && (e.contexts = {
        ...e.contexts,
        culture: { ...t, ...e.contexts?.culture }
      });
    },
    processSegmentSpan(e) {
      const t = ks();
      t && (e.attributes = {
        "culture.locale": t.locale,
        "culture.timezone": t.timezone,
        "culture.calendar": t.calendar,
        ...e.attributes
      });
    }
  })), DE = kE;
  function ks() {
    try {
      const e = H.Intl;
      if (!e)
        return;
      const t = e.DateTimeFormat().resolvedOptions();
      return {
        locale: t.locale,
        timezone: t.timeZone,
        calendar: t.calendar
      };
    } catch {
      return;
    }
  }
  const $E = "GlobalHandlers", FE = ((e = {}) => {
    const t = {
      onerror: !0,
      onunhandledrejection: !0,
      ...e
    };
    return {
      name: $E,
      setupOnce() {
        Error.stackTraceLimit = 50;
      },
      setup(n) {
        t.onerror && (BE(n), Ds("onerror")), t.onunhandledrejection && (UE(n), Ds("onunhandledrejection"));
      }
    };
  }), jE = FE;
  function BE(e) {
    hg((t) => {
      const { stackParser: n, attachStacktrace: r } = Wc();
      if (X() !== e || Bc())
        return;
      const { msg: o, url: i, line: s, column: a, error: c } = t, u = WE(
        wo(n, c || o, void 0, r, !1),
        i,
        s,
        a
      );
      u.level = "error", Sc(u, {
        originalException: c,
        mechanism: {
          handled: !1,
          type: "auto.browser.global_handlers.onerror"
        }
      });
    });
  }
  function UE(e) {
    gg((t) => {
      const { stackParser: n, attachStacktrace: r } = Wc();
      if (X() !== e || Bc())
        return;
      const o = HE(t), i = kn(o) ? zE(o) : wo(n, o, void 0, r, !0);
      i.level = "error", Sc(i, {
        originalException: o,
        mechanism: {
          handled: !1,
          type: "auto.browser.global_handlers.onunhandledrejection"
        }
      });
    });
  }
  function HE(e) {
    if (kn(e))
      return e;
    try {
      if ("reason" in e)
        return e.reason;
      if ("detail" in e && "reason" in e.detail)
        return e.detail.reason;
    } catch {
    }
    return e;
  }
  function zE(e) {
    return {
      exception: {
        values: [
          {
            type: "UnhandledRejection",
            // String() is needed because the Primitive type includes symbols (which can't be automatically stringified)
            value: `Non-Error promise rejection captured with value: ${String(e)}`
          }
        ]
      }
    };
  }
  function WE(e, t, n, r) {
    const o = e.exception = e.exception || {}, i = o.values = o.values || [], s = i[0] = i[0] || {}, a = s.stacktrace = s.stacktrace || {}, c = a.frames = a.frames || [];
    return c.length === 0 && c.push({
      colno: r,
      lineno: n,
      filename: VE(t) ?? ho(),
      function: qe,
      in_app: !0
    }), e;
  }
  function Ds(e) {
    Un && O.log(`Global Handler attached: ${e}`);
  }
  function Wc() {
    return X()?.getOptions() || {
      stackParser: () => [],
      attachStacktrace: !1
    };
  }
  function VE(e) {
    if (!(!Te(e) || e.length === 0))
      return e.startsWith("data:") ? `<${FA(e, !1)}>` : e;
  }
  const qE = () => ({
    name: "HttpContext",
    preprocessEvent(e) {
      if (!H.navigator && !H.location && !H.document)
        return;
      const t = v_(), n = {
        ...t.headers,
        ...e.request?.headers
      };
      e.request = {
        ...t,
        ...e.request,
        headers: n
      };
    }
  }), GE = "cause", JE = 5, KE = "LinkedErrors", ZE = ((e = {}) => {
    const t = e.limit || JE, n = e.key || GE;
    return {
      name: KE,
      preprocessEvent(r, o, i) {
        const s = i.getOptions();
        n_(
          // This differs from the LinkedErrors integration in core by using a different exceptionFromError function
          bo,
          s.stackParser,
          n,
          t,
          r,
          o
        );
      }
    };
  }), YE = ZE;
  function XE() {
    return QE() ? (Un && gt(() => {
      console.error(
        "[Sentry] You cannot use Sentry.init() in a browser extension, see: https://docs.sentry.io/platforms/javascript/best-practices/browser-extensions/"
      );
    }), !0) : !1;
  }
  function QE() {
    if (typeof H.window > "u")
      return !1;
    const e = H;
    if (e.nw || !(e.chrome || e.browser)?.runtime?.id)
      return !1;
    const n = ho();
    return !(H === H.top && /^(?:chrome-extension|moz-extension|ms-browser-extension|safari-web-extension):\/\//.test(n));
  }
  function eb(e) {
    return [
      // TODO(v11): Replace with `eventFiltersIntegration` once we remove the deprecated `inboundFiltersIntegration`
      // eslint-disable-next-line deprecation/deprecation
      JA(),
      WA(),
      h_(),
      RE(),
      gE(),
      jE(),
      YE(),
      c_(),
      qE(),
      DE(),
      LE()
    ];
  }
  function tb(e = {}) {
    const t = !e.skipBrowserExtensionCheck && XE();
    let n = e.defaultIntegrations == null ? eb() : e.defaultIntegrations;
    const r = {
      ...e,
      enabled: t ? !1 : e.enabled,
      stackParser: dg(e.stackParser || pE),
      integrations: dA({
        integrations: e.integrations,
        defaultIntegrations: n
      }),
      transport: e.transport || Q_
    };
    return DA(D_, r);
  }
  const nb = !1, rb = {
    itIsProductionDomain() {
      const e = window.location.hostname;
      return e.endsWith(".acronis.com") && !e.includes(".gcp.") && !e.includes(".webdev.") || e === "acronis.events";
    },
    isEnabled() {
      return this.itIsProductionDomain();
    }
  }, ob = [
    /extensions\//i,
    /chrome-extension:/i,
    /chrome:\/\//i,
    /webkit-masked-url:\/\//i,
    /^https:\/\/cdn\.cookielaw\.org\//i,
    /^https:\/\/munchkin.marketo.net\//i,
    /^https:\/\/www.googletagmanager.com\//i,
    /^https:\/\/script.hotjar.com\//i,
    /ping[.]min[.]js/i,
    /gtm[.]js/i,
    /6si[.]min[.]js/i,
    /clarity[.]js/i,
    /fbevents[.]js/i
  ], ib = [
    // Sentry recommended rules
    "top.GLOBALS",
    "originalCreateNotification",
    "canvas.contentDocument",
    "fb_xd_fragment",
    "conduitPage",
    // Well-known 3rd party integrations
    "MktoForms2",
    "google_tag_manager",
    "unsafe-eval",
    "Munchkin",
    // Custom rules
    /ResizeObserver/i,
    // probably an error from a vendor lib. Should not affect users
    "The quota has been exceeded",
    // inaccessible localstorage, usually fired because of OneTrust wrapping
    /Loading chunk \d+ failed/i,
    // flacky kubectl rollout
    /Failed to fetch/i,
    // fetch API errors (excluded because without a context it's just a noise)
    /^TypeError: Load failed$/i,
    // looks like the same as above, but for iOS Safari
    /NetworkError/i,
    // we can't do anything with users connection quality
    /video-only background media was paused/i,
    // has no value and besides a completely normal behavior
    /The play\(\) request/i,
    // same
    /reCAPTCHA placeholder element must be an element or id/i,
    // judging by breadcrumbs it happens when a user opens a form without OneTrust consent given
    /Object Not Found Matching Id/i,
    // same
    /Avoided redundant navigation/i,
    // should not be an error at all
    /evaluating 'r\["@[^"]+"\].toLowerCase'/i,
    // For fucks sake Safari tries to parse and process ld+json schema, but of course does it wrong
    /NotAllowedError/i,
    // restrictive environments, not our fault
    /SecurityError/i,
    // same
    /Non-Error promise rejection captured with value: Timeout/i,
    // no idea what's this but anyway we don't need to ingest timeout errors
    /CustomEvent/i,
    // no idea, non-reproducible, mostly Safari
    /\s+fbq/i,
    // too many and totally useless
    /property 'createElement' is a read-only and non-configurable/i
    // microfrontend issues on Solutions. Can be removed after the rework
  ], sb = () => {
    if (!rb.isEnabled())
      return;
    const e = G("public_context");
    tb({
      debug: nb,
      release: `head-site-main@browser@${e.appVersion}`,
      dsn: "https://cd987e00d8a4970ecf4d96f763fa9798@o394472.ingest.sentry.io/6232185",
      maxBreadcrumbs: 10,
      attachStacktrace: !0,
      denyUrls: ob,
      ignoreErrors: ib,
      sampleRate: 0.1
    });
  };
  function ab(e) {
    return new Promise((t) => setTimeout(t, e));
  }
  async function cb() {
    const e = G("vwo_opts", !1);
    if (!e)
      return window.VWO_OUTCOME;
    for (let t = 0; t < e.pollMaxAttempts; t++)
      if (window.VWO_OUTCOME === "pending")
        await ab(e.pollInterval);
      else
        return window.VWO_OUTCOME;
    return window.VWO_OUTCOME;
  }
  let Vc = -1;
  const _t = (e) => {
    addEventListener("pageshow", (t) => {
      t.persisted && (Vc = t.timeStamp, e(t));
    }, !0);
  }, ye = (e, t, n, r) => {
    let o, i;
    return (s) => {
      t.value >= 0 && (s || r) && (i = t.value - (o ?? 0), (i || o === void 0) && (o = t.value, t.delta = i, t.rating = ((a, c) => a > c[1] ? "poor" : a > c[0] ? "needs-improvement" : "good")(t.value, n), e(t)));
    };
  }, vo = (e) => {
    requestAnimationFrame(() => requestAnimationFrame(e));
  }, To = () => {
    const e = performance.getEntriesByType("navigation")[0];
    if (e && e.responseStart > 0 && e.responseStart < performance.now()) return e;
  }, Jt = () => To()?.activationStart ?? 0, Ae = (e, t = -1) => {
    const n = To();
    let r = "navigate";
    return Vc >= 0 ? r = "back-forward-cache" : n && (document.prerendering || Jt() > 0 ? r = "prerender" : document.wasDiscarded ? r = "restore" : n.type && (r = n.type.replace(/_/g, "-"))), { name: e, value: t, rating: "good", delta: 0, entries: [], id: `v5-${Date.now()}-${Math.floor(8999999999999 * Math.random()) + 1e12}`, navigationType: r };
  }, mr = /* @__PURE__ */ new WeakMap();
  function Co(e, t) {
    return mr.get(e) || mr.set(e, new t()), mr.get(e);
  }
  class ub {
    t;
    i = 0;
    o = [];
    h(t) {
      if (t.hadRecentInput) return;
      const n = this.o[0], r = this.o.at(-1);
      this.i && n && r && t.startTime - r.startTime < 1e3 && t.startTime - n.startTime < 5e3 ? (this.i += t.value, this.o.push(t)) : (this.i = t.value, this.o = [t]), this.t?.(t);
    }
  }
  const Kt = (e, t, n = {}) => {
    try {
      if (PerformanceObserver.supportedEntryTypes.includes(e)) {
        const r = new PerformanceObserver((o) => {
          queueMicrotask(() => {
            t(o.getEntries());
          });
        });
        return r.observe({ type: e, buffered: !0, ...n }), r;
      }
    } catch {
    }
  }, Ro = (e) => {
    let t = !1;
    return () => {
      t || (e(), t = !0);
    };
  };
  let rt = -1;
  const qc = /* @__PURE__ */ new Set(), $s = () => document.visibilityState !== "hidden" || document.prerendering ? 1 / 0 : 0, Vr = (e) => {
    if (document.visibilityState === "hidden") {
      if (e.type === "visibilitychange") for (const t of qc) t();
      isFinite(rt) || (rt = e.type === "visibilitychange" ? e.timeStamp : 0, removeEventListener("prerenderingchange", Vr, !0));
    }
  }, Hn = () => {
    if (rt < 0) {
      const e = Jt();
      rt = (document.prerendering ? void 0 : globalThis.performance.getEntriesByType("visibility-state").find((n) => n.name === "hidden" && n.startTime >= e)?.startTime) ?? $s(), addEventListener("visibilitychange", Vr, !0), addEventListener("prerenderingchange", Vr, !0), _t(() => {
        setTimeout(() => {
          rt = $s();
        });
      });
    }
    return { get firstHiddenTime() {
      return rt;
    }, onHidden(e) {
      qc.add(e);
    } };
  }, zn = (e) => {
    document.prerendering ? addEventListener("prerenderingchange", e, !0) : e();
  }, Fs = [1800, 3e3], Gc = (e, t = {}) => {
    zn(() => {
      const n = Hn();
      let r, o = Ae("FCP");
      const i = Kt("paint", (s) => {
        for (const a of s) a.name === "first-contentful-paint" && (i.disconnect(), a.startTime < n.firstHiddenTime && (o.value = Math.max(a.startTime - Jt(), 0), o.entries.push(a), r(!0)));
      });
      i && (r = ye(e, o, Fs, t.reportAllChanges), _t((s) => {
        o = Ae("FCP"), r = ye(e, o, Fs, t.reportAllChanges), vo(() => {
          o.value = performance.now() - s.timeStamp, r(!0);
        });
      }));
    });
  }, js = [0.1, 0.25], lb = (e, t = {}) => {
    const n = Hn();
    Gc(Ro(() => {
      let r, o = Ae("CLS", 0);
      const i = Co(t, ub), s = (c) => {
        for (const u of c) i.h(u);
        i.i > o.value && (o.value = i.i, o.entries = i.o, r());
      }, a = Kt("layout-shift", s);
      a && (r = ye(e, o, js, t.reportAllChanges), n.onHidden(() => {
        s(a.takeRecords()), r(!0);
      }), _t(() => {
        i.i = 0, o = Ae("CLS", 0), r = ye(e, o, js, t.reportAllChanges), vo(r);
      }), setTimeout(r));
    }));
  };
  let Jc = 0, gr = 1 / 0, an = 0;
  const fb = (e) => {
    for (const t of e) t.interactionId && (gr = Math.min(gr, t.interactionId), an = Math.max(an, t.interactionId), Jc = an ? (an - gr) / 7 + 1 : 0);
  };
  let qr;
  const Bs = () => qr ? Jc : performance.interactionCount ?? 0, db = () => {
    "interactionCount" in performance || qr || (qr = Kt("event", fb, { durationThreshold: 0 }));
  };
  let Us = 0;
  class pb {
    l = [];
    u = /* @__PURE__ */ new Map();
    m;
    p;
    v() {
      Us = Bs(), this.l.length = 0, this.u.clear();
    }
    T() {
      const t = Math.min(this.l.length - 1, Math.floor((Bs() - Us) / 50));
      return this.l[t];
    }
    h(t) {
      if (this.m?.(t), !t.interactionId && t.entryType !== "first-input") return;
      const n = this.l.at(-1);
      let r = this.u.get(t.interactionId);
      if (r || this.l.length < 10 || t.duration > n.L) {
        if (r ? t.duration > r.L ? (r.entries = [t], r.L = t.duration) : t.duration === r.L && t.startTime === r.entries[0].startTime && r.entries.push(t) : (r = { id: t.interactionId, entries: [t], L: t.duration }, this.u.set(r.id, r), this.l.push(r)), this.l.sort((o, i) => i.L - o.L), this.l.length > 10) {
          const o = this.l.splice(10);
          for (const i of o) this.u.delete(i.id);
        }
        this.p?.(r);
      }
    }
  }
  const Kc = (e) => {
    const t = globalThis.requestIdleCallback || setTimeout, n = globalThis.cancelIdleCallback || clearTimeout;
    if (document.visibilityState === "hidden") e();
    else {
      const r = Ro(e);
      let o = -1;
      const i = () => {
        n(o), r();
      };
      addEventListener("visibilitychange", i, { once: !0, capture: !0 }), o = t(() => {
        removeEventListener("visibilitychange", i, { capture: !0 }), r();
      });
    }
  }, Hs = [200, 500], hb = (e, t = {}) => {
    if (!globalThis.PerformanceEventTiming || !("interactionId" in PerformanceEventTiming.prototype)) return;
    const n = Hn();
    zn(() => {
      db();
      let r, o = Ae("INP");
      const i = Co(t, pb), s = (c) => {
        Kc(() => {
          for (const l of c) i.h(l);
          const u = i.T();
          u && u.L !== o.value && (o.value = u.L, o.entries = u.entries, r());
        });
      }, a = Kt("event", s, { durationThreshold: t.durationThreshold ?? 40 });
      r = ye(e, o, Hs, t.reportAllChanges), a && (a.observe({ type: "first-input", buffered: !0 }), n.onHidden(() => {
        s(a.takeRecords()), r(!0);
      }), _t(() => {
        i.v(), o = Ae("INP"), r = ye(e, o, Hs, t.reportAllChanges);
      }));
    });
  };
  class mb {
    m;
    h(t) {
      this.m?.(t);
    }
  }
  const zs = [2500, 4e3], gb = (e, t = {}) => {
    zn(() => {
      const n = Hn();
      let r, o = Ae("LCP");
      const i = Co(t, mb), s = (c) => {
        t.reportAllChanges || (c = c.slice(-1));
        for (const u of c) i.h(u), u.startTime < n.firstHiddenTime && (o.value = Math.max(u.startTime - Jt(), 0), o.entries = [u], r());
      }, a = Kt("largest-contentful-paint", s);
      if (a) {
        r = ye(e, o, zs, t.reportAllChanges);
        const c = Ro(() => {
          s(a.takeRecords()), a.disconnect(), r(!0);
        }), u = (l) => {
          l.isTrusted && (Kc(c), removeEventListener(l.type, u, { capture: !0 }));
        };
        for (const l of ["keydown", "click", "visibilitychange"]) addEventListener(l, u, { capture: !0 });
        _t((l) => {
          o = Ae("LCP"), r = ye(e, o, zs, t.reportAllChanges), vo(() => {
            o.value = performance.now() - l.timeStamp, r(!0);
          });
        });
      }
    });
  }, Ws = [800, 1800], Gr = (e) => {
    document.prerendering ? zn(() => Gr(e)) : document.readyState !== "complete" ? addEventListener("load", () => Gr(e), !0) : setTimeout(e);
  }, yb = (e, t = {}) => {
    let n = Ae("TTFB"), r = ye(e, n, Ws, t.reportAllChanges);
    Gr(() => {
      const o = To();
      o && (n.value = Math.max(o.responseStart - Jt(), 0), n.entries = [o], r(!0), _t(() => {
        n = Ae("TTFB", 0), r = ye(e, n, Ws, t.reportAllChanges), r(!0);
      }));
    });
  };
  function Ab() {
    const e = /* @__PURE__ */ new Set(), t = {
      INP: "interaction-to-next-paint",
      LCP: "largest-contentful-paint",
      FCP: "first-contentful-paint",
      TTFB: "time-to-first-byte",
      CLS: "cumulative-layout-shift"
    }, n = G("public_context"), r = (i) => {
      const s = t[i.name];
      if (!s) {
        pe.error(`Unknown metric name: ${i.name}`);
        return;
      }
      const a = {
        latency: i.value,
        url: window.location.href,
        bag: []
      };
      switch (i.name) {
        case "INP":
          a.bag = i.entries.map((d) => ({
            eventName: d.name,
            targetClasses: d.target?.className
          }));
          break;
        case "LCP":
          a.bag = i.entries.map((d) => ({
            loadTime: d.loadTime,
            renderTime: d.renderTime,
            size: d.size,
            url: d.url,
            targetClasses: d.element?.className
          }));
          break;
        case "FCP":
          break;
        case "TTFB":
          a.bag = i.entries.map((d) => ({
            size: d.decodedBodySize,
            domComplete: d.domComplete,
            domInteractive: d.domInteractive,
            download: d.responseEnd - d.responseStart,
            status: d.responseStatus
          }));
          break;
        case "CLS":
          a.bag = i.entries.map((d) => ({
            startTime: d.startTime,
            value: d.value,
            shifts: d.sources.map((y) => y.node?.className)
          }));
          break;
      }
      const c = JSON.stringify(a), u = { type: "application/json" }, l = new Blob([c], u), f = navigator.sendBeacon.bind(navigator), p = `${n.env.HEAD_SITE_MAIN_PUBLIC_BASE_URL_SERVICE_API_CLIENT}/svc/v1/metrics/latency/${s}`;
      f(p, l);
    }, o = (i) => {
      if (!e.has(i.name))
        e.add(i.name);
      else
        return;
      r(i);
    };
    try {
      hb(r), gb(o), Gc(o), yb(o), lb(r);
    } catch (i) {
      pe.error({ error: i });
    }
  }
  class _b {
    youtubeAPIisReallyReady = !1;
    subscribers = [];
    constructor() {
      window.onYouTubePlayerAPIReady = () => {
        this.youtubeAPIisReallyReady = !0, this.subscribers.forEach((t) => t());
      };
    }
    subscribe(t) {
      this.youtubeAPIisReallyReady ? t() : this.subscribers.push(t);
    }
  }
  function Ne(e, t) {
    try {
      e();
    } catch (n) {
      pe.error(n, `Failed to boot ${t}`);
    }
  }
  async function Vs(e, t) {
    try {
      await e();
    } catch (n) {
      pe.error(n, `Failed to boot ${t}`);
    }
  }
  async function Eb() {
    await Vs(ng, "bootSVGSprite"), Ne(eg, "splits"), Ne(Ab, "sendWebVitals"), Ne(sb, "initSentry"), Ne(Dm, "increaseNavvaticCounter"), window.youtubePlayerReadyBus = new _b(), Ne(rg, "monkeyPatchVWO"), Ne(um, "injectVWOCode"), Ne(og, "initVWO"), Ne(cm, "initGTM"), Vs(km, "initMatomo"), typeof window < "u" && window.OptanonActiveGroups?.includes("C0002") && window.ytTracker?.init(), await cb() !== "redirect" && (await rm(), Hm(), Bm());
  }
  Eb();
});
export default bb();
//# sourceMappingURL=header.D22XXGnV.js.map
